/** Copy shared by the IDC step tables, so the same control never gets two wordings. */

/** Shown by the table when the search/filter set matches nothing (the list itself is not empty). */
export const IDC_FILTER_EMPTY_MESSAGE = '조건에 맞는 결과가 없어요.';

/** IDC rows are identified by host, not by the scan-assigned Resource Name/ID pair the cloud
 *  toolbar promises — the search matches what the 연동 대상 column shows. */
export const IDC_SEARCH_PLACEHOLDER = 'Host 또는 IP 검색';
