/** Ops console — Target Source 운영 목록 (검색 → 단건 운영 화면). */
import { TargetSourceListView } from '@/app/admin/pipelines/ops/target-sources/_components/TargetSourceListView';

export default function OpsTargetSourcesPage() {
  return <TargetSourceListView />;
}
