# v16 Review — Round 1 findings (Workflow, 28 cells)

28 cells reviewed (opus, font/design/transition/modal). **9 blockers · 35 majors · 75 minors.**

needs-work: azure-s5, azure-s6, gcp-s5, gcp-s7, aws-s4, aws-s5, aws-s7, idc-s6

## Blockers
- **[azure-s5][design]** v16 step5 is a single full-width card 연결 테스트 (HTML 6885 data-prov-view="azure gcp aws") containing the conn-progress bar + the connection table + footer. Impl renders no such card; the connection test is a small max-w-md gray box (ConnectionTestPanel.tsx:136) sitting between two unrelated tables. Whole-section layout does not match.
- **[azure-s5][design]** v16 connection table (HTML 6912-6960) has DB Credential as an inline <select> (자격 증명 선택/Key1..), a Connection Status tag column (Success green / Pending orange, block2 L611-615), and a 논리 DB 확인 설정 ghost-button column. The impl 연동 대상 정보 table (ConfirmedIntegrationTable) has none of these interactive columns - DB Credential shows static text and there is no per-row Connection Status tag or 설정 action.
- **[azure-s5][transition]** v16 footer button 완료 승인 요청 (HTML 6995, id=connApproveBtn) is entirely missing from cloud step5 impl. grep for 완료 승인 요청 in cloud layout returns only IDC components; cloud relies on auto-advance via SUCCESS polling instead of an explicit approval-request action.
- **[azure-s5][modal]** v16 #reqApprovalModal (HTML 8086, 760px, 3 approval-stats + 논리 DB 개수/제외한 논리 DB table + 취소/요청하기) has no cloud implementation; only the IDC variant (IdcReqApprovalModal.tsx) exists. Cloud step5 cannot open this modal.
- **[gcp-s5][design]** v16 step 5 is a single unified 연결 테스트 card (lines 6883-6996) containing the DB-list table, progress bar, Run Test, and 완료 승인 요청. Impl renders a separate wide 연동 대상 정보 table card + a small max-w-md bg-gray-50 연결 테스트 panel (ConnectionTestPanel.tsx:136, ConfirmedResourcesSlot.tsx). Information architecture differs fundamentally.
- **[gcp-s5][modal]** v16 완료 승인 요청 modal (Step 5, lines 8086-8146: 760px, 3 stat tiles, 5-col 논리 DB table, 취소/요청하기) is absent from the impl step-5 card; impl exposes only CredentialSetupModal instead (ConnectionTestPanel.tsx:188).
- **[aws-s5][modal]** Missing 완료 승인 요청 modal. v16 reqApprovalModal (lines 8086-8146: rm-eyebrow 'Step 5 · 연결 테스트', approval-stats 3-grid, 5-col summary table, 취소/요청하기 footer) opened by connApproveBtn (line 6995) has no impl counterpart in the step-5 components.
- **[aws-s5][transition]** Missing 완료 승인 요청 action button. v16 card footer has id=connApproveBtn -> openReqApproval() (line 6995) gated by line 6993; impl step 5 (ConnectionTestPanel + slots) exposes only 연결 테스트 수행 and no completion/approval-request affordance, so the user cannot advance step 5 the way v16 prescribes.
- **[aws-s5][design]** Single-card vs three-card structure. v16 packs progress bar + test table + credential select + 논리 DB 확인 column + approval footer into one 연결 테스트 card (lines 6884-6998); impl splits into 연동 대상 정보 (ConfirmedResourcesSlot) + a max-w-md 연결 테스트 box (ConnectionTestPanel) + 논리 DB 확인 (LogicalDbSlot) — a different information architecture.

## Majors
- **[azure-s2][design]** Resource ID truncation direction is reversed. v16 .res-id-text uses 'direction: rtl; text-align: left' (HTML L2897) so it truncates from the LEFT and shows the distinguishing tail (e.g. '...DBforMySQL/servers/mysql-prod-01'). Impl ResourceIdCell.tsx L28 uses '[direction:ltr]', truncating from the RIGHT, so every row shows only the identical common prefix '/subscriptions/2867a4f9-1234-567...' and the meaningful server name is hidden. Visible in both screenshots. Fix: switch the res-id span to rtl/left like v16.
- **[azure-s3][design]** Toolbar first filter wrong: v16 step-3 cloud toolbar = [연동 상태·전체 {Integrated/Pending/제외}, DB Type·전체] (v16 HTML lines 6379-6389), but impl WaitingApprovalToolbar renders [DB Type·전체, Region·전체] (WaitingApprovalToolbar.tsx lines 41,48). The impl reuses the step-2 toolbar verbatim and never surfaces the 연동 상태 (integration-status) filter that v16 introduces specifically for the applying view.
- **[azure-s5][transition]** Run Test affordance label/behavior mismatch: v16 primary button text is 'Run Test' with a play-triangle icon (HTML 6893-6896); impl uses 연결 테스트 수행 / 테스트 진행 중... (ConnectionTestPanel.tsx:167). Also v16 has an always-visible conn-progress bar with 성공/실패/대기 counts and pct (HTML 6899-6911); impl shows a ProgressBar only while PENDING and with no counts row.
- **[azure-s5][design]** v16 has no separate 논리 DB 확인 section - logical-DB entry is the 설정 button inside the connection table (HTML 6940 openLogicalModal). Impl adds a standalone 논리 DB 확인 card (LogicalDbSlot.tsx) with its own table cols 리소스 ID | DB 타입 | 작업 and a 논리 DB 확인 outline button, which is not in v16 step5.
- **[azure-s5][fontSize]** Connection-test heading is 14px/600 (h4, ConnectionTestPanel.tsx:139) vs v16 26px/800 (.card-header h2, block2 L403-405). The 논리 DB 확인 heading is 15px/600 (LogicalDbSlot.tsx:46) vs v16 card-header h2 26px/800; table th 12px/medium vs v16 13px/700.
- **[azure-s6][modal]** v16 confirm-rewind modal absent. v16 retest button (line 7173) -> openConfirmStep('retest') opens confirmStepModal '연결 테스트를 다시 실행할까요?' with 머무르기/되돌아가기 and setStep(5). Impl fires toast.info('연결 테스트 재실행 기능 준비중입니다.') — modal not implemented.
- **[azure-s6][transition]** Retest affordance behavior is a stub. v16 rewinds to step 5 (setStep(5)) via the confirm modal; impl only shows a '기능 준비중' toast (ConnectionVerifiedStep.tsx L36; asserted in test L113-117). Button looks correct but does nothing functional.
- **[azure-s6][design]** Pagination row missing. v16 step-6 card has .pagination-row (표시 N건씩 / 1–2 / 전체 N건 / « ‹ 1 › » pager, HTML 7140-7168). Impl (ConfirmedIntegrationTable via ConfirmedResourcesSlot bare) renders a bare <table> with no pager.
- **[azure-s6][design]** Resource ID cell not truncated. v16 .res-id-text is single-line ellipsized inside .res-id-cell with a hover copy button (HTML 7110-7115, CSS 2885-2900). Impl renders the full resourceId with no truncate, wrapping to ~3 lines and inflating row height/table density.
- **[azure-s7][modal]** v16 step-7 인프라 변경 / 연결 테스트 재실행 buttons open confirmStepModal (#confirmStepModal, v16 HTML 8212-8237: title '정말 되돌아가시겠어요?', 머무르기 + 되돌아가기 footer). Impl InstallationCompleteStep.tsx wires both to toast.info('...기능 준비중입니다') stubs — the confirm-rewind modal does not exist in the impl.
- **[azure-s7][transition]** v16 step-7 action buttons drive a real flow (openConfirmStep -> confirmStepProceed re-checks Agent / rewinds). Impl buttons are non-functional toast stubs, so the re-test / infra-change affordance is present visually but has no state behavior.
- **[gcp-s4][design]** v16 renders step 4 as a single card titled 'Agent 설치' (h2 26px/800, .card-header L6584/CSS L908) with subtitle '승인된 인프라에 PII Agent를 배포하기 위한 설치 작업을 진행합니다.' (12px fg-3) and 'Provider: GCP' (11.5px) in the header. The impl has no such card title - install content sits under a small 'GCP 에이전트 설치 상태' label (text-sm/semibold ~14px, GcpInstallationInline.tsx L64) + 마지막 확인 timestamp, and 'Provider:' is relocated to the page-meta as a 16/700 dotted badge. The card's primary heading hierarchy (26/800) is absent.
- **[gcp-s5][design]** v16 connection table has a per-row DB Credential <select> dropdown and a 논리 DB 확인 설정 button column (lines 6920-6922, 6938-6940). Neither exists in the impl connection-test card.
- **[gcp-s5][transition]** v16 has a top-right Run Test button + persistent conn-progress bar with 성공/실패/대기 counts and % and a 완료 승인 요청 CTA gated on all-Success + 논리DB-done (lines 6893, 6899-6911, 6995, 10231-10267). Impl has only a single 연결 테스트 수행 button with a transient ProgressBar and no approval CTA in this card.
- **[gcp-s5][fontSize]** v16 card title h2 26px/800 (line 909) vs impl h4 ~14px/600 (ConnectionTestPanel.tsx:139) and 15px/600 (ConfirmedResourcesSlot.tsx:36); v16 table th 13px/700 (line 1863) vs impl 12px/500 (ConfirmedIntegrationTable.tsx:78).
- **[gcp-s6][modal]** v16 step 6 retest button (HTML 7173: onclick=openConfirmStep('retest')) opens the confirm-rewind modal (title '연결 테스트를 다시 실행할까요?', desc references 5단계, note '6·7단계 상태 초기화', then setStep(5)). The impl ConnectionVerifiedStep.tsx (ConnectionVerifiedRetestButton, line 36) instead fires toast.info('연결 테스트 재실행 기능 준비중입니다.'). The matching ConfirmStepModal component exists but is not wired here - the rewind affordance is behaviorally absent on this step.
- **[gcp-s6][design]** Pagination row missing. v16 step 6 renders a full pagination-row (HTML 7140-7168: '표시 [10/20/50/100] 건씩', '1-2 / 전체 2건', and «‹1›» page buttons) below the table. The impl table (ConfirmedIntegrationTable, default pre-install variant via ConfirmedResourcesSlot bare) renders no pagination at all - it jumps straight from the last row to the retest button.
- **[gcp-s7][design]** Database Type column rendered as plain text (MYSQL/POSTGRESQL) in ConfirmedIntegrationTable.tsx (<td className={cellClass}>{resource.databaseType}</td>), but v16 renders it as a colored pill: tag.blue (#E8F1FF bg / #1747B5 text) for MySQL (v16 L7302/7320) and inline #EEF2FF/#4338CA pill for PostgreSQL (v16 L7338).
- **[gcp-s7][design]** Resource ID rendered as full, line-wrapping monospace text (wraps to 2 lines, no ellipsis) in ConfirmedIntegrationTable monoCellClass. v16 .res-id-cell{max-width:280px} + .res-id-text{overflow:hidden; text-overflow:ellipsis; white-space:nowrap; direction:rtl; font-size:12px} (v16 block-2 L2885-2899) yields a single-line, left-truncated id with leading ellipsis (e.g. ...datasets/sea_bdp_prd), as seen in rendered v16.
- **[gcp-s7][modal]** Confirm-rewind modal missing. v16 step-7 buttons openConfirmStep('infra')/('retest') open #confirmStepModal (design L8212; config L10131) to confirm process rewind to step 1 / step 5 before resetting later steps. Impl InstallationCompleteStep.tsx (L41,47,55) wires both to toast.info('... 준비중입니다') with no modal and no navigation.
- **[aws-s4][design]** AWS step-4 resource table missing. v16 renders db-list-table (data-prov-view includes aws, lines 6803-6876): Database Type | Resource ID | Region | Resource Name | VPC Endpoint 상태 + pagination row. Impl AwsInstallationInline.tsx renders only InstallTaskPipeline; InstallResourceTable is never mounted for AWS (it IS for Azure/GCP via Azure/GcpInstallationInline). Net: cards then empty space.
- **[aws-s4][modal]** AWS step-4 cards do not open the task-detail modal. v16 cards use openTaskDetailModal(title,'aws') (6641/6649/6657). buildAwsAutoItems/buildAwsManualItems omit onClick, so cards are inert. InstallTaskDetailModal is wired for GCP only.
- **[aws-s5][design]** Missing conn-progress status bar. v16 #connProgress (HTML lines 6899-6911, CSS 2552-2645) is a state-colored bar (idle/pending/running/success/fail) with cp-label, cp-counts (성공/실패/대기), cp-pct, animated cp-fill+shimmer. Impl ProgressBar.tsx is a plain gray bar shown only during PENDING with no state coloring, counts, or percent token (16/800).
- **[aws-s5][design]** DB Credential rendered as plain text instead of an inline <select>. v16 uses .idc-cred-select dropdown per row (lines 6938/6954, CSS 2622-2635, options Key1/Key2/Key3/...) which also gates Run Test; impl ConfirmedIntegrationTable.tsx:177 renders credentialId as static text.
- **[aws-s5][design]** Missing 논리 DB 확인 column in the test table. v16 connTest table has a 7th column 논리 DB 확인 with a 설정 button per row (th line 6922, td 6940). Impl's pre-install table (ConfirmedIntegrationTable.tsx:149-156) has only 6 columns and instead exposes 논리 DB 확인 as a wholly separate card/table.
- **[aws-s5][fontSize]** Card title scale far below v16. v16 .card-header h2 = 26px/800 (lines 908-913); impl section titles are text-[15px] font-semibold (ConfirmedResourcesSlot/LogicalDbSlot) and text-sm h4 (ConnectionTestPanel). No 26/800 headline present.
- **[aws-s7][design]** Database Type column rendered as plain text. v16 wraps each value in a colored tag pill — <span class="tag blue">MySQL</span> (#E8F1FF/#1747B5) and indigo for PostgreSQL (HTML 7302, 7338). Impl ConfirmedIntegrationTable.tsx:110 (variant complete) renders {resource.databaseType ?? '-'} via cellClass = plain grey uppercase text, no pill. The idcStyles.tag.blue token (bg-[#E8F1FF] text-[#1747B5]) already exists and matches v16 exactly but is unused here. Visually obvious in screenshot.
- **[aws-s7][modal]** Step-7 confirm-rewind modal missing. v16 #confirmStepModal (HTML 8212) is opened by 인프라 변경 -> openConfirmStep('infra') (rewind to Step 1) and 연결 테스트 재실행 -> openConfirmStep('retest') (rewind to Step 5). Impl InstallationCompleteStep.tsx:41 stubs both via toast.info('기능 준비중입니다'); ConfirmStepModal exists in the codebase but is not wired into step 7.
- **[aws-s7][transition]** Rewind affordance non-functional. v16 step-7 buttons drive a real state reset (Step6/7 -> Step5/1) through the confirm modal + confirmStepProceed(); impl replaces both with a placeholder toast, so neither 인프라 변경 nor 연결 테스트 재실행 performs any action.
- **[idc-s2][design]** Pagination row missing. v16 IDC step-2 (HTML 6321-6334) shows '표시 [10] 건씩 · 1–3 / 전체 3건 · ‹ [1] ›' below the table; impl IdcStep2WaitingApproval + IdcResourceTable render no pagination at all — the table goes straight to the footer cancel button (visible in screenshot).
- **[idc-s3][transition]** Missing in-card primary Next button. v16 IDC step-3 variant has a right-aligned <button class=btn primary onclick=setStep(4)>Next</button> (HTML lines 6569-6574); impl IdcStep3Applying.tsx renders nothing below the table.
- **[idc-s4][design]** v16 step-4 card header block is missing. v16 wraps IDC step 4 in a .card whose header is <h2>Agent 설치</h2> (26px/800) + subtitle '승인된 인프라에 PII Agent를 배포하기 위한 설치 작업을 진행합니다.' (13.5px/500) + a right-aligned 'Provider: <x>' meta (v16 L6581-6593, CSS L403-423). The impl (IdcStep4Installing.tsx:124-139) renders none of these; its only header is a 15px/700 '설치 진행 상태' label plus an added '설치 상태 새로고침' button.
- **[idc-s5][design]** conn-progress status bar missing. v16 IDC step 5 card (HTML 7014-7027, #connProgressIdc) renders an inner pill: cp-icon + cp-label '연결 테스트 대기 중 — Run Test를 실행해 주세요', cp-counts '성공 0 · 실패 0 · 대기 2', cp-pct '0%', and a cp-track/cp-fill bar (8px, #E4E7EC). Visible in the v16 render; entirely absent in IdcStep5ConnectionTest.tsx (no progress element — only a transient StepBanner while testing).
- **[idc-s6][design]** DB Credential column missing. v16 IDC step-6 table (HTML 7213 th width:150px + tbody data-idc-cols=credro) renders a read-only credential chip (key icon + mono name, e.g. idc_svc_mysql for the MySQL/Single row; — for the Oracle row) between Source IP and Connection Status. Impl IdcStep6ConnectionVerified.tsx:85 passes cols=[src,conn], so the column is absent and there is no read-only credro renderer in IdcResourceTable (only editable cred via IdcCredSelectCell).
- **[idc-s6][modal]** Confirm-rewind modal not wired. v16 연결 테스트 재실행 (step 6) calls openConfirmStep('retest') -> modal 연결 테스트를 다시 실행할까요? that rewinds to Step 5 and warns steps 6/7 reset (HTML 10133-10138). Impl replaces it with toast.info('연결 테스트 재실행 기능 준비중입니다.') (IdcStep6ConnectionVerified.tsx:28). ConfirmStepModal exists (app/components/ui/ConfirmStepModal.tsx) but is only bound to 승인 취소, not this retest button.

## Minors (count by cell)
aws-s1:3, aws-s2:5, aws-s3:2, aws-s4:3, aws-s5:3, aws-s6:2, aws-s7:2, azure-s1:4, azure-s2:3, azure-s3:2, azure-s4:4, azure-s5:1, azure-s6:3, azure-s7:2, gcp-s1:1, gcp-s2:5, gcp-s3:3, gcp-s4:3, gcp-s6:2, gcp-s7:2, idc-s1:3, idc-s2:4, idc-s3:4, idc-s4:3, idc-s5:2, idc-s6:1, idc-s7:3