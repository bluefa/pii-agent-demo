# 설치 화면 「설치 대상」 요약 줄 — 정보 표현 벤치마크 결정 기록

- **일자**: 2026-08-22
- **대상 화면**: `/pass/target-sources/{id}` 서비스측 설치 화면 헤더
  (`ProjectPageMeta` 제목 행 + 「설치 대상」 요약 바)
- **아티팩트**: https://claude.ai/code/artifact/9746b341-2e04-4435-b155-df0767fac2b9
  (진단 P1~P7 · 레퍼런스 13(확인 12 / 기억 1) · 개선안 A~E · 비교표)
- **선행**: `target-source-layout.md`(개선안 D, 같은 PR #747에서 요약 바를 만든 변경)
- **채택**: **시안 C — 경로가 정체성이다 (운영측 문법 이식)**

## 오너 지시 (2026-08-21)

1. 서비스 이름이 **50자** 되는 경우가 있다
2. **CloudProvider 와 계정은 축약된 정보에서도 반드시 보여야 한다** — CloudProvider 는 분명히 강조
3. `PII Agent 설치` 가 이렇게 강조될 필요가 있나 — **태그 정도나 더 낮은 계층으로**
4. (2026-08-22) CSP 아이콘은 `/pass/admin/pipelines` 의 아이콘을 쓸 것

## 문제 진단 (근거 등급)

| # | 문제 | 근거 | 등급 |
|---|------|------|------|
| P1 | 요약 바가 예산 100%를 **좌 서비스 레일이 40px 옆에서 이미 말한 두 값**(서비스명·코드칩)에 쓴다 | 레일 선택 행이 `A / AWS / aws` 를 파란 바로 강조 중 | UX 원칙(중복) |
| P2 | 프로바이더가 줄에 없다. 뺀 근거는 커밋 `58c84e69` 의 *"클라우드 정보 그룹과 중복 방지"* 인데, **#747 이 그 그룹을 접힘 뒤로 보낸 순간 전제가 죽었다** | 커밋 메시지 + #747 diff | 자체 판례(만료) |
| P3 | mock 1008 은 `serviceName === "AWS"` — 줄이 프로바이더를 말하는 것처럼 **틀린 신호**를 준다 | `lib/mock-data.ts` | 수치 |
| P4 | `service_name` 에 **maxLength 가 없다**(swagger 4곳 전부). 목 주석의 "계약상 상한 30자"는 코드의 낙관 | `docs/swagger/install-v1.yaml` | 계약 |
| P5 | `providerName` 에 truncate 가 없고 부모가 `flex-wrap` → 넘치면 자르는 게 아니라 **접는다**. 본문 열 744px 에 한글 43자(419px)를 넣으면 바가 **40 → 69px**. 30자는 40px 유지 | 브라우저 실측(재현함) | 수치 |
| P6 | 헤더는 본문 열 스크롤러 안(client 746 / scroll 1293)이라 스크롤하면 사라진다 — 참조표가 아니라 **도착 문장**이다 | 실측 | UX 원칙 |
| P7 | (무관·미수정) 우 가이드 레일 Agent 알약이 `--ib-accent` 10% 면 위에 같은 accent 를 **글자색**으로 써서 AA 미달 — Azure 2.6:1 · AWS 2.2:1 | 브라우저 실측 | 수치 |

### 폭 예산 (실측, 재사용할 숫자)

사실 예산 = 요약 바 − 패딩 24 − 간격 12 − 큐 84.
**1710px → 884 · 1360px → 542 · 1280px → 782.** **1360(우 레일 하한)의 542px 이 상한.**

- 한글 이름 27자 281px · 43자 419px · 평균 ≈10.6px/자 (Pretendard 14/500)
- Azure Subscription UUID **mono14 293px** ← 계정 슬롯 최악. AWS 계정 98px. IDC·SDU 는 0건.
  → 계정 슬롯 폭이 **0~293px 로 흔들리는 것**이 진짜 제약이었다.

## 레퍼런스 — 실제로 차용한 것

| # | 레퍼런스 | URL | 차용한 요소 |
|---|---|---|---|
| R1 | **이 저장소의 운영측 헤더** `OpsHeader.tsx` (오너 08-20 지시로 이미 같은 문제를 푼 형제 화면) | [OpsHeader.tsx](https://github.com/bluefa/pii-agent-demo/blob/main/app/admin/pipelines/ops/target-sources/%5BtargetSourceId%5D/_components/OpsHeader.tsx) | 경로 `직무 / 서비스명 / #id` · 이름 `max-w-[280px] truncate` + 네이티브 `title` · 12px weak 크럼 · `#id` = "you are here" |
| R2 | AWS Cloudscape — Details page | https://cloudscape.design/patterns/resource-management/details/details-page/ | 제목이 경로의 마지막 마디라는 규칙 / 요약 컨테이너 |
| R12 | Datadog — primary tags (scope) | https://docs.datadoghq.com/tracing/guide/setting_primary_tags_to_scope/ | **스코프는 상주한다** — env·service 를 화면에 고정으로 남기는 문법 |
| R? | 이 저장소의 운영 대시보드 identity cell (`_dashboard/cells.tsx`) | — | `ProviderGlyph tone="brand"` 20px 마크 (오너 4차 지시 #4) |

전체 13개 카탈로그와 나머지 시안(A·B·D·E)의 차용 관계는 아티팩트에 있다.

## 왜 C 인가 (비교표 근거)

| | 접근 | 1360 이름 폭 | 헤더 | 닫는 문제 |
|---|---|---|---|---|
| A | 스코프 우선 한 줄 | 207px ≈19자 | 189 | P2·P5 |
| B | 정체성 2단 | 397px ≈32자 | 200 | P2·P4·P5 |
| **C** | **경로 정체성** | **280px 고정** | **170(추정)** | **P2·P3·P4·P5** |
| D | 브랜드 칩 | 248px ≈23자 | 158 | P2·P5, `sr-only` h1 비용 |
| E | 우선순위 강등 | 동적 | 189 | 새 메커니즘 값 > 이득 → **비권장** |

C 를 고른 이유는 취향이 아니라 표다. **P2~P5 를 한 번에 닫는 유일한 안**이고,
같은 대상을 그리는 **운영측 화면과 같은 문법**이 되며, `PII Agent 설치` 가 지워지지 않고
경로의 첫 마디로 내려간다 — 오너가 말한 "태그 정도"의 무게가 정확히 이것.

**감수한 비용**: 서비스명이 화면에 두 번 나온다(좌 레일 + 경로). C 는 P1(중복)을 못 없앤다.

## 구현 (PR #747, 커밋 2)

**경로 = h1.** 링크가 하나도 없으므로 브레드크럼(`<nav>`)이 아니라 **경로 모양의 제목**이다.
접근성 이름은 `PII Agent 설치 / {서비스명} / #{id}` 로 직무와 대상을 함께 말한다.

**바 = 스코프 전용.** `[브랜드 마크] {프로바이더}(+gloss) │ {계정 라벨} {계정 값} [서비스 코드]`

| 결정 | 값 | 출처 |
|---|---|---|
| 크럼 | `12px / #4E5968 / gap-1.5`, 이름 `max-w-[280px] truncate`+`title`, `#id` semibold `#191F28` | `opsStyles.crumb`/`crumbHere` |
| 프로바이더 마크 | `ProviderGlyph tone="brand"` **20px, 판 없음** | 운영 대시보드 `identityGlyphMark` (오너 지시 #4) |
| 계정 슬롯 | `min-w-0 truncate` + 네이티브 `title` | 벤치마크 공통 규칙 |
| 바 | `flex-nowrap` — 나머지 전부 `flex-none` | P5 재발 방지 |

### 시안과 다르게 간 세 가지

1. **`설치 모드` 칩을 바에 안 넣었다.** 목업엔 있었지만 오너 요구는 프로바이더·계정 둘이고,
   칩을 넣으면 1360에서 계정 슬롯이 132px(UUID 6자)로 잘린다. 칩은 접힘 안에 그대로 있다.
2. **계정 슬롯에 `max-w-[140px]` 상한을 두지 않았다.** 고정 상한은 1710px 화면에서도 UUID 를
   자른다. `min-w-0 truncate` 만 두면 **여유가 있으면 전부 찍고 없을 때만 줄인다** —
   실측 1710px에서 36자 전부, 744px 열에서 216px, 620px 열에서 92px, **어디서도 바는 40px**.
3. **접힘 블록에서 프로바이더 행을 뺐다.** 바가 프로바이더를 말하는데 블록이 같은 글리프와
   이름을 60px 아래에서 다시 말해서, 열자마자 중복이 보였다(구현 중 브라우저에서 발견).
   블록은 이제 **바가 못 하는 것만** 진다 — 복사 버튼(누름 안에는 버튼을 못 넣는다),
   두 번째 식별자(Azure Tenant ID), 설치 모드. 사실이 하나도 없으면(IDC) **그룹째 사라진다**.
   계정 값이 바와 블록에 두 번 나오는 것은 남겼다 — 블록 쪽이 **복사 가능한 기록**이라서.

### 실측 결과 (1710px 뷰포트, mock 1008)

| | 이전(#747 커밋 1) | 이후 |
|---|---|---|
| 헤더 높이 | 195px | **184px** |
| 요약 바 | 40px (43자에서 69px) | **40px — 43자·744px 열·620px 열 전부 40px** |
| 접힘 상태 프로바이더·계정 | 없음 | **있음** |

아티팩트가 추정한 ≈170px 은 도달 못 했다. 실제 감소분은 h1(24px×1.2=29px) → 크럼(18px) 의
**11px** 이 전부다.

## 2차 라운드 — 「요약본처럼 보이게」 (2026-08-22, 커밋 3)

시안 C 를 보고 나온 오너 지시 네 건. 벤치마크를 다시 돌리지 않고 같은 문서에 이어 적는다.

| # | 지시 | 한 일 |
|---|------|------|
| 5-a | 카드 디자인이 약하다. **요약본이라는 느낌**이 들게 | 면·계층·구조 세 가지를 바꿈 (아래) |
| 5-b | 요약 카드엔 **Stroke, Shadow 를 한 단계만** 더 높여 | `#E1E4EB`→`#D6DEEC`, none→`tossShadow.sm` |
| 5-c | **#1008 삭제**하고 그 자리에 **서비스 코드**를 올려 | 경로 마지막 마디 = `project.serviceCode` |
| 6 | **Azure 는 sub·tenant 를 2줄로.** 그러면 클라우드 정보행도 날릴 수 있을 듯 | 식별자 전부 카드로, 접힘의 그룹 삭제 |
| 7-a | 라벨의 **보라색을 빼라.** 색은 알아서, **명도비 4.5:1 이상**만 지켜라 | 중립 2단계 — 7.11:1 / 4.62:1 |
| 7-b | **자동설치 유무는 중요**하다. 요약본에도 보여라 | 설치 모드가 카드 그리드의 마지막 행 |

### 진단 — 「약하다」가 가리킨 것

1차 카드는 **워시 위에 선만 그은 빈 사각형**이었다(`border-[#E1E4EB]`, 배경 없음, hover 에만 흰색).
같은 색 위에 같은 색을 두르면 물체가 아니라 필터 툴바로 읽힌다. 나머지 셋:

- 제일 큰 대비가 **내용이 아니라 컨트롤**이었다 — 오른쪽 끝 파란 `설치 대상 정보`
- **서비스 코드 칩만 면을 가져서** 면 없는 상자 안에서 칩이 더 단단해 보였다 (포함 관계 역전)
- 사실들이 전부 `gap-2` — 라벨↔값 거리와 사실↔사실 거리가 같아 네 토큰이 네 개의 동급으로 읽혔다

### 바꾼 것

| | 값 | 근거 |
|---|---|---|
| 면 | `bg-white` | 이 저장소가 이미 쓰는 조합(흰 면+선). 헤더에서 **유일한** 판 |
| 선 | `#D6DEEC` | 파랑 기운 중립 램프의 다음 칸. 저장소에 이미 있던 값 |
| 그림자 | `tossShadow.sm` | 이름 붙은 사다리의 첫 칸. hover 는 `md` 한 칸 더 |
| 경로 | **카드 안으로** | 8px 위에 맨몸으로 떠 있던 걸 들여놓은 것뿐 — 이게 두 계층을 **+5px** 에 산 방법 |
| 사실 간격 | 12px / 라벨↔값 6px | 아래 블록과 같은 1:2 |
| 코드 칩 | 삭제 | 코드가 경로로 올라가서 슬롯 자체가 빔 (5-c) |

**hover 는 그림자만 올린다.** 면을 틴트하면 카드 위 모든 계층이 새 바닥에 서게 되어
design-guard 의 고정 쌍을 전부 다시 재야 한다 — 한 칸 올리자는 지시에 그 비용은 과하다.

### Azure 2줄 → 클라우드 정보행 삭제 (지시 6)

식별자를 `grid-cols-[auto_1fr]` 로 깐다. **wrap 하는 flex 행이 아니라 그리드**인 이유:
`auto` 가 라벨 열을 제일 긴 라벨에 맞춰서 `Subscription ID` 와 `Tenant ID` 의 값이 같은 x 에서
시작한다. 두 줄이 **목록으로 읽히는지, 자리 모자라 접힌 줄로 읽히는지**가 여기서 갈린다.
행은 구조에서 나오므로 **식별자가 둘이라 두 줄**이지, 화면이 좁아서가 아니다.

그래서 접힘의 `클라우드 정보` 그룹이 사라졌다 — 값도 복사 버튼도 전부 카드에 있으니
같은 세 값을 60px 아래에서 다시 찍고 있을 뿐이었다. 접힘에 남은 것: **설명 · 설치 모드**
(SDU 는 연동 방식). 둘 다 설명 블록과 같은 문법의 독립 블록.

### 누름의 범위가 바뀌었다

**복사 버튼이 카드로 올라온 대가.** `<button>` 안에 `<button>` 은 못 넣으므로,
행 전체가 누름일 수는 없다. 디스클로저는 **제목 행 오른쪽의 `설치 대상 정보` 컨트롤**이 됐다.
행을 계속 누름으로 두려면 식별자에서 복사를 뺐어야 했고, 그건 카드가 유일한 출처가 된
지금은 기능 손실이다. 카드 전체 hover 는 그대로 살아 있다(그림자 상승).

### 라벨에서 보라를 뺐다 (지시 7-a)

두 라벨 층이 **색상 계열(hue)로** 갈리고 있었다 — `blockLabel` 라벤더 `#4C3F7A`,
`kvLabel` `#6B5E93`. 계층은 등급이지 계열이 아니므로, 카드의 흰 바닥에서
저장소가 이미 쓰는 중립 램프로 같은 말을 한다.

| 토큰 | 이전 | 지금 | 흰 바닥 명도비 |
|---|---|---|---|
| `blockLabel` (블록 눈썹) | `#4C3F7A` | **`#4E5968`** | **7.11:1** |
| `kvLabel` (필드 이름) | `#6B5E93` | **`#6B7684`** | **4.62:1** |

둘 다 4.5:1 통과, 두 층 간격 **1.54배**(라벤더 쌍은 1.59배였다). 브라우저 실측으로 확인했고
design-guard 가 두 쌍 모두 흰 바닥 기준으로 고정한다. hue 가 신호에서 빠졌으므로
`blockLabel` 의 `tracking-[0.02em]` 이 남은 두 번째 신호다 — 지우지 말 것.

### 설치 모드가 카드로 (지시 7-b)

자동/수동은 참고 자료가 아니라 **이 화면에서 할 일이 있는지를 가르는 값**이다.
접힘 뒤가 아니라 식별자와 같은 그리드의 **마지막 행**. 라벨 열을 공유하므로
`Account ID` 와 `설치 모드` 가 같은 x 에서 시작한다.

접힘에 남은 것: **설명**, 그리고 SDU 의 **연동 방식** 한 줄.

### 실측 (1710px 뷰포트)

| | 시안 C | 2차 |
|---|---|---|
| 헤더 (AWS 1008 — 식별자 1 + 설치 모드) | 184px | **216px** |
| 헤더 (Azure 1003 — 식별자 2, 모드 없음) | 184px | **218px** |
| 헤더 (IDC 1026 — 식별자 0) | 184px | **187px** |
| 카드 자체 | 40px 바 | 100 / 102 / 71px |
| 931·782·620·520px 열 | — | **넘침 없음, 값만 말줄임** |

접힘 상태 헤더가 184 → 216 으로 **32px 늘었다**. 접힘의 목적(333→195)을 일부 되돌리는
값이고, 대가로 **접지 않아도 클라우드 사실 전부가 보인다** — 지시 4·6·7 이 요구한 것이 이것.

**되돌린 버그 하나**: `summaryFact` 에 `flex-none` 을 박았더니 `flex-shrink:0` 이 고정돼
옆에 붙인 `min-w-0` 이 무력화됐고, 520px 열에서 값이 잘리는 대신 **행이 카드를 넘쳤다**.
`flex-none` 은 이제 호출부에서 프로바이더 셀에만 붙인다. 테스트가 이 줄을 지킨다.

## 3차 라운드 — 로고가 이름이다 (2026-08-22, 커밋 4)

| 지시 | 내용 |
|---|---|
| 8-a | 「AWS Cloud」 표시 제거 |
| 8-b | 아이콘을 더 키워서 표시 |

### 진단

AWS 브랜드 마크는 심볼이 아니라 **워드마크**다 — 그림에 이미 "aws" 라고 쓰여 있다.
그 옆에 「AWS Cloud」를 찍는 건 이름을 두 번 쓴 것. Azure·GCP 의 마크는 워드마크는
아니지만 그 클라우드로 읽히는 브랜드 심볼이다.

반대로 **IDC(서버랙)·SDU(업로드 화살표)의 글리프는 우리가 그린 도형**이고 아무것도
지칭하지 않는다. 여기서 이름을 빼면 중복 제거가 아니라 프로바이더 삭제다.

### 규칙

> 마크가 벤더 로고면 이름을 화면에서 뺀다. 우리 글리프면 잉크로 남긴다.

`PROVIDER_DISPLAY` 에 `brandMark?: true` 한 칸. `CloudProviderIcon` 의 `BRAND_BY_KEY`
와 같은 집합(aws·azure·gcp)을 가리킨다.

**지우지 않고 감춘다** — 로고는 낭독되지 않으므로, 텍스트를 문서에서 아예 없애면 화면을
보지 않는 사용자에게 브랜드 클라우드 3종의 이름이 통째로 사라진다. `sr-only` 로 화면에서만
빼고 접근성 이름은 남긴다. 래퍼는 계속 `aria-hidden` (`ProviderGlyph` 에 aria prop 없음).

### 크기 — 20 → 28px

발명한 값이 아니라 `CloudProviderIcon` 자신의 `lg` 칸(`w-7 h-7`). 20px 은 ops 대시보드
식별 셀의 `md` 칸이었다.

`getBBox()` 실측 — AWS 워드마크의 잉크는 슬롯의 **0.598배**(Azure·GCP 는 0.80), 글자만
보면 **0.32배**다:

| 슬롯 | AWS 잉크 | 글자 |
|---|---|---|
| 20px (이전) | 12.0px | ~6.3px |
| 24px | 14.4px | ~7.6px |
| **28px (채택)** | **16.7px** | **~8.9px** |
| 32px | 19.1px | ~10.1px |

20px 에서 글자가 6px — 읽는 크기가 아니라 짐작하는 크기다. 마크가 프로바이더의 **유일한
이름**이 된 이상 이 상태로는 못 넘긴다. 24px 은 옆의 `Account ID` 라벨보다 작아 보여
주어로 안 읽혔고, 32px 은 2행 블록보다 커졌다. 28px 에서 멈춘다.

### 실측 (1710px 뷰포트, 접힘 상태)

| 대상 | 카드 20px | 카드 28px | 헤더 |
|---|---|---|---|
| AWS 1008 (식별자 1 + 모드) | 100px | **99.8px** | 216px 그대로 |
| Azure 1003 (식별자 2) | 102px | 그대로 | 218px 그대로 |
| GCP 1002 (식별자 1) | 73.8px | **77.8px** | 190 → **194** |
| IDC 1026 (식별자 0) | 70.8px | **77.8px** | 187 → **194** |

행이 2개 이상이면 글리프가 카드 높이를 못 건드린다. 1행 이하에서만 +4~7px 이고, 그 덕에
짧은 카드들이 77.8px 로 수렴한다. 구분선은 `self-stretch` 라 글리프 높이(28px)를 따라간다.

### 뮤테이션 검증

| 뮤테이션 | 깨지는 테스트 |
|---|---|
| 항상 `providerName` (감춤 제거) | `lets a branded logo be the provider name…` 1건 |
| 항상 `sr-only` (전부 감춤) | IDC·SDU 잉크 2건 + 한 줄 정렬 1건 |

### 후속 (지시 9) — 마크를 세로 가운데로

커진 마크가 `items-start` 때문에 **사실 블록 중심보다 11px 위**에 있었다(28px 을 50px 스택
꼭대기에 붙인 값). 첫 줄에 붙은 불릿처럼 읽혔는데, 마크는 **블록 전체의 주어**다.
`summaryRow` 를 `items-center` 로. 구분선은 `self-stretch` 로 빠져 있고 스택이 최장 항목이라
**마크만 움직인다** — 카드 높이 99.8px 그대로, 오프셋 −11 → **0**.

## 미착수 / 기각

- **E(우선순위 강등)** 기각 — 데스크톱 전용에서 `ResizeObserver` 가 버는 값이 새 메커니즘 값을 못 넘는다.
- **P1(좌 레일 ↔ 경로의 서비스명 중복)** 미해결 — C 가 감수한 비용. 지우려면 레일 쪽을 건드려야 한다.
- **접힘이 설명 하나뿐인 대상이 있다** — `installMode` 가 없으면(예: Azure 1003) 「설치 대상 정보」
  뒤에 설명만 남는다. 라벨을 바꿀지 접힘을 없앨지는 별도 판단.
- **P7(가이드 레일 Agent 알약 AA 미달)** 미수정 — 이 PR 범위 밖, 별도 지시 필요.
- **`ProjectPageMeta` 의 SDU 분기는 여전히 도달 불가** — `ProjectDetail` 이 `SduUnsupportedNotice` 로
  먼저 가른다. 테스트만 그 경로를 지킨다.

---

## 4차 라운드 — 벽 없는 그룹 (2026-08-22, 개선안 ㄷ)

대상: `/pass/target-sources/{id}` 헤더 전체. 오너 지시 10·11·12.
아티팩트: [흔들리는 축](https://claude.ai/code/artifact/912cc12a-c2c4-41d9-9e46-271279a040c1) ·
[벽 없는 그룹](https://claude.ai/code/artifact/a0b1ce7f-d44b-4964-b4f3-08416dd4eeb1) · PR #747.

### 발주 경위

지시 10 *"카드 밑 「설치 진행」 글자가 카드 안 글자보다 왼쪽이라 불안하다 + 카드가 Summary 느낌이 약하다"*
→ 1차 벤치마크에서 개선안 A~E 도출, 오너가 **A·D·E** 를 지시.
착수 직전 지시 11 *"Summary를 카드로 선언할 필요가 있어? Layout으로 바닥에 뿌리면 안 되나?"* 로 재발주.
**지시 11 이 A·E 를 대체한다** — 둘 다 카드가 남는 전제였다. **D(단일 축)만 그대로 살아남았다.**

### 진단 (실측, Chrome innerWidth 1710, AWS #1008)

| # | 발견 | 근거 등급 |
|---|---|---|
| F1 | 카드 851px / 헤더 열 931px = **91%**. 담는 뷰만큼 커진 상자는 분리를 못 말한다(Apple HIG) | 수치 위반 |
| F2 | 한 화면에 판 방언 **4종** — r10+선+그림자 / r20+그림자 / r12+선 / r12+면+선. 뒤 둘은 카드 안의 카드 | 수치 위반 |
| F3 | 요약 카드와 스텝 카드가 **같은 x(336)·같은 폭(851)·같은 그림자**. 반경만 다르다 = 형제로 읽힌다 | UX 원칙 |
| F4 | NN/g 정의(트럼프 카드 크기 + **링크됨**)로 이 판은 카드가 아니다 | UX 원칙 |
| F5 | 사실 잉크 281px / 안쪽 폭 817px → **536px(66%) 빈칸**. 판이 아니라 내용량 문제 | 수치 위반 |
| F6 | 판을 지우면 텍스트 10종 중 **`kvLabel` 하나만** AA 미달 | 수치 위반 |
| F7 | 카드 안 `<button>` 2개는 사실의 어포던스이지 카드의 행위가 아니다 | 제안 |
| F8 | 축 3개: 상자 336 / 카드 글 353 / 스텝 카드 글 364 | 수치 위반 |

### ★ 카드는 대비를 떠받치고 있었다 (F6)

| 토큰 | 색 | 흰 면 | 워시 `#F4F4FB` |
|---|---|---|---|
| `crumb` · `blockLabel` · `modeNote` · `descText` | `#4E5968` | 7.11 | 6.50 |
| `crumbHere` · `summaryValue` | `#191F28` | 16.56 | 15.13 |
| `metaCue` | `#0050D6` | 6.73 | 6.15 |
| **`kvLabel`** | `#6B7684` | 4.62 | **4.22 미달** |

교체 후보 실측: `#68717F` 4.51 · `#667085` 4.54 · `#656E7C` 4.71.
**`#68717F` 채택** — AA 를 넘기는 최소 어두워짐이고 `blockLabel` 과의 계층 간격을 가장 많이 남긴다
(6.50:4.51 = 1.44배). ⛔ 더 밝게 갈 곳이 없다.

### 채택: 개선안 ㄷ — 이름표 + 헤어라인

5차 지시(「요약본처럼 보이게」)가 기각한 것은 **이름 없는 선**이었지 **이름 있는 무테**가 아니었다.
필요한 건 벽이 아니라 **이름**이었고, 헤더엔 이미 같은 문법의 이름(`blockLabel` 「설치 진행」)이 있었다.
→ [[feedback_rejection_expires_with_its_premise]]

- `targetGroup` 에서 `bg-white`·`border-[#D6DEEC]`·`rounded-[10px]`·`tossShadow` 전부 제거.
  남은 것은 `mt-[18px] max-w-[860px]`.
- 「설치 대상」 을 `blockLabel` 로 세우고 `blockHead`(이름 + 큐 + 하단 1px `#E1E4EB`) 신설.
  **`InstallationProcessProgressBar` 의 「설치 진행」 도 같은 `blockHead` 를 쓴다** —
  한쪽에만 선이 있으면 두 블록이 다른 종류로 읽힌다.
- `<section aria-labelledby>` — **카드에는 없던 접근성 이름**. 테스트가
  `getByRole('region', { name: '설치 대상' })` 로 조회한다(예전엔 `[class*="bg-white"]`).
- 큐 이름 「설치 대상 정보」 → **「설명」**, 그리고 설명이 비면 **큐 자체가 사라진다**.
  접힘 안의 「설명」 라벨은 삭제 — 큐가 이미 그 이름이다.
- **단일 축**: `inner` 를 `px-10` → **`px-[68px]`**(40 열 + 28 스텝 카드 키라인).
  블록들의 자체 가로 패딩은 전부 제거. 실측 결과 경로·설치 대상·설치 진행·스텝 카드 글이 **전부 x=364**.
- 두 블록 모두 `max-w-[860px]`(스테퍼 도로의 기존 캡) → 헤어라인 두 줄이 **x=1224 에서 같이 끝난다**.

### 경로 문법 — 마디마다 종류를 말한다 (지시 12)

`PII Agent 설치 / [서비스] {이름} / [서비스 코드 {코드}]`

- 경로는 값만 말하고 **그 값이 무슨 종류인지 말할 문법이 없다**. `/ DLV` 는 처음 보는 사람에게 아무것도
  아니었다. 태그가 그걸 한다.
- `codeChip`/`codeChipLabel`/`codeChipValue` 는 **`dddad0da` 에서 지웠던 토큰을 복원**한 것 —
  라벨+값 한 칩이라는 모양 그대로다. `crumbKind` 는 같은 껍데기의 라벨 전용 변형.
- ⛔ **복원하지 않은 값 하나**: `codeChip` 의 원래 fill `#E9EEF9` 는 현행 `modeChipAuto` 의 `#EAEEF7` 과
  **ΔE 1.08**(둘 다 L* 94.0) — 눈으로 구분 불가능한 색을 토큰 두 개로 들일 이유가 없다.
  껍데기는 `#EAEEF7` 하나, 두 칩은 **값의 mono** 로 갈린다.
- `crumbHere` 삭제(코드가 칩이 되면서 고아).

### 레퍼런스 (13건 전부 이 세션에서 확인)

| # | 출처 | 빌린 것 |
|---|---|---|
| 01 | [Cloudscape Container](https://cloudscape.design/components/container/) | *"Don't use containers simply for page hierarchy or for general page-layout purposes."* — 그리고 반대편 인용도 같은 페이지에 |
| 02 | [Apple HIG Boxes](https://developer.apple.com/design/human-interface-guidelines/boxes) | 상자/담는 뷰 **비율**로 판정 (F1) |
| 03 | [GitLab Pajamas Border](https://design.gitlab.com/product-foundations/border/) | *"boxes within boxes"* 금지 + **반복·보편 요소는 무테** (F2) |
| 04 | [Atlassian Elevation](https://atlassian.design/foundations/elevation) | 여백 < 테두리 < 그림자 비용 사다리 |
| 05 | [NN/g Cards](https://www.nngroup.com/articles/cards-component/) | 카드 자격 = 크기 + **링크** (F4) |
| 06 | [NN/g Common Region](https://www.nngroup.com/articles/common-region/) | 경계는 근접을 **이긴다** — 그래서 6/18px 리듬이 안 읽혔다 |
| 07 | [NN/g Proximity](https://www.nngroup.com/articles/gestalt-proximity/) | 이미 보유한 1:3 거리비를 그룹 경계로 |
| 08 | [Cloudscape Layout](https://cloudscape.design/foundation/visual-foundation/layout/) | *"…or directly laying the content on the background of the page."* — 오너 제안이 정식 선택지 |
| 09 | [Material 3 Layout](https://m3.material.io/foundations/layout/understanding-layout/parts-of-layout) | 그리드 자체가 그룹화 장치 |
| 10 | [USWDS Card](https://designsystem.digital.gov/components/card/) | *"Standalone content. Consider an `aside`…"* |
| 11 | [Cloudscape Details page](https://cloudscape.design/patterns/resource-management/details/details-page/) | **반증** — 요약 컨테이너 필수 + 이름 필수. 다만 거기서도 **경로는 컨테이너 밖** |
| 12 | [Cloudscape Key-value pairs](https://cloudscape.design/components/key-value-pairs/) | **반증** — 속성 목록은 레이아웃이지만 예제는 늘 컨테이너 안. 1~4열 규칙은 F5 용 |
| 13 | [Primer Card](https://primer.style/product/components/card/) | **반증** — 카드가 링크일 필요 없음. "카드는 행위 단위" 는 우리 저장소의 선택이지 업계 합의가 아니다 |

### 실측 (innerWidth 1710, AWS #1008, 접힘)

| | 헤더 높이 | 첫 카드 y |
|---|---|---|
| 이전 (카드) | 216px | 312 |
| 이후 (개선안 ㄷ) | **252px** | **348** |

**+36px**. 내역: 경로 칩 라인박스 +6 · 「설치 대상」 머리(라벨 18 + pb 6 + 선 1) +25 ·
「설치 진행」 머리 pb+선 +7 · `targetGroup` mt 12→18 +6 · 스테퍼 `list` mt 6→10 +4 ·
카드 `pt-3`·`pb-3` 제거 −12. **이름과 태그의 값이다** — 줄이고 싶으면 레버는 헤어라인 한 줄을 빼는 것.

Azure 1003 254px · IDC 1026 230px. 열 931·782·620·520px 전부 넘침 0.

### 뮤테이션 검증

| 되돌린 것 | 실패 |
|---|---|
| `kvLabel` → `#6B7684` | design-guard 1건 (워시 쌍) |
| 경로를 section 안으로 | 5건 (개선안 ㄷ 핀 포함) |
| `aria-labelledby` 제거 | 16건 |

### 남은 것

- **F5(66% 빈칸) 미해소** — 다섯 안 모두 못 푼다. 내용량 문제라 ref 12 의 2열 배치나
  1차 라운드 개선안 C(접힘 몸통 채우기)로만 풀린다. 별도 발주.
- ⛔ **침강면(시안 ㅁ) 재제안 금지** — 세 번째 배경을 들이면 `design-guard.test.ts` 171쌍이
  통째로 다시 열린다. 지금 관리 중인 바닥은 흰 면과 워시 둘뿐.
- 서비스명·코드를 블록 안으로 내리는 안은 지시 12 로 **취소**됨(경로 유지 + 태그).
  내릴 경우 필요했던 가드는 기록해 둔다: `target-source-response.ts:78` 이
  `serviceName ... || serviceCode` 로 폴백하므로 두 행이 같은 값을 찍을 수 있다.
- **`document.title` 은 여전히 루트 레이아웃의 "PII Agent" 고정값** — 이 경로엔
  `generateMetadata` 가 없다. 대상별 탭 제목이 필요하면 `PipelineDetailView.tsx:252` 방식이 선례.

### 후속 (지시 13) — 거터 20, 진행은 한 줄, 경로 머리는 semibold

세 지시가 같은 방향이라 한 커밋으로 묶었다: **헤더를 더 줄인다.**

**1. 페이지 거터 40 → 20.** `CloudTargetSourceLayout`·`IdcTargetSourceLayout` 의
`px-10` → `px-5`, 그리고 `projectHeaderStyles.inner` 는 `px-[68px]` → **`px-[48px]`**(20 + 28).
v16 `.main` 의 40px 에서 벗어나는 값이지만 두 레이아웃에만 적용된다.
⛔ **둘은 반드시 같이 움직인다** — 가터만 바꾸면 헤더 글자가 카드 글자에서 미끄러지고
테스트는 전부 초록이다(지시 10 의 불평이 정확히 그 11px 이었다).
`ProjectPageMeta.test.tsx` 에 소스 문자열 트립와이어를 달았다:
`cardStyles.header` 의 28 + 가터 20 = `inner` 의 48, 그리고 두 레이아웃 파일의 `px-5 pt-8 pb-20`.

실측: 스텝 카드 상자 **316..1362**, 헤더 본문·헤어라인 두 줄 **전부 344..1334**,
경로 x = 스텝 카드 글 x = **344**. 단일 축 유지.

**2. 「설치 진행」 을 한 줄로.** 7점 도로가 사라지고 `전체 **7**단계 중 **4**단계 [Agent 설치]`
한 문장이 됐다. 7과 N 만 **14px**(문장은 12px) — 위치가 이 블록의 유일한 존재 이유이므로
숫자만 크기 계단을 올라간다. `items-baseline` 필수.

- **삭제된 토큰 8개**: `list`·`item`·`track`·`lineBase`·`line`·`lineDone`·`dotBase`·
  `dotPending`·`dotDone`·`dotCurrent`·`labelBase`·`labelRest`·`labelCurrent`.
  design-guard 에서 도로·점·라벨 핀 8개가 빠지고 문장·숫자·태그 핀 3개가 들어왔다(171 → 167).
- **`tagSlot` 이 흐름 안으로 들어왔다** — 도로가 없으니 매달 스텝이 없다. 그래서
  `absolute top-full` 이 만들던 문제(바깥 흐름이라 아무것도 밀지 않아, 본문 `pt-8` 이
  좁아지면 **첫 카드를 조용히 덮었다**)가 통째로 사라졌다. 그 자리를 지키던 소스 문자열 핀도 삭제.
  대신 "다시는 흐름 밖으로 나가지 않는다"를 핀으로 남겼다.
- `<nav aria-label="설치 진행 단계">` → `<section aria-labelledby>` — 목록이 없으면
  내비게이션이 아니다. 설치 대상과 같은 문법이 됐다.
- 알 수 없는 `ProcessStatus` 는 위치 줄을 **통째로 안 그린다**(「0단계」 금지).

**3. 「PII Agent 설치」 semibold.** `crumbRoot: 'flex-none font-semibold'` — 무게만,
색은 그대로. 어둡게까지 하면 뒤따르는 서비스 이름과 경쟁한다.

**높이**: 카드 시절 216px → 개선안 ㄷ 252px → **242px**(첫 카드 y 312 → 338).
한 줄 진행이 12px 을 돌려받았다. 남은 +26px 은 이름 두 개와 태그 값이다.

### 후속 (지시 14) — 이름까지 한 줄로, 단계는 파란 태그, 도로는 접어서 되돌림

지시 13 이 도로를 **삭제**했다면, 지시 14 는 그걸 **강등**으로 되돌린다. 남은 건 순위다:
한창 설치 중인 사람이 원하는 사실 하나는 늘 화면에 있고, 처음 오는 사람이 한 번 보고 싶은
전체 경로는 한 번의 클릭 뒤에 있다.

**1. 「설치 진행」이 위치 줄과 같은 행에 선다.**

```
설치 진행   전체 7단계 중  [4단계 Agent 설치]  [최근 테스트 성공 82일 전]      전체 단계 ⌄
──────────────────────────────────────────────────────────────────────────────────
```

- 새 토큰 `head`(`flex min-w-0 flex-wrap items-baseline gap-x-3 gap-y-1`) 가 이름과 문장을
  묶어 공유 `blockHead` 의 **왼쪽 절반**이 된다. 컨테이너를 따로 둔 이유: `blockHead` 는
  `justify-between` 이라 자식이 셋이면 문장이 행 가운데로 밀린다.
- 이름과 문장 사이 **12px**, 문장 내부 8px. 둘은 색이 같고 무게도 가까워서 거리가 유일한
  분리 장치다 — 8px 이면 「설치 진행 전체 7단계 중」이 한 구로 읽힌다.
- `summary` 에서 `mt-2` 삭제(더 이상 아래 줄이 아니다).

**2. 「4단계 Agent 설치」는 파란 태그.** 오너 지시.

- `#E8F1FF` / **`#0050D6`**, 5.92:1. 새 색이 아니라 `primaryColors.bgLight`/`textOnLight`.
  경로가 쓰는 슬레이트(`#EAEEF7`/`#2C3A55`)를 벗은 이유는 이 행이 이제 블록 **이름까지**
  싣기 때문 — 눈이 내려앉을 지점이 하나 필요하다.
- ⛔ **`cardStyles.stepTag` 와 채움·잉크가 같아야 한다.** 스텝 카드 제목 위의 「N단계」
  태그는 *같은 사실을 두 번째로* 그린 것이고 동시에 화면에 있다. 한 라운드 동안
  `#1747B5` 로 나갔는데, 잉크를 `metaCue` 하고만 견주고 정작 카드 태그와는 안 견준 탓이다
  (교차 리뷰 P3). **같은 판에 틴트가 둘이면 뜻이 둘로 읽힌다.**
  `metaCue` 와 색이 겹치는 건 견딜 만하다 — 큐는 채움이 없고, 셰브런을 달고, 행 반대편에
  있으며, **같은 사실이 아니다.** 카드 충돌은 그 넷이 전부 반대였다.
- 토큰은 리터럴 hex 로 둔다 — `design-guard.test.ts` 의 `classOf` 는 소스 문자열을
  정규식으로 긁으므로 `${primaryColors.bgLight}` 보간을 못 따라간다. 대신 두 토큰의
  **동등성을 테스트로** 박았다.
- 태그 안 숫자는 `tagCount` — 같은 14px 계단이되 **태그의 잉크를 상속**한다.
  `count`(#191F28) 를 그대로 쓰면 두 단어짜리 판 안에 색이 둘이 된다.

**3. 도로는 「전체 단계」 뒤로.** 지시 13 이 지운 토큰 13개를 **원문 그대로** 되살렸다
(`list`·`item`·`track`·`lineBase`·`line`·`lineDone`·`dotBase`·`dot*`·`label*`).
design-guard 도 같이 복구 — 도로·점·라벨 핀 8쌍이 돌아오고, 태그 핀은 파란 판으로 갱신.

- 문법은 한 블록 위 **설치 대상과 동일**: `blockHead` 에 이름 + 큐 하나, 몸통은 그 아래.
  형제 블록과 다르게 접히면 다른 종류의 것으로 읽힌다.
- **기본 접힘**(`useState(false)`). `aria-expanded`/`aria-controls` → `install-progress-steps`.
- `item` 에서 `relative` 제거 — 매달릴 것이 없어졌다.
- 알 수 없는 상태에서도 도로는 열린다(현재 단계만 없음). 경로는 이 대상의 사실이 아니라
  제품의 사실이다.

**4. 검증 태그(`tagSlot`)는 도로와 같이 접힌다** — 지시 14 후속으로 **뒤집힘**.
처음엔 "머리 행에 남는다"로 구현·기록했으나(가장 신선한 사실이라는 근거), 오너가
「전체 단계 펼쳤을 때만 보이게」로 결정. 게이트가 둘이 됐다: `stepsOpen && 단계 5 도달`.

- ⛔ **도로의 연결 테스트 스텝에 다시 매달지 말 것.** 접는 방법으로 제일 먼저 떠오르지만,
  그게 바로 `absolute top-full` 버그(첫 카드를 조용히 덮음)를 되살리는 길이다.
  태그는 **머리 행 흐름 안**에 그대로 두고 렌더 조건만 하나 더 건다.
- 두 게이트 모두 **숨기는 게 아니라 안 그린다** — `TcHeaderTag` 는 마운트 시
  `latest_version` 을 가져오므로, 접혀 있으면 요청도 없다. 펼칠 때 한 번 왕복하는데,
  dev 서버 첫 히트의 2초는 라우트 컴파일이고 웜 상태는 ~120ms.

**실측**(innerW 1710, IDC 1024 = 5단계 + 검증 태그):

| | 접힘 | 펼침 |
|---|---|---|
| 헤더 높이 | **219px** (지시 13: 242) | 261px |
| 첫 카드 y | **315** (지시 13: 338) | 357 |
| 위치 행 | 1줄 | 1줄 |
| 큐까지 여백 | 545px | 545px |

열 폭 926 / 776 / 616 / **516** 전부: 행 1줄, 큐 여백 71px 이상, `overflow` 0.
516px 에서 도로 라벨은 2줄로 접히지만(구 도로와 동일) `li` 높이가 균일하고 넘침 없음.

**뮤테이션 4건 전부 잡힘**: 태그를 슬레이트로 → 1 실패 · 이름을 `head` 밖으로 → 1 실패 ·
기본 펼침 → 12 실패 · 검증 태그 제거 → 5 실패.


### 교차 리뷰 반영 (지시 14 후속, `/code-review`)

머지 직전 교차 리뷰 5건. 2건은 리뷰가 도는 동안 이미 처리됐고(PR 제목·본문 전면 교체),
3건 반영:

- **P3 단계 태그 잉크 충돌** → 위 항목 참조. `#0050D6` 로 통일 + 동등성 테스트.
- **P2 prop 계약이 반대로 남아 있었다** — `tcTag` JSDoc 이 "도로와 같이 접히지 않는다"
  그대로였다. theme 주석과 이 문서는 고쳤는데 인터페이스만 놓쳤다. 이 저장소는 규칙을
  주석에 싣기 때문에, 나중에 누가 그 계약을 복원하려다 테스트 2개를 깨고
  "테스트가 틀렸다"고 판단하게 된다.
- **P2 `aria-controls` 가 펼침이 드러내는 걸 다 못 덮었다** — 그 누름은 도로 **와** 검증
  태그 둘을 드러내는데, 검증 태그는 머리 행(=`<ol>` 밖, DOM 상 앞)에 산다.
  `aria-controls` 는 ID 목록이므로 둘 다 넣었다
  (`install-progress-steps install-progress-verdict`).
- **P2 리베이스 후 트리는 검증된 적이 없었다** — pre-commit 마커가 리베이스 이전 트리를
  가리켰고 이 저장소엔 CI 가 없다. 4개 게이트를 실제 머지 트리에서 다시 돌렸다:
  tsc 0 · eslint 0 errors · **vitest 292 files / 2728 tests** · build OK.
  리베이스가 main 의 새 테스트 2파일 24개를 끌어왔으니 지적이 정확했다.
- **P3 이 PR 이 스스로 stale 문서를 실었다** — `target-source-layout.md`(1차 라운드 기록)가
  195px·"스텝퍼는 건드리지 않았다"·"`tagSlot` 과 얽혀 미착수"를 그대로 담고 있었다.
  1차 기록이니 본문은 두고 머리에 경고 블록을 달았다.
  `step-flow-ux-improvement-report.md` 의 `px-10` 인용도 수정.

**미반영(오너 판단 영역)**: 검증 태그가 5~7단계에서 기본 숨김이 됐다. `TcHeaderTag` 의
JSDoc 대로 Step 5 카드는 5단계에서만 마운트되므로 **6·7단계에서는 이 헤더 태그가 유일한
표면이었다.** 이제 「전체 단계」 뒤에 있고, 그 라벨은 단계를 약속하지 테스트 결과를
약속하지 않는다. 오너 지시라 기록만 남긴다.


### 지시 15 — 경로 태그에 획 (커밋 예정)

「서비스」·「서비스 코드」 태그에 stroke 추가. **`#D7DBE3`, 1px, 두 태그 공용.**

- **"2단계"는 잰 값이다.** 태그 fill `#EAEEF7` 기준으로 이 저장소가 이미 쓰는 획들:

  | 획 | 색 | ΔE00(fill) | ΔE00(wash) |
  |---|---|---|---|
  | `blockHead` 헤어라인 | `#E1E4EB` | 2.31 | 3.83 |
  | `divider` | `#E4E5EE` | 2.32 | 3.33 |
  | 옛 카드 stroke | `#D6DEEC` | **4.29** | 6.45 |
  | `providerGlossBar` | `#C6CCD6` | 7.84 | 9.60 |

  `#D7DBE3` = **4.20 / 5.89** → 두 번째 칸. ⛔ 세 번째 칸(`#C6CCD6`)으로 가지 말 것 —
  이 태그들은 카드가 아니라 **12px 경로 위**에 있다.
- **블랙 획**(오너 지시). 정확히 `rgba(0,0,0,.08)` 을 fill 위에 얹은 값을 불투명 hex 로
  구운 것 — border box 는 어차피 배경 위에 그려지므로 렌더는 동일하고, hex 여야
  `design-guard.test.ts` 가 잰다. 더 진한 슬레이트가 아닌 이유: fill 이 이미 파란 기가
  있어서, 파란 칸을 하나 더 쌓으면 **가장자리가 아니라 "칩이 더 진해진 것"으로 읽힌다.**
- 테스트가 `fill × 0.92` **산술을 핀**한다 — fill 을 상수배한 게 아닌 hex 로 바꾸면
  한 어휘여야 할 줄에 새 색상이 하나 생긴다.
- design-guard 175 → **177 쌍**(획 × 자기 fill, 태그당 하나). 획 대 워시는 안 박는다 —
  더 어두우니 자동으로 더 벌어진다(5.89).
- 헤더 219 → **221px**(위아래 1px씩). 경로는 여전히 한 줄.
- **미적용(범위 밖)**: 같은 `#EAEEF7` 을 쓰는 `modeChipAuto`(자동 설치)는 획이 없다.
  지시가 경로 태그 둘만 지목했고, 그 둘은 워시 위에 맨몸으로 서 있는 반면 모드 칩은
  블록 **안** 격자의 한 칸이라 방어할 수 있는 차이다. 통일하려면 별도 발주.
