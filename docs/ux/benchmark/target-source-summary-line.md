# 설치 화면 「설치 대상」 요약 줄 — 정보 표현 벤치마크 결정 기록

- **일자**: 2026-08-22
- **대상 화면**: `/pass/target-sources/{id}` 서비스측 설치 화면 헤더
  (`ProjectPageMeta` 제목 행 + 「설치 대상」 요약 바)
- **아티팩트**: https://claude.ai/code/artifact/9746b341-2e04-4435-b155-df0767fac2b9
  (진단 P1~P7 · 레퍼런스 13(확인 12 / 기억 1) · 개선안 A~E · 비교표)
- **선행**: `target-source-layout.md`(개선안 D, 같은 PR #747에서 요약 바를 만든 변경)
- **채택**: **시안 C — 경로가 정체성이다 (운영측 문법 이식)**

## 오너 지시 (2026-08-21)

1. 서비스 이름이 **50자** 되는 경우가 있다
2. **CloudProvider 와 계정은 축약된 정보에서도 반드시 보여야 한다** — CloudProvider 는 분명히 강조
3. `PII Agent 설치` 가 이렇게 강조될 필요가 있나 — **태그 정도나 더 낮은 계층으로**
4. (2026-08-22) CSP 아이콘은 `/pass/admin/pipelines` 의 아이콘을 쓸 것

## 문제 진단 (근거 등급)

| # | 문제 | 근거 | 등급 |
|---|------|------|------|
| P1 | 요약 바가 예산 100%를 **좌 서비스 레일이 40px 옆에서 이미 말한 두 값**(서비스명·코드칩)에 쓴다 | 레일 선택 행이 `A / AWS / aws` 를 파란 바로 강조 중 | UX 원칙(중복) |
| P2 | 프로바이더가 줄에 없다. 뺀 근거는 커밋 `58c84e69` 의 *"클라우드 정보 그룹과 중복 방지"* 인데, **#747 이 그 그룹을 접힘 뒤로 보낸 순간 전제가 죽었다** | 커밋 메시지 + #747 diff | 자체 판례(만료) |
| P3 | mock 1008 은 `serviceName === "AWS"` — 줄이 프로바이더를 말하는 것처럼 **틀린 신호**를 준다 | `lib/mock-data.ts` | 수치 |
| P4 | `service_name` 에 **maxLength 가 없다**(swagger 4곳 전부). 목 주석의 "계약상 상한 30자"는 코드의 낙관 | `docs/swagger/install-v1.yaml` | 계약 |
| P5 | `providerName` 에 truncate 가 없고 부모가 `flex-wrap` → 넘치면 자르는 게 아니라 **접는다**. 본문 열 744px 에 한글 43자(419px)를 넣으면 바가 **40 → 69px**. 30자는 40px 유지 | 브라우저 실측(재현함) | 수치 |
| P6 | 헤더는 본문 열 스크롤러 안(client 746 / scroll 1293)이라 스크롤하면 사라진다 — 참조표가 아니라 **도착 문장**이다 | 실측 | UX 원칙 |
| P7 | (무관·미수정) 우 가이드 레일 Agent 알약이 `--ib-accent` 10% 면 위에 같은 accent 를 **글자색**으로 써서 AA 미달 — Azure 2.6:1 · AWS 2.2:1 | 브라우저 실측 | 수치 |

### 폭 예산 (실측, 재사용할 숫자)

사실 예산 = 요약 바 − 패딩 24 − 간격 12 − 큐 84.
**1710px → 884 · 1360px → 542 · 1280px → 782.** **1360(우 레일 하한)의 542px 이 상한.**

- 한글 이름 27자 281px · 43자 419px · 평균 ≈10.6px/자 (Pretendard 14/500)
- Azure Subscription UUID **mono14 293px** ← 계정 슬롯 최악. AWS 계정 98px. IDC·SDU 는 0건.
  → 계정 슬롯 폭이 **0~293px 로 흔들리는 것**이 진짜 제약이었다.

## 레퍼런스 — 실제로 차용한 것

| # | 레퍼런스 | URL | 차용한 요소 |
|---|---|---|---|
| R1 | **이 저장소의 운영측 헤더** `OpsHeader.tsx` (오너 08-20 지시로 이미 같은 문제를 푼 형제 화면) | [OpsHeader.tsx](https://github.com/bluefa/pii-agent-demo/blob/main/app/admin/pipelines/ops/target-sources/%5BtargetSourceId%5D/_components/OpsHeader.tsx) | 경로 `직무 / 서비스명 / #id` · 이름 `max-w-[280px] truncate` + 네이티브 `title` · 12px weak 크럼 · `#id` = "you are here" |
| R2 | AWS Cloudscape — Details page | https://cloudscape.design/patterns/resource-management/details/details-page/ | 제목이 경로의 마지막 마디라는 규칙 / 요약 컨테이너 |
| R12 | Datadog — primary tags (scope) | https://docs.datadoghq.com/tracing/guide/setting_primary_tags_to_scope/ | **스코프는 상주한다** — env·service 를 화면에 고정으로 남기는 문법 |
| R? | 이 저장소의 운영 대시보드 identity cell (`_dashboard/cells.tsx`) | — | `ProviderGlyph tone="brand"` 20px 마크 (오너 4차 지시 #4) |

전체 13개 카탈로그와 나머지 시안(A·B·D·E)의 차용 관계는 아티팩트에 있다.

## 왜 C 인가 (비교표 근거)

| | 접근 | 1360 이름 폭 | 헤더 | 닫는 문제 |
|---|---|---|---|---|
| A | 스코프 우선 한 줄 | 207px ≈19자 | 189 | P2·P5 |
| B | 정체성 2단 | 397px ≈32자 | 200 | P2·P4·P5 |
| **C** | **경로 정체성** | **280px 고정** | **170(추정)** | **P2·P3·P4·P5** |
| D | 브랜드 칩 | 248px ≈23자 | 158 | P2·P5, `sr-only` h1 비용 |
| E | 우선순위 강등 | 동적 | 189 | 새 메커니즘 값 > 이득 → **비권장** |

C 를 고른 이유는 취향이 아니라 표다. **P2~P5 를 한 번에 닫는 유일한 안**이고,
같은 대상을 그리는 **운영측 화면과 같은 문법**이 되며, `PII Agent 설치` 가 지워지지 않고
경로의 첫 마디로 내려간다 — 오너가 말한 "태그 정도"의 무게가 정확히 이것.

**감수한 비용**: 서비스명이 화면에 두 번 나온다(좌 레일 + 경로). C 는 P1(중복)을 못 없앤다.

## 구현 (PR #747, 커밋 2)

**경로 = h1.** 링크가 하나도 없으므로 브레드크럼(`<nav>`)이 아니라 **경로 모양의 제목**이다.
접근성 이름은 `PII Agent 설치 / {서비스명} / #{id}` 로 직무와 대상을 함께 말한다.

**바 = 스코프 전용.** `[브랜드 마크] {프로바이더}(+gloss) │ {계정 라벨} {계정 값} [서비스 코드]`

| 결정 | 값 | 출처 |
|---|---|---|
| 크럼 | `12px / #4E5968 / gap-1.5`, 이름 `max-w-[280px] truncate`+`title`, `#id` semibold `#191F28` | `opsStyles.crumb`/`crumbHere` |
| 프로바이더 마크 | `ProviderGlyph tone="brand"` **20px, 판 없음** | 운영 대시보드 `identityGlyphMark` (오너 지시 #4) |
| 계정 슬롯 | `min-w-0 truncate` + 네이티브 `title` | 벤치마크 공통 규칙 |
| 바 | `flex-nowrap` — 나머지 전부 `flex-none` | P5 재발 방지 |

### 시안과 다르게 간 세 가지

1. **`설치 모드` 칩을 바에 안 넣었다.** 목업엔 있었지만 오너 요구는 프로바이더·계정 둘이고,
   칩을 넣으면 1360에서 계정 슬롯이 132px(UUID 6자)로 잘린다. 칩은 접힘 안에 그대로 있다.
2. **계정 슬롯에 `max-w-[140px]` 상한을 두지 않았다.** 고정 상한은 1710px 화면에서도 UUID 를
   자른다. `min-w-0 truncate` 만 두면 **여유가 있으면 전부 찍고 없을 때만 줄인다** —
   실측 1710px에서 36자 전부, 744px 열에서 216px, 620px 열에서 92px, **어디서도 바는 40px**.
3. **접힘 블록에서 프로바이더 행을 뺐다.** 바가 프로바이더를 말하는데 블록이 같은 글리프와
   이름을 60px 아래에서 다시 말해서, 열자마자 중복이 보였다(구현 중 브라우저에서 발견).
   블록은 이제 **바가 못 하는 것만** 진다 — 복사 버튼(누름 안에는 버튼을 못 넣는다),
   두 번째 식별자(Azure Tenant ID), 설치 모드. 사실이 하나도 없으면(IDC) **그룹째 사라진다**.
   계정 값이 바와 블록에 두 번 나오는 것은 남겼다 — 블록 쪽이 **복사 가능한 기록**이라서.

### 실측 결과 (1710px 뷰포트, mock 1008)

| | 이전(#747 커밋 1) | 이후 |
|---|---|---|
| 헤더 높이 | 195px | **184px** |
| 요약 바 | 40px (43자에서 69px) | **40px — 43자·744px 열·620px 열 전부 40px** |
| 접힘 상태 프로바이더·계정 | 없음 | **있음** |

아티팩트가 추정한 ≈170px 은 도달 못 했다. 실제 감소분은 h1(24px×1.2=29px) → 크럼(18px) 의
**11px** 이 전부다.

## 미착수 / 기각

- **E(우선순위 강등)** 기각 — 데스크톱 전용에서 `ResizeObserver` 가 버는 값이 새 메커니즘 값을 못 넘는다.
- **P1(좌 레일 ↔ 경로의 서비스명 중복)** 미해결 — C 가 감수한 비용. 지우려면 레일 쪽을 건드려야 한다.
- **P7(가이드 레일 Agent 알약 AA 미달)** 미수정 — 이 PR 범위 밖, 별도 지시 필요.
- **`ProjectPageMeta` 의 SDU 분기는 여전히 도달 불가** — `ProjectDetail` 이 `SduUnsupportedNotice` 로
  먼저 가른다. 테스트만 그 경로를 지킨다.
