# 작업 유형 글리프 — INSTALL · DELETE · CUSTOM

- **날짜** 2026-08-14
- **대상** `PipelineTypeTag`(목록 작업 유형 열) · `TypeTile`(상세 실행 카드·이력 행·모달 눈썹)
- **아티팩트** https://claude.ai/code/artifact/cdbcb330-7f3f-4635-9e30-a4de0f70521e
- **PR** #710

## 문제

오너 관찰: "작업 유형 아이콘이 조금 이상하다."

| # | 진단 | 근거 등급 |
|---|------|-----------|
| ① | `install`(받침대로 내려오는 화살표)은 **내려받기** 관용구다. Lucide가 `download`에 붙인 태그가 `import · export · save`. 이 동작은 아무것도 내려받지 않고 고객 클라우드 **쪽으로** 인프라를 세운다 — 화살표 방향이 반대다. | UX 원칙 |
| ② | `trash`(휴지통)는 **되돌릴 수 있는** 삭제의 그림이다. 여기 DELETE는 `terraform destroy`라 되돌리기가 없다. 이 열의 빨강은 이미 뺐으므로(실패 빨강 충돌) 모양이 유일한 채널이고, 그만큼 정확해야 한다. | UX 원칙 |
| ③ | `sliders`가 **이 목록 자신의 필터 트리거와 쌍둥이**다. 둘 다 길이가 줄어드는 가로 막대 더미이고(`FilterIcon` = `M2 3.5h10M4 7h6M5.5 10.5h3`), Lucide는 `sliders-horizontal`에 `filters`를 직접 달아 놓았다. 한 화면에서 두 뜻이 한 모양을 쓴다. | 같은 화면 충돌 |
| ④ | 셋이 서로 다른 은유 족에서 왔다 — `install`=동작, `trash`=사물, `sliders`=조작부. 한 열에 쌓이는 형제는 한 족에서 나와야 그 열이 하나의 축으로 읽힌다. | UX 원칙 |

①②③보다 ④가 "이상하다"의 정체에 가깝다. 글리프 하나하나가 아니라 셋의 관계 문제였다.

## 조사

후보 32종을 Lucide 1.31.0 원본에서 받아 **실제 렌더**로 비교했다(40px 검수용 + 표에서 실제로 쓰이는 14px).
판정 근거는 라이브러리가 스스로 붙인 의미 태그(`tags.json`)와 획 개수다 — 내 해석이 아니다.

- Lucide 1.31.0 아이콘 SVG · `tags.json` — 확인함
- GitHub Primer Octicons 19.15.1 `data.json` — 확인함(배포 어휘 교차 확인: `rocket → launch·ship`)
- Cloudscape Iconography — JS 렌더라 목록 추출 실패. **판단 근거로 쓰지 않았다.**

시안 넷 중 A(꾸러미 대칭)와 C(연산자 최소)만 네 진단을 모두 지웠다. 둘의 차이는 구체성 ↔ 14px 내구성 하나.

## 결정 (오너)

**시안 A의 INSTALL·CUSTOM만 채택.** DELETE는 `trash` 유지.

| 유형 | 이전 | 이후 |
|------|------|------|
| INSTALL | `install` | `package-plus` |
| DELETE | `trash` | `trash` (유지) |
| CUSTOM | `sliders` | `blocks` |

DELETE를 두었으므로 시안 A가 내세웠던 **plus/minus 대칭은 성립하지 않는다.** 대신 얻은 것은
`package-plus`와 `blocks`가 공유하는 **상자 어휘**다 — 셋 중 둘이 한 족이 되어 ④가 부분 해소된다.
①③은 완전 해소, ②는 잔존(오너 판단).

## 실측

14px 내구성이 시안 A의 유일한 위험이었다(`package-plus`는 획 6개). 실제 화면에서 확인:
세 글리프 모두 12px 라벨 옆 14px에서 형태가 살아 있다. 상자의 면 분할과 `+`가 구별되고,
`blocks`의 노치와 떨어진 사각형도 뭉치지 않는다. 시안 C로 내려갈 이유가 없었다.

## 적용 범위

파이프라인 **유형**을 말하는 두 곳만 바꿨다 — `PipelineTypeTag`(목록), `r24Task.TYPE_TONE`(상세 TypeTile).

`install` 글리프는 세트에 남는다. 유형이 아닌 다른 뜻으로 쓰는 소비자가 셋 있다
(`ConfirmedInfoCard` 제목·빈 상태, `RequestTab` 빈 상태 — 전부 "확정 정보"이지 INSTALL이 아니다).
`sliders`는 소비자가 이 둘뿐이었으므로 세트에서 **삭제**했다.

---

# 2차 — 한글 라벨 · 글리프 색 · 20px (2026-08-15)

같은 자리에 대한 오너 후속 지시 세 건. 글리프 모양은 그대로 두고 **낱말·색·크기**를 바꿨다.

## 진단 — 한 enum, 네 어휘

지시는 "설치·삭제·CUSTOM 용어를 전체 app에 적용"이었는데, 코드를 열어보니 고칠 대상은
번역이 아니라 **분열**이었다. 같은 `PipelineType` 하나를 화면마다 다르게 부르고 있었다.

| 화면 | INSTALL | DELETE | CUSTOM |
|------|---------|--------|--------|
| 대시보드 목록 (`PipelineTypeTag`) | `INSTALL` | `DELETE` | `CUSTOM` |
| 대시보드 필터 메뉴 | `INSTALL` | `DELETE` | `CUSTOM` |
| 상세 헤더 (`PipelineDetailView`) | 설치 | 삭제 | 커스텀 |
| 시작 모달 (`PreviewModal`) | 설치 | 삭제 | **Custom** |
| 현재 작업 카드 · 작업 이력 | — | — | **Custom 작업** |

`수치 위반` 은 아니지만 `UX 원칙` 으로 명백하다 — 같은 값을 네 이름으로 부르면 목록에서 본
것과 상세에서 본 것이 같은 것인지 사용자가 매번 다시 판단해야 한다. 상태 라벨은 이미
`statusKo` 한 벌로 정리돼 있었고 (`lib/pipeline/format.ts`), 유형만 정리가 안 돼 있었다.

## 결정

**어휘 한 벌을 `format.ts` 의 `typeKo` 로 승격하고, 라벨을 쓰는 모든 자리가 거기서 읽는다.**
`statusKo` 바로 옆에 두어 다음 사람이 유형만 따로 만들 여지를 없앴다.

| 유형 | 라벨 |
|------|------|
| INSTALL | 설치 |
| DELETE | 삭제 |
| CUSTOM | 커스텀 |

CUSTOM 을 `커스텀` 으로 정한 것은 **이 라벨이 세 자리에서 동시에 버텨야 하기 때문**이다 —
나란히 서고(`설치·삭제·커스텀`), 앞에 provider 가 붙고(`AWS 커스텀`), 뒤에 명사가 붙는다
(`커스텀 작업`). `직접 구성`·`사용자 정의` 는 뜻은 더 정확하지만 뒤 두 자리에서 문장이
깨진다. 코드에 이미 있던 답(`PipelineDetailView` 의 지역 `TYPE_KO`)을 그대로 올린 것이기도 하다.

`TypePill` 은 그대로 wire enum 을 담는다. 상태 라벨이 세운 규칙과 같다 — **사람이 읽는 자리는
한글, enum 원문은 데이터 표기에만.** 시작 모달의 옵션 행은 둘을 나란히 보여준다(`설치` + `INSTALL`).

## 색 — 채널을 나눠서 되살렸다

오너 지시: INSTALL 초록, DELETE 빨강, **아이콘만**. 08-14 에 유형 틴트를 걷어냈던 판단이
하루 만에 뒤집힌 것처럼 보이지만, 뒤집힌 것은 **전제**다.

- 08-14 의 사유: 틴트가 글리프와 라벨을 함께 물들여 유형이 *모양·낱말·색* 세 채널로 말했고,
  DELETE 빨강이 한 행 안에서 실패 빨강과 같은 뜻으로 읽혔다.
- 08-15 의 전제: 색이 **글리프에만** 얹힌다. 유형=도형, 상태=낱말로 채널이 갈리므로
  같은 `#B42318` 이 한 행에 두 번 나와도 20px 도형과 12px 낱말은 서로를 덮지 않는다.
  오히려 "삭제 작업이 실패했다"는 실제로 구분해야 하는 조합이 두 사실로 읽힌다.

`--pl-type-install` 은 `#2563EB`(primary 계열 파랑)에서 `#027A48` 로 옮겼다. 파랑이던 시절엔
같은 행의 **실행 중** 파랑과 충돌했으므로, 초록으로의 이동은 지시 이행이자 충돌 하나의 제거다.
라벨은 `--pl-text-medium` 고정 — 여기에 색을 주면 08-14 의 문제로 그대로 돌아간다.

실측 (흰 행 / hover `#F6F3FF`): install 5.41 / 4.94 · delete 6.57 / 6.01 · custom 6.62 / 6.05.
비텍스트 도형이라 기준선은 WCAG 1.4.11 의 3:1 이지만 전부 텍스트 기준(4.5:1)도 넘는다.

CUSTOM 은 지시에 없었으나 기존 `--pl-type-custom` 보라를 그대로 입혔다. 셋 중 둘만 칠하면
남은 하나가 *비활성*으로 읽힌다 — 새 값을 만든 게 아니라 상세 화면 타일이 이미 쓰던 토큰이다.

## 크기

글리프 14px → **20px** (오너). `Icon` 의 스케일(sm 14 / md 16 / lg 22 / xl 26)에 20 이 없어
raw px 로 넘긴다. 20px 는 12px 라벨과 짝일 때 글리프가 라벨에 종속되지 않고 열의 첫 신호가
되는 크기이며, 색을 받는 대상이기도 하므로 색이 실릴 면적이 필요하다.

## 적용 범위

라벨을 **사람이 읽는 자리** 전부:
`PipelineTypeTag`(목록) · 대시보드 필터 메뉴(`typeOptionLabel`) · `PipelineDetailView`(상세 헤더) ·
`PreviewModal`(옵션 행·버튼·토스트·툴팁) · `CurrentPipelineCard`(제목·빈 상태) · `TargetPipelineSections`(이력 행).

색은 `--pl-type-install` 토큰 값을 바꿨으므로 `PipelineTypeTag` 와 `r24Task.TypeTile` 양쪽에
같이 반영된다 — 목록과 상세가 한 어휘를 유지한다.

가드: `PipelineTypeLabel.test.tsx` (뮤테이션 확인 — 라벨을 enum 으로 되돌리면 3건,
20px→14px 1건, 틴트를 라벨까지 확장하면 3건, CUSTOM 을 `Custom` 으로 되돌리면 1건 실패).

구현 PR: #711
