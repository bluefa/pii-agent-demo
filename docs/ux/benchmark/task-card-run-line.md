# Task 판정 가독성 벤치마킹 → 시안 F(Task Card 실행 요약) 채택 기록

> 작성 2026-08-14 (design-benchmark §6 decision record)
> 대상 화면: `/pass/admin/pipelines/{id}` — 흐름 캔버스의 Task Card + 우측 TaskDrawer
> 선행 기록: `docs/ux/benchmark/pipeline-detail.md` · `pipeline-detail-header.md`

## 아티팩트

| 단계 | 아티팩트 |
|---|---|
| 1차 리서치 (진단 11 · 레퍼런스 13 · 시안 6) | https://claude.ai/code/artifact/16706ea1-5f07-4eb9-9dbd-24843e4f5913 |
| 2차 패널 재정리 (진단 8 · 시안 A~D 지금/적용 후) | https://claude.ai/code/artifact/145418e8-fd72-4cf6-bccc-fb58c3c4929b |

## 발단

"130에서 task를 열었을 때 중요한 정보가 강조되지 않은 느낌이 강하다. Terraform 실행이
성공했는지 실패했는지, 각 Job의 상태가 가장 먼저 눈에 들어와야 한다"(오너).
리서치 중 추가 지시 — "Started/Finished/소요는 Task Card에 표시해도 되지 않을까.
카드에 비해서 배경이 너무 휑하다."

## 문제 요약 (근거 등급 · 실측)

진단 11건 전체는 아티팩트에 있고, **이번 PR이 다루는 것은 P11 하나**다.
P2는 이 변경으로 드로어 자리가 비워져 시안 A에서 풀 수 있게 됐고, P8은 시안 A 몫이다.

1. **P11 Task Card가 크기에 비해 비어 있고, 그 정보는 드로어에 들어가 있다** — `제안`
   카드 실측 **288 × 168px · padding 24 · radius 16**, 내용은 태그 2개 + 이름 + 설명 4줄뿐.
   캔버스는 **984px**에 노드 4개(뷰포트 1710 기준). 그런데 `Started`/`Finished`/`소요`는
   **500px 드로어**의 첫 블록을 차지한다 → 공간이 남는 쪽에 정보가 없고 모자란 쪽에 몰린
   **배치 반전**.
2. **P2 드로어 본문에서 가장 먼저 읽히는 줄이 타임스탬프다** — `UX 원칙`
   `execTabs.tsx` 첫 섹션 "진행 기록"의 첫 행이 `Started`. 여백 7원칙 §7("타이틀 다음엔
   본문을 건너뛰고 액션/결론을 찾는다") 위반. P11을 뒤집으면 이 자리가 비워진다.
3. **P8 "이번 실행의 판정"이라는 객체가 UI에 없다** — `UX 원칙` · **이번 PR 범위 밖**
   태스크 상태(12px 칩)와 Job 판정(10px 칩) 사이의 중간 층이 없다. 판정을 말로 하는 자리는
   **드로어(시안 A)**이지 카드가 아니다 — 카드는 이미 테두리로 상태를 말하고 있다(오너).

## 사용한 레퍼런스

| # | 레퍼런스 | URL | 가져온 것 |
|---|---|---|---|
| 04 | GitHub Actions — Workflow run | https://docs.github.com/en/actions/how-tos/monitor-workflows/view-workflow-run-history | 잡 레일의 **상태 + 이름 + 소요시간을 한 행에** |
| 06 | Spacelift — Run 페이즈 | https://docs.spacelift.io/concepts/run/ | **페이즈별 소요시간**이 상태와 같은 행에 |
| 08 | Datadog CI Visibility | https://docs.datadoghq.com/continuous_integration/pipelines/ | **소요를 비교 가능한 수치로** 노출(어느 단계가 오래 걸렸나) |

## 채택안: 시안 F — Task Card 실행 요약

비교표에서 A·B·F·C 묶음이 11건 중 10건을 덮었고, 그중 **F를 먼저 적용**하기로 결정
(오너: "우선 정보 정리부터 하자"). F는 라우팅·계약 변경이 없고 카드 규격을 그대로 쓴다.

카드 하단에 hairline으로 구분된 **라벨-값 2행**을 붙인다(오너 지정 배치).

```
PLAN  서비스측
AWS Service Level 테라폼          ← 제목에서 Plan/Apply/Destroy 제거
대상 AWS service level 인프라의…
──────────────────────────────
시작 시간  2026-08-14 20:19
완료 시간  2026-08-14 20:21   소요 2분
```

**제목의 실행 종류 접미사를 뗀다.** 바로 위 `JobKindTag`가 이미 `PLAN`/`APPLY`를 말하고
있어 이름에서는 중복이다(오너: "제목에서 Plan, Apply, Destroy는 없애보자"). 드로어 제목은
그대로 둔다 — 드로어에는 `JobKindTag`가 없어 이름이 실행 종류의 유일한 전달자다.

**실행 블록은 카드 전폭을 쓴다.** 처음엔 `.nd-body`(제목 열) 안에 넣었는데, 56px 로고마크
옆이라 폭이 **162px**뿐이라 `완료 시간` 행이 **39px 넘쳐** 잘렸다. 카드를 넓히는 대신
노드를 `[아이콘+제목 행]` / `[실행 블록]` 2단 컬럼으로 바꿨다. 실측: `소요 2시간 5분`까지
한 줄에 들어가고, 그보다 긴 `소요 12시간 45분`은 `flex-wrap`으로 다음 줄에 떨어진다.

**카드 폭 288 → 352px, 이름은 절대 두 줄이 되지 않는다**(오너: "AWS BDC Common 테라폼
이렇게 2줄로 표현되면 안 된다"). 폭은 **가장 긴 카탈로그 이름**에서 역산했다 —
`AWS BDC Service Level 테라폼` 실측 **217px**(16px/700) → 제목 열 226px 확보.
288px에서는 제목 열이 162px라 AWS 계열 이름이 전부 2줄이었다.
캔버스는 **드로어를 연 순간 이미 가로 스크롤**이다(뷰포트 992px 대 트랙 1320px, 288px 기준)
— 넓힌 폭은 새 어포던스를 만들지 않고 한 줄 제목만 산다.
이름이 한 줄로 줄면서 카드 높이는 235 → **213px**.

**소요는 숫자만 한 단계 올린다**(오너: "숫자는 2px 키우고 굵기 한 단계 / 분 12픽셀").
`소요` 라벨도 단위(`분`·`시간`·`초`)도 12px/600 그대로고, **자릿수만 14px/700**이다
(`elapsedDigits`가 `/(\d+)/`로 쪼갠다 — `2시간 5분`이면 `2`와 `5` 둘 다). 카드에서 유일하게
파생된 수치라 양옆 타임스탬프보다 한 급 위이고, 베이스라인은 `.nd-run-row`가 잡는다.

**판정어는 넣지 않는다.** 1차 구현에서 `● 완료` / `● 실패` 를 함께 넣었다가 오너가
"완료/실행중 이런 상태 표시는 하지마, 어차피 stroke를 보고 알 수 있는 정보야"로 잘라냈다.
카드에는 이미 상태 채널이 둘(테두리 색 + 코너 배지)이고, FAILED 카드는 `nd-meta`가
"2회 실패했습니다. 원인은 JOB_FAILED."로 세 번째를 말하고 있었다. 그래서 이 행은
**시간만** 맡는다 — 카드에 없던 정보만 더한다.

### 갈래 규칙 (`taskRunLine`)

| 조건 | 시작 시간 | 완료 시간 | 소요 |
|---|---|---|---|
| `started_at` 없음 | `-` | `-` | 없음 |
| `started_at` 만 있음 | `YYYY-MM-DD HH:mm` | `-` | 없음 |
| 둘 다 있음 | `YYYY-MM-DD HH:mm` | `YYYY-MM-DD HH:mm` | `fmtElapsedMs` |

빈 칸은 `fmtDateTime(null) === '-'` 이 그대로 처리하므로 별도 갈래를 두지 않는다.

- **진행 중 카드에 두 번째 시계를 두지 않는다.** 라이브 경과는 실행 밴드가 이미 담당하고,
  렌더 중 `Date.now()`는 React compiler lint가 막는다(선행 기록 `pipeline-detail.md` 함정).
- **FAILED는 `finished_at`이 비어 올 수 있어**(`runWindow` 주석) 자동으로 "시작만" 갈래로
  떨어진다 — 별도 분기 없음.
- `finished < started`(음수)면 `fmtElapsedMs`가 `-`를 돌려주므로 **소요를 지어내지 않고 비운다.**
- 모든 갈래가 2행을 채우므로 **카드 높이가 상태와 무관하게 균일**하다(실측 235px, 전 상태 동일).

### 재사용한 수치 (출처)

| 값 | 출처 |
|---|---|
| 카드 p24 × r16 | 기존 `DETAIL_CSS .pl-tnode` 그대로 (폭만 288 → 352) |
| 카드 폭 352 | 최장 카탈로그 이름 실측 217px + 아이콘 56 + gap 20 + 패딩 48 을 8배수로 올림 |
| 소요 자릿수 14px/700 | 폰트 세트 12/14 · 굵기 600 → 700 (기존 램프 안), 단위는 12px 유지 |
| 소요 `2분 / 21분 / 2시간 5분` | 기존 `fmtElapsedMs()` — 실행 밴드와 같은 문법 |
| 시각 표기 `YYYY-MM-DD HH:mm` | 기존 `fmtDateTime()` — 화면 전체와 같은 서울 기준 포맷 |
| 12px · gap 4/8 · 컬럼 gap 16 · 패딩 12 | 폰트 세트 12 · 간격 8배수 골격(미세 4) |
| 라벨 `text-weak` / 값·소요 `text-medium` | 기존 토큰 — 값이 스캔 대상이라 라벨보다 진하다 |

새 색·새 폰트 크기·새 컴포넌트는 **0개**.

### 실측 검증 (dev 서버 브라우저)

| 항목 | 값 | 판정 |
|---|---|---|
| 값·소요 vs 카드 | **10.46:1** | 통과 |
| 라벨 vs 카드 | **4.97:1** | 통과(4.5:1) |
| 행 넘침 | 전 상태 **0px** (`소요 2시간 5분`까지) | 통과 |
| 카드 높이 | **213px**, DONE/RUNNING/BLOCKED 전부 동일 | 통과 |
| 카드 폭 | **352px**, 제목 4개 전부 **22px(1줄)** | 통과 |

### 접근성

테두리 색은 스크린리더에 묵음이고 코너 배지는 `aria-hidden`이라, 상태를 시각 채널에만
두면 판정이 아예 전달되지 않는다. 카드에서 판정어를 뺀 대신 **접근 이름에는 남겼다**:
`aria-label={이름} · {판정} · 상세 열기`. (`aria-label`이 자식 텍스트를 대체하므로
자식에 넣는 방법으로는 해결되지 않는다.)

## 2차: 시안 A + B (패널 재정리)

F가 시각을 카드로 올린 뒤 패널을 다시 재고, 진단 8건을 새로 잡았다(2차 아티팩트).
그중 **2건은 F가 만든 문제**다 — 패널 첫 블록 `진행 기록`이 카드의 사본이 됐고,
같은 소요를 카드는 `2분`(`fmtElapsedMs`), 시도 정보는 `2m 0s`(`spanLabel`)로 쓴다.
오너 지시는 "이대로 적용" — 비교표대로 **A + B를 한 커밋**, C는 다음, D는 보류.

### 시안 A — 판정 히어로 (진단 01 · 03)

`진행 기록` 블록과 `시도 횟수` 독립 행을 걷어내고 그 자리에 **판정**을 놓는다.

```
● 실패  [JOB_FAILED]        ← 20px/700 + 점, 코드는 흰 면+stroke 칩
시도 2/2회 · 다음 확인 …     ← 14px text-weak
```

- 틴트 판을 깔지 않는다 — 판정은 타입 크기와 점, 사실은 한 급 아래(기존 규칙).
- `TerraformExec`/`ConditionExec` 둘 다 같은 문법. 조건 확인은 판정이
  `충족`/`미충족`이고 코드 칩 자리에 **외부 상태**(`READY`)가 들어간다.
- 카드가 이미 말하는 시각(Started/Finished)은 **되풀이하지 않는다**. `다음 확인`만
  사실줄로 살아남는다 — 카드에 없는 값이라서다.
- 아티팩트 목업의 `Job 3개 중 1개 실패`는 **넣지 않았다**. B의 집계 줄이 바로 아래에서
  같은 수를 세므로 한 화면에 두 번 적힌다. 사실은 한 번씩만.

### 시안 B — Job 현황을 루트로 (진단 04 · 05 · 08)

최초 요청("각 Job의 상태가 먼저 눈에 들어와야 한다")에 직접 답하는 블록.

- **집계 줄** — `1 실패 · 2 성공 … 총 3개`. 숫자는 16px/700 + 판정 색, `jobVerdict`로 센다.
- **실패 우선 정렬** — 실패 → 진행 중 → 기록 없음 → 성공. 안정 정렬이라 같은 판정끼리는
  계약 순서를 지킨다.
- **`last_fail_reason`을 실패 행 아래 인라인** — 원인까지 **3홉 → 0홉**. 계약이 이미
  주고 있으면서 아무 데서도 안 쓰던 필드다(진단 05).
- **`PlPagination` 제거** — 21개가 5페이지로 흩어지던 걸 접기 하나로 바꿔 패널의
  넘침 문법을 통일했다(진단 08). 6개 이상일 때만 **성공만** 접힌다.
- 같은 블록을 시도 드릴다운도 쓴다 — 루트는 최신 시도, 드릴다운은 그 시도.
  시도가 2개 이상이면 `최신 시도 #2 기준` 힌트로 주어를 못박는다.

### 재사용한 수치 (출처)

| 값 | 출처 |
|---|---|
| 판정 헤드 20px | 폰트 세트 내 기존 값(ops `cardTitle`·`AlertStageCard`). 드로어 제목 18px/700 바로 위 한 칸 |
| 판정 점 8px | 상태 점 기존 규격(`calm-status-color-and-layering`) |
| 코드 칩 | `d.tag` 문법(흰 면 + stroke) 그대로, 12px |
| 집계 숫자 16px/700 | 드로어 `sectionLabel`과 같은 크기 — 라벨과 동급이지 상위가 아니다 |
| 접기 버튼 | 기존 `j.moreBtn`(확인 이력 `전체 N회 보기`) 그대로 |
| 접힘 임계 6개 | 삭제한 `JOBS_PER_PAGE = 5` 를 그대로 승계 |

새 색 **0개**. 새 폰트 크기 0개(13px은 짝수 규칙에 걸려 12/14로 맞췄다).

### 실측 검증 (dev 서버 브라우저, 패널 #F1F4F9)

| 항목 | 값 | 판정 |
|---|---|---|
| 판정 헤드 `실패` | 20px/700 · **5.96:1** | 통과 |
| 집계 `1 실패` / `2 성공` | **5.96:1** / **4.91:1** | 통과 |
| 사실줄 `시도 2/2회` | **4.51:1** | 통과(4.5:1) |
| 실패 사유 인라인 | 14px · **5.96:1** | 통과 |
| 4개 상태 | 완료 / 실패+원인 / 실행 중 / 대기 전부 렌더 | 통과 |

- **Job 목록은 420px에서 자체 스크롤한다.** 드로어는 높이가 묶여 있지 않아(페이지가 같이
  자란다) 21행을 다 펴면 콘텐츠 행이 1519px까지 늘어 **흐름 카드가 화면 밖으로 밀렸다**.
  목록에 상한을 줘 패널을 한 화면 안에 붙잡았다.
- 전부 성공인 시도는 목록이 통째로 접히므로 **빈 목록 래퍼를 그리지 않는다**(마진만 남던 문제).

### 시안 C — 시도 드릴다운을 접기로 (진단 02 · 06)

시도 행을 누르면 **헤더까지 통째로 바뀌던**(← 뒤로) 화면 전환을 없애고 제자리에서 편다.

- `DrawerView`·`j.subHeader`/`back`/`subTitle`/`subCrumb` 삭제 → **Esc 계층이
  뷰어 → 드로어 2단**으로 줄었다. 드로어는 이제 헤더가 하나뿐이다.
- `시도 정보` 3행 kv → **한 줄**: `실행  2026-08-15 07:29 → 07:34 · 5분`.
  끝 시각은 **날짜가 다를 때만** 날짜를 반복한다(자정을 넘긴 시도).
- **`spanLabel` 삭제, `fmtElapsedMs`로 통일** — 카드가 `21분`이라 쓰는 값을 한 클릭
  거리에서 `21m 0s`로 쓰던 문법 분기를 없앴다(진단 02). 문법은 이제 화면 전체에 하나.
- **미니 배지 10px → 12px**(`d.miniBadge`·`j.miniBadge`) — 판정을 나르는 칩이
  타입 세트 최소 아래에 있었다(진단 06).
- **한 번에 하나만 열린다.** 시도 2개 × Job 21개를 다 펴면 패널이 끝없이 길어진다.
- `Response 원문`은 **접힌 채로 시작**한다 — 접기 안의 접기라서.
- **최신 시도를 펼치면 `Job 현황`을 다시 그리지 않는다**(`jobsShownAbove`). 루트가
  이미 그 시도의 Job을 보여주고 있어, 같은 화면에 같은 블록이 두 번 나오는 걸 막는다
  — 진단 01이 지적한 그 중복이다. 이전 시도(#1)를 펼치면 그 시도의 Job이 나온다.

실측: 판정 칩 **12px**, Esc 한 번에 드로어가 닫히고(시도 계층 없음), 뷰어가 열려 있으면
드로어는 닫히지 않는다. 시도 #2 펼침 = `실행 · 확인 요약 · Response`,
#1 펼침 = `실행 · Job 현황 · 확인 요약 · Response`.

## 남긴 것 / 후속

- **시안 D**(드로어 500 → 640px)는 보류. 카드가 352px가 된 뒤라 캔버스와 다투고,
  A·B·C가 내용을 줄여 넓힐 이유가 약해졌다.
- 진단 07(헤더+탭이 본문 전 31%)은 이번 범위 밖 — 별도 라운드.
- **드로어는 여전히 높이가 묶여 있지 않다**(페이지가 같이 자란다). Job 목록에만
  420px 상한을 둬 최악을 막아둔 상태 — D를 다룰 때 레이아웃째 봐야 한다.

## 기각 (재제안 금지)

- **시안 E — Task 전용 라우트**: 비용 최대, 캔버스와 상세를 동시에 볼 수 없게 되고
  Figma 70:35 드로어 규격 이탈.
- **하단 도킹 팬**: 선행 라운드(`pipeline-detail.md`)에서 오너가 명시 기각한 안이라
  이번 6안에 아예 포함하지 않았다.
- **카드 위 판정어(`● 완료` / `● 실패`) 표기**: 오너 기각 — 테두리 stroke 로 이미 아는 정보.
  카드에 상태 채널을 늘리는 제안은 재제안 금지(접근 이름은 예외, 위 참고).
- **제목의 Plan/Apply/Destroy 유지**: 오너 기각 — `JobKindTag` 와 중복. 카드 한정이며
  드로어 제목은 그대로다.

---

# 3차 라운드 — Job 현황 우선순위 (2026-08-15)

오너 지적: **"패널에서 보여지는 정보에 중요도가 잘못 설정된 것 같다. 가장 최근 시도의 결과,
몇 번의 Job이 있었는지, 최근 시도의 Job 목록의 로그를 쉽게 확인, Job 상태별 필터."**

아티팩트: https://claude.ai/code/artifact/0ce06c75-ace1-4b72-b694-b1d2024f1bb0
(진단 9 · 레퍼런스 13 · 시안 5). 오너 "그래 한번 적용해봐" → **A + B + C 채택**.

## 실측 진단 (기준 c23b79a6, 뷰포트 861)

| # | 문제 | 등급 | 실측 |
|---|------|------|------|
| 01 | 헤더+탭이 패널의 34.5% | 수치 | #130 aside 576 / header 154 / nav 45 · #124 761 / 131 / 45 |
| 02 | 판정이 태스크를 말하고 최신 시도의 창은 접힘 안 | UX | `attemptWindow`는 시도 이력 펼침 뒤에만 존재 |
| 03 | 집계 12px, 섹션 라벨 16px — 원하는 수치가 최소 활자 | 수치 | rollupItem/rollupTotal 12px vs sectionLabel 16/600 |
| 04 | 집계가 span이라 상태로 좁힐 수 없음 | UX | 유일한 button은 `성공 N개 펼치기` |
| 05 | 완료 태스크는 목록 통째 접힘, 펴면 3중 스크롤 | 수치 | aside 576→1012 · doc 891→1327 · 목록 420/925 · 가시 9행 |
| 06 | 행에 스캔 단서 없음 | UX | last_state·poll_count·last_polled_at 미사용 |
| 07 | 로그 진입점 51×17px가 21회 반복 | 수치 | logBtn 51×17 / 12px |
| 08 | 1회 시도 태스크의 시도 이력은 한 줄짜리 중복 | 제안 | 섹션 111px / 행 1개 |
| 09 | 실패만 사실을 얻고 성공·진행 중은 칩뿐 | 제안 | — |

## 쓴 레퍼런스 (전부 세션에서 열람)

| 제품 | URL | 가져온 것 |
|------|-----|-----------|
| Buildkite Build page | https://buildkite.com/docs/pipelines/build-page | 상태 그룹핑 토글(중요한 것 위로), 실패 순회 |
| GitHub Actions run logs | https://docs.github.com/actions/managing-workflow-runs/using-workflow-run-logs | 실패 자동 펼침, 목록 상주 |
| GitLab CI Jobs | https://docs.gitlab.com/ci/jobs/ | 상태를 1급 필터 축으로 |
| Databricks job runs | https://docs.databricks.com/aws/en/jobs/monitor | 필터+정렬 한 줄, 상태·소요를 스캔 축으로 |
| Airflow Grid | https://airflow.apache.org/docs/apache-airflow/stable/ui.html | 셀(면)이 클릭 타깃 |
| Jenkins Blue Ocean | https://www.jenkins.io/doc/book/blueocean/pipeline-run-details/ | 실패가 있으면 기본 포커스를 실패로 |
| AWS CodePipeline | https://docs.aws.amazon.com/codepipeline/latest/userguide/pipelines-view-console.html | 행에 "언제"를 상시 표기 |
| Google Cloud Build | https://docs.cloud.google.com/build/docs/view-build-results | 목록↔로그 마스터-디테일 |
| Vercel build logs | https://vercel.com/docs/deployments/logs | 제거 가능한 필터 pill |
| Stripe request logs | https://docs.stripe.com/development/dashboard/request-logs | 1급 필터는 인라인, 2급은 More 뒤 |
| Temporal Web UI | https://docs.temporal.io/web-ui | 요약뷰↔전체뷰 전환 |
| Cloudscape filter patterns | https://cloudscape.design/patterns/general/filter-patterns/ | 유한값 속성은 선택 필터, 즉시 반영 |
| Carbon status indicator | https://v10.carbondesignsystem.com/patterns/status-indicator-pattern/ | 버킷 상한, **텍스트 라벨이 주인공** |

## 구현 (A+B+C)

- **A 집계 → 상태 필터**(`j.filter*`): 버킷당 버튼 하나(0건은 버튼 없음),
  기본 선택은 **실패가 있으면 실패, 없으면 전체**(Blue Ocean·Buildkite 규칙).
  **성공 접기 토글 삭제** — 목록을 좁히는 문법은 하나. 버킷이 하나뿐이면 `전체`를 렌더하지
  않는다(같은 숫자를 두 번 말하게 된다). 폴링으로 선택한 버킷이 비면 자동 선택으로 되돌린다.
  수치 출처: `pipelineStyles.seg`(컨테이너 p1 gap1 r8 · 버튼 px3 py1 r6 14px · active gray-900),
  카운트 접미는 `tqStyles.segLg.count`의 문법이되 **톤은 weak** — 원본의 faint(#98A2B3)는
  이 패널에서 2.5:1로 미달이다. 8px 점은 `segLg.dot`.
- **B 행이 스캔되게**: 행 = 점 + job_id + `last_state · N회 폴링 · HH:mm` + chevron,
  **행 전체(451×44)가 로그 진입점**. 메타 12px = `j.vSub`(JobViewer 헤더가 같은 Job에 쓰는 부제),
  시각은 시:분만 — 날짜는 판정 사실줄의 실행 창이 이미 말한다. 칩이 점으로 바뀌면서
  한국어 판정 라벨은 필터가 짊어진다(Carbon: 라벨은 화면 어딘가에 반드시 남아야 한다) →
  **A와 B는 함께 가야 한다**.
- **C 히어로가 최신 시도를 말한다**: 사실줄 = `시도 2/2회 · 2026-08-15 07:40 → 07:50 · 10분`.
  **시도 2회 이상일 때만** 창을 붙인다 — 1회면 `taskRunLine(task)`와 같은 값이라
  "카드가 말하는 시각은 되풀이하지 않는다"(2차 결정)를 그대로 지킨다.
  `시도 이력`은 16px → **14px 보조 라벨**(`d.sectionLabelSub`), 힌트는 시도 2회 이상일 때만.
  `attemptWindow`는 AttemptDetail에서 `taskDrawerShared`로 이동(두 곳이 같은 문자열을 쓴다).

**실측(적용 후)**: #124 판정 5.96:1 · 필터 idle 4.97:1 · active 카운트 14.24:1 ·
행 메타 4.51:1 · job_id 16.1:1. 필터 클릭 → 목록 즉시 전환, 행 클릭 → 뷰어,
Esc는 뷰어만 닫고 드로어는 유지. #124 패널 761→699px, Job 현황 264→190px,
#130 시도 이력 111→80px.

## 채택안에서 벗어난 것 (1건)

아티팩트의 시안 C는 "시도 1회면 **시도 이력 섹션 자체를 렌더하지 않는다"였지만,
그 섹션이 단일 시도의 `Response 원문`·`확인 요약`에 이르는 **유일한 경로**라 삭제하면
접근이 사라진다. 그래서 **강등(14px)까지만** 했다. 회수한 높이는 111→80px.

## 남긴 것

- **시안 E**(헤더 154→84px + 실패 자동 스크롤 + 목록 캡·드로어 높이 정리)가 다음 커밋.
  진단 01·05의 잔여가 여기 묶여 있다.
- **접기가 사라져 전부 성공인 태스크는 21행이 항상 펼쳐진다** — #130 패널 576→959px,
  문서 1274px(뷰포트 861). 420px 목록 상한이 최악은 막지만 **드로어 높이 미고정은 그대로**다.
  E에서 레이아웃째 다뤄야 한다.
- **시안 D(인라인 로그 패널) 보류** — 451px 폭에 terraform 로그(120자 줄)는 가로 스크롤이
  되고, `JobViewer`(720×572 리사이즈·ANSI·복사·상태 탭)와 기능이 겹친다. 드로어 폭·높이가
  정리된 뒤에 다시 본다.

---

# 4차 — 오너 피드백 (2026-08-16)

3차 적용본(8a892c59)을 보고 온 지적 3건. 벤치마크 라운드가 아니라 **오너 직접 지시**라
레퍼런스 조사 없이 바로 반영했다.

| # | 지적 | 반영 |
|---|------|------|
| 1 | "Job 현황이 최신 시도 #2 기준이잖아. **다른 시도의 작업을 확인할 방법은 없나?**" | 시도 피커 신설 · `시도 이력` 접기 섹션 삭제 |
| 2 | 드로어 헤더(제목·설명·타입·탭 2개)를 "**아예 날리면 안 될까**" | 헤더 154px + 탭 45px = **199px 제거** |
| 3 | "**패널 자체의 스크롤을 내리는 일은 없었으면**" — 작업 목록이 패널 안에서 스크롤되게 | 페이지 열을 뷰포트에 고정 + Job 목록이 남는 높이를 먹고 스스로 스크롤 |

## 1. 시도 피커 — 접힌 이력을 없애고 전환으로 바꿨다

`시도 이력` 행을 눌러 펼치는 구조(3차 시안 C)는 **오너가 못 찾았다**. 접힌 행은
"이 안에 다른 시도의 Job이 있다"를 말하지 못한다. 그래서 이력 섹션을 통째로 지우고,
판정 히어로 바로 아래에 **Job 상태 필터와 같은 세그먼트 문법**의 시도 피커를 놓았다.

- 버튼 = `● #2` `● #1`(최신 먼저). 점은 그 시도의 판정색 — 어느 회차가 실패했는지 안 열고 읽힌다.
- 선택한 시도의 **Job 현황 · 확인 요약 · Response 원문**이 아래에 항상 펼쳐져 있다.
  `AttemptDetail`이 접힘 없이 그대로 렌더된다 → `jobsShownAbove` 중복 방지 로직도 같이 삭제.
- **시도 1회면 피커를 렌더하지 않는다**(버튼 하나 = 전환할 대상이 없음).
- 실행 창은 **히어로 사실줄로 이전**: `시도 2/2회 · 2026-08-15 07:40 → 07:50 · 10분`.
  피커로 시도를 바꾸면 이 창도 그 시도 것으로 바뀐다. 시도 1회면 창을 붙이지 않는다
  (2차 결정 "카드가 말하는 시각은 되풀이하지 않는다" 유지).

**잃은 것 1건**: 이력 행에 있던 시도별 `error_code` 미니 칩. 히어로가 태스크 판정 코드를
말하고, 실패 원인은 Job 행의 `last_fail_reason`이 인라인으로 말한다. 회차별 코드가 서로
다른 진단이 필요해지면 그때 되살린다.

## 2. 헤더 삭제 — 199px

제목(+상태 배지) · 설명 · `타입 TERRAFORM_JOB` · `실행 정보 | 정의·계약` 탭바를 모두 제거.
남긴 것은 **닫기 버튼 한 줄**(+재시작 태스크의 `이전 실행 이력 보기` 링크, §8.4).

- **이름**은 옆 흐름 카드가 말한다(선택 카드는 primary 아웃라인). 접근 이름은
  `aria-label={displayName + " 상세"}`로 유지 — 화면에서 지웠지 스크린리더에서 지운 게 아니다.
- **상태**는 판정 히어로가 말한다(카드 stroke·배지와 합쳐 이미 3채널).
- **타입/계약**은 `정의·계약`이 말한다 → 탭을 없애고 **본문 마지막 줄의 접힘**으로 강등.
  `실행 방식 TERRAFORM_JOB`이 그 안에 이미 있어서 `타입` 행은 순수 중복이었다.

## 3. 패널 스크롤 제거 — 페이지 열을 뷰포트에 묶었다

`layout.shell`은 **min-height만** 잡고 있어서, 드로어 본문이 길어지면 `.pl-flow` → `main`
→ 페이지가 통째로 늘어났다(#130에서 문서 1274px / 뷰포트 810px). 그래서:

1. `layout.contentDetail`에 `min-h-0 max-h-[calc(100vh-64px)]` — 이 열이 뷰포트를 넘지 않는다.
   `isDetail`은 `/admin/pipelines/{id}` 한 화면에만 걸린다.
2. `d.body`에 `min-h-0` — 안에서 줄어들 수 있게.
3. `Section grow` (`flex-1 min-h-[208px]`) + `j.jobList`(`flex-1 min-h-0 overflow-y-auto`) —
   Job 현황이 남는 높이를 다 먹고, 목록만 자기 안에서 스크롤한다.

**함정 2개(둘 다 실측으로 잡았다)**:
- `Section`에 `min-h-0`만 주면 형제(확인 요약·접힘 3개)에 밀려 **섹션 전체가 0px로 찌그러진다**.
- 반대로 `min-height:auto`로 두면 스크롤 컨테이너의 **콘텐츠 전체 높이(21행 925px)**가
  최소값으로 잡혀 아예 줄지 않는다. → **명시 floor(208 = 라벨 24 + 필터 56 + 3행 129)**만이 답.

**실측(뷰포트 810px)**: #130 문서 1274→810px(페이지 스크롤 0), 패널 본문 451px = 스크롤 0,
목록 142px 안에서 925px가 스크롤. #124는 본문 411px에 콘텐츠 529px라 **118px 남는다** —
`확인 요약`을 접힘으로 강등(117→20px)하고도 남은 차이로, **창 높이 ~890px 이상이면 0**이다.
그 아래에서는 아래쪽 접힘 3개만 스크롤되고 Job 현황은 항상 화면에 남는다.

## 곁들여 정리한 것

- `확인 요약`·`Response 원문`·`정의·계약` 3개가 **같은 14px 접힘 문법**(`d.foldSummary`)을 쓴다.
  `확인 요약`은 폴링 카운터라 Job 행의 `N회 폴링`과 겹쳐서 기본 접힘으로 내렸다.
- 고아 정리: `j.list`/`labelHint`/`attempt*` 6종/`respSummary`, `d.title`/`titleBadge`/
  `headerDesc`/`typeRow`/`typeLabel`/`tag`, `TaskDrawer`의 `task` prop.
  `d.nav*`는 **JobViewer의 로그/상태 탭이 계속 쓴다** — 지우면 안 된다.

## 4차 후속 — 히어로 압축 + 시각 라벨링 (같은 날)

오너 2건 더:

**(a) "판정+시도 블록이 너무 많은 공간을 차지, 위쪽 margin도 더 줄여도 된다"**
→ 시도 피커를 **판정 머리줄의 빈 오른쪽 절반으로 이전**(`d.verdictAside`, `ml-auto` +
20px/bold 리셋). 재시도 예산 `시도 2/2회`가 **피커의 라벨 역할**을 하므로 별도 `시도` 라벨은
제거. 드로어 상단 여백도 `header pt-3→pt-2`, `body pt-2→pt-1`.
**히어로 블록 117 → 66px**(−51). #124 패널 초과분 118 → **58px**, 0이 되는 창 높이 890 → **832px**.

**(b) "`시도 2/2회 · 07:40 → 07:50 · 10분`이 뭘 말하는지는 알겠는데 시작/완료/소요를 명확히 구분해라"**
→ `attemptWindow()`가 **화살표를 버리고 카드와 같은 라벨 3개**를 쓴다:
`시작 2026-08-15 07:40 · 완료 07:50 · 소요 10분`.
- 끝 시각의 날짜는 자정을 넘길 때만 반복(기존 규칙 유지).
- **없는 값은 라벨째 빠진다** — 진행 중이면 `시작 …`만. `완료 -`는 값처럼 읽혀서 안 쓴다.
- 사실줄에서 `시도 n/m회`가 빠졌으므로(머리줄로 이동), 시도 1회 태스크는 **사실줄 자체를 렌더하지 않는다**
  (카드가 이미 말한 값이라 붙일 게 없다).

---

# 5차 — 패널 감량 벤치마크 → 시안 B + C 채택 (2026-08-16)

오너: **"패널에 정보가 너무 많지 않아? 그리고 패널이 너무 넓기도 하고. 어떤 식으로 정보를
정리해볼까? design benchmark를 좀 해와볼래?"** → 리서치 후 **"B, C를 한번 적용해볼래요?"**

아티팩트: https://claude.ai/code/artifact/a5727803-7343-4c7d-942a-9353dbd24aeb
(진단 6 · 레퍼런스 13 · 시안 A~E). 기준 커밋 73dd946b, 뷰포트 1554×861.

## 실측 진단

| # | 문제 | 등급 | 실측 |
|---|------|------|------|
| P1 | `JOB_FAILED`가 한 화면에 4번 | 수치 | 실패 스트립 · 흐름 카드 `nd-meta` · 패널 히어로 칩 · 정의·계약 판정 정책 |
| P2 | 폭 500px이 내용이 요구한 값이 아님 | 수치 | 440/400/380 전부 가로 오버플로 0 · 본문 1,075px 동일. 360→1095, 320→1139 |
| P3 | 접힘을 다 펴면 1,054px 대 가시 508px | 수치 | 2.07배 |
| P4 | 실행 패널이 정의 문서를 겸함 | UX | 정의·계약 펼침 425px = 전체의 40%, 실행마다 바뀌지 않는 계약 상수 |
| P5 | Job 목록 바닥이 3행(208px) | 수치 | 패널이 설명하는 카드는 352px, 패널은 그 1.42배 |
| P6 | 접힘 3개가 같은 문법으로 평평 | 제안 | 확인 요약 / Response 원문 / 정의·계약 |

폭 실험은 `aside.style.width`를 직접 바꾼 뒤 `scrollWidth > clientWidth`인 자손 수와
본문 `scrollHeight`를 함께 기록했다. **380px이 무료 바닥**이고 500px은 그 위 120px이다.

## 쓴 레퍼런스 (B·C가 실제로 차용한 것만)

| # | 제품 | URL | 가져온 것 | 확인 |
|---|------|-----|-----------|------|
| 01 | Cloudscape — Secondary panels | https://cloudscape.design/patterns/general/secondary-panels/ | 보조 패널은 **내용을 골라 담는다** — 리소스의 모든 필드가 아니다 | 확인함 |
| 02 | Cloudscape — Split view | https://cloudscape.design/patterns/resource-management/view/split-view/ | 패널에 안 맞는 건 **목적지 화면으로 내보낸다** | 확인함 |
| 03 | Sentry — Issue Details | https://docs.sentry.io/product/issues/issue-details/ | 증거(스택)와 메타(태그·컨텍스트)를 **다른 층으로** 분리 | 확인함 |
| 04 | Apache Airflow — Task Instance | https://airflow.apache.org/docs/apache-airflow/stable/ui.html | 정적 메타는 **끝으로**, 실행 사실이 먼저 | 확인함 |
| 05 | Databricks — Monitor job runs | https://docs.databricks.com/aws/en/jobs/monitor | **실행 회차 드롭다운** — 회차가 늘어도 폭이 고정 | 확인함 |
| 07 | Temporal — Web UI | https://docs.temporal.io/web-ui | 원문(payload)은 **별도 뷰**에서 | 확인함 |
| 10 | Carbon (IBM) — Side panel 폭 | https://carbondesignsystem.com/community/patterns/create-flows/ | 사이드 패널 폭 램프(320/400/480) | 기억 기반 |

10번은 검색 스니펫에서 온 값이고 문서를 끝까지 읽지 못했다 — **폭 400px의 근거는 위
실측이지 이 램프가 아니다.** 램프는 400이 램프 안 값임을 확인하는 용도로만 썼다.

## 구현 — 시안 B (계약 추방 + 폭 400px)

- **정의·계약 접힘 → `DefinitionModal`**(`ModalShell variant="task"` 600px).
  패널 마지막 줄의 `정의·계약 보기 ↗` 텍스트 버튼이 진입점. 문법은
  `TerraformStatusModal` 그대로 — 인라인 접힘을 모달로 승격한 같은 선례다.
  Esc 계층에 한 단 추가(모달 → 드로어). **실측: Esc 1회에 모달만 닫히고 드로어는 유지.**
- **폭 500 → 400px**. `detailStyles.flow.panel`이 같은 역할에 이미 들고 있던 값.
- **Response 원문은 삭제가 아니라 `!hasJobs` 게이트**로 바꿨다. 아티팩트는 "JobViewer의
  상태 탭이 같은 페이로드"라고 썼는데 **틀렸다** — `attempt.response`는 디스패치 응답이고
  뷰어의 `상태 응답`은 **job별 폴링 응답**(`TerraformJobStateDetail.last_response`)이다.
  Job 행이 있으면 그 JSON은 행으로 이미 파생돼 있으니 접지만, 행이 없으면 디스패치가
  무엇을 돌려줬는지 아는 **유일한 기록**이라 남긴다(`failure_detail`이 쓰는 예외와 동일).

## 구현 — 시안 C (중복 제거)

- **히어로의 에러 코드 칩 제거**(TERRAFORM만). 실패 스트립이 화면 상단에서, 흐름 카드가
  바로 옆에서 `원인은 JOB_FAILED.`(PipelineDetailView `resolveMeta`)라고 이미 말한다.
  **CONDITION의 코드 칩은 유지** — 그건 `last_external_status`(`READY`)로, 다른 표면이
  말하지 않는 값이다. **실측: 한 화면의 `JOB_FAILED` 4회 → 2회.**
- **시도 세그먼트 → `PlSelect` 드롭다운**. 재시도 예산이 5까지 가면 세그먼트가 히어로
  줄을 함께 늘렸다. 세그먼트가 점으로 나르던 회차별 판정은 **옵션 문구**로 옮겼다
  (`시도 #2 · 실패`) — 채널만 바뀌고 정보는 남는다.
- **`attemptWindow`가 히어로 → Job 현황 캡션으로 이전**. "지금 보고 있는 시도의 창"이라는
  소속이 분명해진다. 시도 2회 이상일 때만 붙이는 규칙(2차 결정)은 그대로고, 그 판단은
  `TerraformExec`이 하고 문자열만 `AttemptDetail` → `JobStatus` → `Section caption`으로 내린다.
- **실패 스트립 `sticky top-0`** — 시안 C의 선행 조건. 다만 **부분적이다**: sticky는 자기
  포함 블록을 못 벗어나는데 `bleed`가 `100vh-64px`로 캡돼 있다(캔버스가 그 밖으로 넘칠 뿐
  열을 키우지 않는다). 같은 측정에서 스트립 top이 **-579 → -72**로만 개선된다.
  완전 고정은 그 열이 자체 스크롤하게 만드는 별도 레이아웃 변경이 필요하다.

## 채택안에서 벗어난 것 (1건, 실측 근거)

아티팩트의 B는 **Job 목록 바닥 208 → 340px**였지만 **226px로 넣었다.**

바닥을 340으로 올리면 **드로어가 61px 스크롤한다** — 4차에서 오너가 명시로 금지한 것이다
("패널 자체의 스크롤을 내리는 일은 없었으면"). 실측 스윕(#124, 뷰포트 861):

| 바닥 | 섹션 | 목록 | 패널 스크롤 |
|---|---|---|---|
| 208 · 226 · 260 · 278 | 279 | 165 | **0** |
| 300 | 300 | 186 | 21 |
| 340 | 340 | 226 | **61** |

**회수한 높이는 바닥이 아니라 `flex-1`로 목록에 간다** — 정의·계약 접힘(20px)과 그 gap(24px)이
빠지면서 목록이 138 → **165px**로 늘었다. 바닥은 바닥이지 할당량이 아니다. 226 =
라벨 24 + 캡션 18 + 필터 56 + 3행 129로, 208이 보장하던 3행을 **시안 C가 더한 캡션까지 얹어**
그대로 유지한 값이다.

곁들여: 접힘이 하나만 남으면서 **P3의 진짜 값은 접힘이 아니라 펼침**이었다는 게 드러난다 —
정의·계약은 접혀 있을 때 44px밖에 안 먹었고, 425px는 **여는 순간** 패널을 스크롤로 밀어넣던
비용이었다. B가 산 건 폭 100px과 그 1,054px 펼침의 소멸이다.

## 실측 검증 (적용 후)

| 항목 | 값 | 판정 |
|---|---|---|
| 패널 폭 | 400px · 가로 오버플로 0 | 통과 |
| 패널 스크롤 | #124 **0** · #128 **0** | 통과 |
| `JOB_FAILED` 등장 | 4 → **2회**(스트립 · 카드) | 통과 |
| Job 목록 | 165px(3.8행), 섹션 279px | 통과 |
| Esc 계층 | 모달만 닫힘, 드로어 유지 | 통과 |
| CONDITION(#128) | 400px · 스크롤 0 · `READY` 칩 유지 | 통과 |

## 기각 / 보류 (재제안 금지)

- **시안 A(폭만 400)** — 진단 6건 중 1건만 덮는다. B가 폭을 포함한다.
- **시안 D(하단 도킹)** — 카드와 패널이 같은 시선 축에 있다는 현재 구조의 장점을 버린다.
  선행 라운드(`pipeline-detail.md`)에서도 오너가 기각한 방향이다.
- **시안 E(400 기본 + 720px 확장 토글)** — 720px은 램프 밖 신규 수치이고, P1·P4의 원인이
  그대로 남는다. 넓힐 수 있게 만들 뿐이다.
- **패널에 태스크 이름 복원** — 4차에서 헤더 199px를 걷어낸 근거(옆 카드가 이름과 상태를
  들고 있다)가 유효하고, B·C 어느 쪽도 카드를 화면에서 밀어내지 않는다.

## 5차 후속 — 오너 피드백 3건 (2026-08-17)

| # | 지적 | 반영 |
|---|------|------|
| 1 | "`전체 3 / 실패 1 / 성공 2` 가 **오른쪽으로 쭉 뻗어 있어서** 깔끔하지 않다" | 필터 트랙에 `self-start` |
| 2 | "드롭다운이 **기본 디자인**을 쓰신 듯 — repo에서 쓰는 drop down 디자인 사용하시죠" | 네이티브 `<select>` → `AttemptPicker`(트리거 + 팝오버) |
| 3 | "`시도 2/2회` 도 없어도 될 듯. **확인 좀**" | 삭제 — 아래 확인 결과 |

**1. `inline-flex`는 flex 자식으로 들어가면 살아남지 않는다.** `j.filter`는
`inline-flex`인데 `Section`의 `flex flex-col` 안에서는 자식이 blockify되고
`align-self: stretch`로 열 폭을 채운다. 실측 **트랙 351px · 마지막 버튼 오른쪽 여백 123px**.
`self-start` 한 줄로 **232px · 여백 5px**(컨테이너 자기 `p-1`)이 됐다.

**2. 네이티브 select의 문제는 토큰이 아니라 `appearance: auto`였다.** `PlSelect`는 repo의
select가 맞고 폭·높이·반경도 램프 안이었지만, `appearance: auto`면 **컨트롤과 옵션 목록을
브라우저가 그린다** — 폼 컨트롤이 하나도 없는 패널에서 혼자 OS 위젯으로 보인다
(`cursor: default`까지). 그래서 repo가 이미 쓰는 **팝오버 문법**으로 바꿨다:

- 트리거 metrics = 공용 select 그대로(`pipelineInputBase` h8 / r8 / 14px). 셰브런은
  `chev-r`을 `rotate-90` 하는 repo 관용구(`CollapsibleSection`·`ProcessGuideStepCard`).
- 패널 = `JiraTicketMenu` · 요청 목록 `FilterMenu`가 쓰는 메뉴 카드
  (r10 · border · `--pl-shadow-lg` · `py-1`, 항목 `px-3 py-2 14px hover:gray-50`).
- 세그먼트가 점으로 나르던 회차별 판정은 **점 + 판정어 + 선택 체크**로 남는다.
  `role="listbox"`/`option` + `aria-selected` — 값 선택이지 액션 메뉴가 아니다.
- **Esc는 목록만 닫는다** — `window` **캡처** 리스너에서 `stopPropagation`
  (`CustomBuildStep`의 선례). 실측: Esc 1회 → 목록 닫힘 · 드로어 유지 · 포커스는 트리거로.

**3. `시도 2/2회` 삭제는 안전하다 (확인 결과).** 이 줄이 나르던 값은 둘인데 둘 다 다른 데
있다 — **실제 시도 횟수**는 피커 목록의 항목 수이고(트리거가 `시도 #2`라고 이름까지 말한다),
**재시도 예산**은 실행 중이면 실행 밴드가 `재시도 f/m`(`retrySuffix`)로, 계약값은
정의·계약 모달의 `retry_budget` 행으로 말한다. 종결된 태스크에 남은 예산은 의미가 없다.

실측(적용 후, #124): 트리거 103×32 · 네이티브 select 0개 · 히어로 `실패` / `시도 #2` 2줄 ·
패널 스크롤 0. 피커로 #1을 고르면 캡션·Job 목록이 그 시도 것으로 함께 바뀐다.
