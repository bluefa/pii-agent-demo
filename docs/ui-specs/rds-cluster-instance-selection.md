# RDS Cluster Instance 선택 UX 설계

> RDS Cluster(Regional/Global) 연동 시 Instance 단위 선택 UX

---

## 1. 배경

### 현재 문제
- RDS Cluster를 단일 리소스로 취급하여 Cluster ARN만 표시
- Private Endpoint Service는 **Instance 단위**로 설치되므로 Cluster 선택만으로 부족
- Regional/Global Cluster 구분 없음, Reader/Writer Instance 역할 정보 없음

### 목표
- Cluster 선택 시 하위 Instance를 노출하여 연동 대상 Instance를 명시적으로 선택
- Regional Cluster, Global Cluster 동일한 UX로 처리
- Instance가 많을 때(5개 이상) 접기/펼치기로 가독성 확보

---

## 2. 데이터 모델 확장

### 기존

```typescript
interface Resource {
  awsType?: AwsResourceType; // 'RDS' | 'RDS_CLUSTER' | ...
  resourceId: string;        // Cluster ARN
}
```

### 확장

```typescript
// Cluster Instance 정보
interface ClusterInstance {
  instanceId: string;        // Instance ARN
  role: 'READER' | 'WRITER';
  availabilityZone: string;  // ap-northeast-2a 등
  isSelected: boolean;
}

// Cluster 유형
type RdsClusterType = 'REGIONAL' | 'GLOBAL';

interface Resource {
  // 기존 필드 유지
  awsType?: AwsResourceType;
  resourceId: string;

  // RDS_CLUSTER 전용 확장
  clusterType?: RdsClusterType;
  clusterInstances?: ClusterInstance[];
}
```

---

## 3. UX 상세 설계

### 3.1 리소스 테이블 내 Cluster 표시 구조

```
ResourceTable
  └─ ResourceTypeGroup (awsType: RDS_CLUSTER)
       ├─ 타입 헤더: "RDS Cluster (N)"
       └─ ClusterRow (Cluster 단위)
            ├─ Cluster 정보 행 (체크박스 + 요약)
            └─ InstancePanel (체크 시 펼침)
                 ├─ 안내 텍스트
                 ├─ Instance 서브테이블
                 └─ 더 보기/접기 (5개 이상)
```

### 3.2 Cluster 미선택 상태

Instance 패널 숨김. Cluster 요약 정보만 표시.

```
┌──┬───────────────┬────────────┬──────────────────────────────────┬──────┐
│  │ 인스턴스 타입  │ 데이터베이스 │ 리소스 ID                        │      │
├──┼───────────────┼────────────┼──────────────────────────────────┼──────┤
│☐ │ 🗄 RDS_CLUSTER│ PostgreSQL │ aurora-prod-01                   │      │
│  │ Regional      │            │ 3 Instances                      │      │
├──┼───────────────┼────────────┼──────────────────────────────────┼──────┤
│☐ │ 🗄 RDS_CLUSTER│ MySQL      │ aurora-global-01                 │      │
│  │ Global        │            │ 7 Instances                      │      │
└──┴───────────────┴────────────┴──────────────────────────────────┴──────┘
```

**표시 요소:**
- 체크박스: 연동 대상 확정(1단계) 또는 편집 모드일 때만 활성화
- 인스턴스 타입: `RDS_CLUSTER` + Cluster 유형 뱃지 (`Regional` / `Global`)
- 데이터베이스: DB 엔진 (PostgreSQL, MySQL 등)
- 리소스 ID: Cluster 식별자 + Instance 개수 (`N Instances`)

### 3.3 Cluster 선택 시 — Instance 패널 노출

Cluster 체크박스 선택 → 하위 Instance 서브테이블이 인라인으로 펼쳐짐.

#### Instance 4개 이하: 전체 노출

```
├──┼───────────────┼────────────┼──────────────────────────────────┼──────┤
│☑ │ 🗄 RDS_CLUSTER│ PostgreSQL │ aurora-prod-01                   │  ▲   │
│  │ Regional      │            │ 3 Instances                      │      │
├──┴───────────────┴────────────┴──────────────────────────────────┴──────┤
│  ┌────────────────────────────────────────────────────────────────────┐ │
│  │ 연동할 Instance를 선택하세요                                       │ │
│  │ ℹ️ Reader Instance 권장 · 1개 이상 선택 필수                       │ │
│  ├────┬──────────┬──────────────────────────────┬─────────────────────┤ │
│  │    │ 역할     │ Instance ID                  │ Availability Zone   │ │
│  ├────┼──────────┼──────────────────────────────┼─────────────────────┤ │
│  │ ☑  │ Reader   │ aurora-prod-01-reader-1      │ ap-northeast-2a     │ │
│  │ ☐  │ Reader   │ aurora-prod-01-reader-2      │ ap-northeast-2c     │ │
│  │ ☐  │ Writer   │ aurora-prod-01-writer        │ ap-northeast-2b     │ │
│  └────┴──────────┴──────────────────────────────┴─────────────────────┘ │
```

#### Instance 5개 이상: 접기/펼치기

최초 3개만 노출, 나머지는 "N개 더 보기" 버튼으로 펼침.

```
│  ├────┼──────────┼──────────────────────────────┼─────────────────────┤ │
│  │ ☑  │ Reader   │ aurora-global-01-reader-1    │ ap-northeast-2a     │ │
│  │ ☐  │ Reader   │ aurora-global-01-reader-2    │ ap-northeast-2c     │ │
│  │ ☐  │ Writer   │ aurora-global-01-writer      │ ap-northeast-2b     │ │
│  ├────┴──────────┴──────────────────────────────┴─────────────────────┤ │
│  │                         ▼ 4개 더 보기                              │ │
│  └────────────────────────────────────────────────────────────────────┘ │
```

**펼친 후:**

```
│  │ ☑  │ Reader   │ aurora-global-01-reader-1    │ ap-northeast-2a     │ │
│  │ ☐  │ Reader   │ aurora-global-01-reader-2    │ ap-northeast-2c     │ │
│  │ ☐  │ Writer   │ aurora-global-01-writer      │ ap-northeast-2b     │ │
│  │ ☐  │ Reader   │ aurora-global-01-reader-3    │ us-east-1a          │ │
│  │ ☐  │ Reader   │ aurora-global-01-reader-4    │ us-east-1b          │ │
│  │ ☐  │ Reader   │ aurora-global-01-reader-5    │ eu-west-1a          │ │
│  │ ☐  │ Reader   │ aurora-global-01-reader-6    │ eu-west-1b          │ │
│  ├────┴──────────┴──────────────────────────────┴─────────────────────┤ │
│  │                         ▲ 접기                                     │ │
│  └────────────────────────────────────────────────────────────────────┘ │
```

**접기/펼치기 규칙:**
- Instance < 5: 전체 노출, 접기/펼치기 버튼 없음
- Instance >= 5: 최초 3개 노출 + "N개 더 보기"
- 이미 선택된 Instance는 접힌 상태에서도 상단에 우선 노출

### 3.4 Instance 선택 완료 후 접힌 상태

`▲` 아이콘 클릭으로 Instance 패널을 접으면 선택 요약 표시.

```
├──┼───────────────┼────────────┼──────────────────────────────────┼──────┤
│☑ │ 🗄 RDS_CLUSTER│ PostgreSQL │ aurora-prod-01                   │  ▼   │
│  │ Regional      │            │ 3 Instances · 1개 선택됨          │      │
│  │               │            │ └ reader-1 (Reader, 2a)          │      │
├──┼───────────────┼────────────┼──────────────────────────────────┼──────┤
│☑ │ 🗄 RDS_CLUSTER│ MySQL      │ aurora-global-01                 │  ▼   │
│  │ Global        │            │ 7 Instances · 2개 선택됨          │      │
│  │               │            │ └ reader-1 (Reader, 2a)          │      │
│  │               │            │ └ reader-3 (Reader, us-east-1a)  │      │
├──┼───────────────┼────────────┼──────────────────────────────────┼──────┤
```

**표시 요소:**
- `N Instances · M개 선택됨`: 전체/선택 카운트
- 선택된 Instance 이름을 요약 라인으로 표시
- `▼` 클릭으로 다시 펼쳐서 수정 가능

### 3.5 읽기 전용 (2단계 이후)

연동 대상 확정 후에는 체크박스 없이 확정 상태만 표시.

```
├──┼───────────────┼────────────┼──────────────────────────────────┼──────┤
│  │ 🗄 RDS_CLUSTER│ PostgreSQL │ aurora-prod-01                   │      │
│  │ Regional      │            │ └ reader-1 (Reader, 2a) ✓ 연동   │      │
├──┼───────────────┼────────────┼──────────────────────────────────┼──────┤
```

---

## 4. 인터랙션 플로우

### 4.1 Cluster 선택 → Instance 선택

```
[Cluster 미선택] ──(체크)──→ [Instance 패널 펼침]
                              ──(Instance 체크)──→ [선택 반영]
                              ──(▲ 접기)──→ [요약 표시]
                              ──(▼ 펼침)──→ [Instance 패널 다시 노출]
```

### 4.2 Cluster 선택 해제

```
[Cluster 선택됨] ──(체크 해제)──→ [하위 Instance 선택 모두 초기화]
                                   [Instance 패널 숨김]
```

### 4.3 연동 대상 확정

```
[확정 버튼 클릭]
  ├─ Cluster 선택 + Instance 0개 → 경고: "선택한 Cluster의 Instance를 1개 이상 선택하세요"
  ├─ Cluster 선택 + Instance 1개+ → 정상 진행
  └─ 일반 RDS → 기존 로직 유지
```

---

## 5. 상태별 동작 정리

| 상태 | Cluster 행 | Instance 패널 | 비고 |
|------|-----------|--------------|------|
| 미선택 | `☐` + Cluster 이름 + `N Instances` | 숨김 | |
| 선택 + 펼침 | `☑` + `N Instances` | 서브테이블 노출 | Instance 선택 가능 |
| 선택 + 접힘 | `☑` + `M개 선택됨` + 선택 요약 | 숨김 | `▼`로 다시 펼침 |
| 확정 후 (읽기) | Cluster + 확정 Instance 인라인 | 숨김 | 수정 불가 |
| 편집 모드 | 선택 모드와 동일 | 서브테이블 노출 | 기존 편집 모드 버튼으로 진입 |

---

## 6. 검증 규칙

| 규칙 | 동작 |
|------|------|
| Cluster 체크 + Instance 0개 | "연동 대상 확정" 버튼 비활성화, 인라인 경고 표시 |
| Cluster 체크 해제 | 하위 Instance 선택 모두 초기화 |
| Writer만 선택 | 허용 (Reader 권장은 안내 수준, 차단하지 않음) |
| Instance 1개 선택 | 정상 (최소 요건 충족) |
| Instance 전체 선택 | 허용 (복수 선택 가능) |

---

## 7. 시각적 구분

### 역할 뱃지
- **Reader**: 녹색 계열 뱃지 (`statusColors.success` 활용)
- **Writer**: 주황색 계열 뱃지 (`statusColors.warning` 활용)

### Cluster 유형 뱃지
- **Regional**: 기본 회색 뱃지
- **Global**: 파란색 계열 뱃지 (`statusColors.info` 활용)

### Instance 패널
- 배경: `bg-slate-50` 또는 유사한 미묘한 구분
- 좌측 들여쓰기로 계층 표현
- 테이블 내부 테이블(서브테이블) 형태

---

## 8. 컴포넌트 구조 (구현 참고)

```
ResourceTable
  └─ ResourceTypeGroup (awsType: RDS_CLUSTER)
       └─ ClusterRow                    ← 신규 컴포넌트
            ├─ ClusterSummary           ← Cluster 행 (체크박스 + 요약)
            └─ ClusterInstancePanel     ← 신규 컴포넌트 (펼침 시)
                 ├─ 안내 텍스트 영역
                 ├─ InstanceRow[]       ← Instance 개별 행
                 └─ ExpandCollapseButton ← 5개 이상 시
```

### 기존 패턴 참고
- `VmDatabaseConfigPanel`: EC2 선택 시 인라인 설정 패널 (확장/축소 패턴)
- `ResourceRow`: 기존 리소스 행 구조 (체크박스, 아이콘, 상태)

---

## 9. 일반 RDS와의 관계

- 일반 RDS (`awsType: 'RDS'`)는 기존과 동일하게 단일 행으로 표시
- RDS_CLUSTER만 Instance 패널이 추가됨
- 타입 그룹 순서: RDS → RDS Cluster → Redshift → DynamoDB → Athena → EC2

---

## 변경 이력

| 날짜 | 내용 |
|------|------|
| 2026-02-08 | 초안 작성 — 계층형 테이블 + 인라인 Instance 선택 UX 확정 |
