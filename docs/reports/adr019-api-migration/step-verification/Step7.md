# Step 7 (설치 완료 / Installation Complete) — API Verification

Scope: cloud Step 7 = `app/integration/target-sources/[targetSourceId]/_components/layout/InstallationCompleteStep.tsx`; IDC Step 7 = `app/integration/target-sources/[targetSourceId]/_components/idc/steps/IdcStep7Complete.tsx`.
Intended-API source: `/Users/study/pii-agent-migration-notes/01-target-source-detail-spec.md` §9.
Contract: `docs/swagger/install-v1.yaml`.

Path resolution note: UI clients call `${CONFIRM_BASE}/...` where `CONFIRM_BASE = '/target-sources'` (`app/lib/api/index.ts:294`). `fetchInfraJson` (`app/lib/api/infra.ts:12`) maps `path` → internal proxy `/integration/api/v1/...` → upstream `/install/v1/...` (`lib/infra-api.ts:1-2`). So a UI call to `/target-sources/{id}/confirmed-integration` lands on swagger path `/install/v1/target-sources/{targetSourceId}/confirmed-integration`. The doc's `/confirm/v1/...` notation (§8) is BFF-internal routing; the actual upstream prefix is `/install/v1`, which is what swagger documents and what the demo proxies to.

## Verification Matrix

| Intended API (doc §9) | In swagger? (path + method + schema) | Currently called? (file:line; stub vs real) | Verdict |
|---|---|---|---|
| GET `…/confirmed-integration` — 승인된 연동 DB 목록 (화면 로드) | YES. `/install/v1/target-sources/{targetSourceId}/confirmed-integration` GET `getConfirmedIntegration` (swagger:2897-2967); 200 → `ConfirmedIntegrationResponse` = `{ resource_infos: ResourceConfigDto[] }` (swagger:5516-5522). | **Cloud: REAL.** `InstallationCompleteStep.tsx:99` mounts `ConfirmedIntegrationDataProvider`, which calls `getConfirmedIntegration` (`ConfirmedIntegrationDataProvider.tsx:40`) → client `app/lib/api/index.ts:421-430` → route `app/integration/api/v1/target-sources/[targetSourceId]/confirmed-integration/route.ts:26` (`bff.confirm.getConfirmedIntegration`, real BFF passthrough, 404→problem). | **OK (cloud).** Path/method/schema match; real wiring. |
| (same) GET `…/confirmed-integration` for IDC Step 7 | (same swagger entry as above) | **IDC: DIVERGENT.** `IdcStep7Complete.tsx:75` uses `useIdcResources(...)` → `getIdcResources` → `${idc}/resources` i.e. `/install/v1/target-sources/{id}/idc/resources` (`app/lib/api/idc.ts:154-160`), NOT `confirmed-integration`. | **DIVERGENCE (intentional).** IDC Step 7 reads the IDC resource list (`…/idc/resources`), not `confirmed-integration`. Code comment notes the mock co-locates fields on `/resources` for the demo "(BFF splits across project/confirmed-integration — see plan §4.1)" (`app/lib/api/idc.ts:12-15`). Doc §9 lists `confirmed-integration` for IDC too; mismatch is a known demo shortcut, not a contract gap. |
| PUT `…/test-connection-acknowledgment` — 연결 테스트 재실행 (Step1으로 안 가고 Step5로 rollback) | YES. `/install/v1/target-sources/{targetSourceId}/test-connection-acknowledgment` PUT `updateTestConnectionConfirmation` (swagger:51-126); body **required** `UpdateTestConnectionConfirmationRequest = { confirmed: boolean }` (swagger:4323-4329); 200 → `TestConnectionConfirmationResponse` (swagger:4330-4340). | **NOT called from Step 7 — STUB.** "연결 테스트 재실행" button (`InstallationCompleteStep.tsx:68-75`; IDC `IdcStep7Complete.tsx:46-49`) only fires `toast.info(...)` placeholder ("…BFF 연동 후 활성화됩니다."). The client `updateTestConnectionConfirmation` exists (`app/lib/api/index.ts:706-713`, PUT body `{confirmed}`) and the route is REAL (`…/test-connection-acknowledgment/route.ts`, `bff.confirm.updateTestConnectionConfirmation`, whose comment states `confirmed:false = 연결 테스트 재실행 rollback`). But grep shows the client has **zero UI callers** — Step 7 does not invoke it. | **GAP (should-call-but-doesn't).** Contract + client + route all exist and `confirmed:false` is the documented rollback path; Step 7's button is a toast stub. Wire the 재실행 button to `updateTestConnectionConfirmation(id, false)`. |
| 인프라 변경 — DB 정보 수정/추가 (Step1 이동); doc says "현재 토스트 스텁" | N/A — no dedicated endpoint; intended as a stepper rewind to Step 1. | **STUB (as documented).** `InstallationCompleteStep.tsx:60-67` (IDC `IdcStep7Complete.tsx:42-45`) opens `ConfirmRewindModal`; `handleConfirm('infra')` → `toast.info('인프라 변경(1단계로 되돌아가기)은 BFF 연동 후 활성화됩니다.')`. Component comment: "the rewind endpoint is not in the contract yet" (`InstallationCompleteStep.tsx:47-48`). | **OK / documented stub.** Matches doc ("현재 토스트 스텁"). No swagger endpoint for rewind exists; deferred to BFF. |
| PUT `…/approval` — "관리자 승인 완료 후 … 최종 승인 저장 + Step7 전환" (doc §9 table) | **NO.** No `/install/v1/target-sources/{targetSourceId}/approval` path in swagger. Approval family present: `…/approval-requests` (+ `/approve`,`/reject`,`/cancel`,`/latest`), `…/approval-unavailable` (+`/confirm`), `…/approval-history`. Bare `…/approval:` does not exist. | Not called anywhere as a bare `/approval` PUT (no UI/client reference). | **DOC ARTIFACT / GAP.** `PUT …/approval` is not in the contract. It describes an admin-side action that flips the target source into INSTALLED/Step7; the real admin endpoint is `POST …/approval-requests/approve` (`approveApprovalRequest`, swagger:1244). Treat the doc's `/approval` as a loose label for the admin approve flow, not a Step 7 user call. No client work needed for Step 7. |

## Summary

- **GET `confirmed-integration`** is correctly wired for **cloud** Step 7 (real BFF passthrough; path/method/`ConfirmedIntegrationResponse` schema all match swagger:2897/5516).
- **IDC** Step 7 diverges: it reads `…/idc/resources` via `useIdcResources`, not `confirmed-integration` as the doc states — a known demo shortcut (mock co-locates fields), not a contract gap.
- **PUT `test-connection-acknowledgment`** is a real, in-contract endpoint with an existing client (`updateTestConnectionConfirmation`, body `{confirmed:false}` = 재실행 rollback), but Step 7's "연결 테스트 재실행" button is a **toast stub** — primary actionable GAP.
- **"인프라 변경"** is a documented toast stub (rewind-to-Step1; no contract endpoint yet) — expected.
- **PUT `…/approval`** from the doc is **not in swagger** (doc artifact); the genuine admin action is `POST …/approval-requests/approve`. Not a Step 7 user-facing call.
