# v15 Audit — Transitions (per cell)

## azure / step 1 (연동 대상 선택·입력) / targetSourceId=1005
- trigger: 연동 대상 승인 요청 button → ApprovalRequestModal submit → createApprovalRequest(targetSourceId) | advanceWorks: True | refetch: True
- issue: Wiring is correct (NOT the IDC step1 TODO bug). approval mutation (CandidateResourceSection.tsx:105-124) calls createApprovalRequest then await refreshProject(); WaitingTargetConfirmationStep.refreshProject (line 38-41) calls getProject(targetSourceId) and onProjectUpdate, which re-renders into the next step. confirm.ts createApprovalRequest advances processStatus toward WAITING_APPROVAL. CAVEAT: button is disabled at load because selectedCount===0 (no rows pre-checked), so a user must manually check rows before it can fire — v15 has them pre-checked. Functional advance path itself is sound.

## azure / step 2 (승인 대기) / targetSourceId 2002
- trigger: auto-poll (admin-side approval). ProcessStatusCard polls getProcessStatus every TIMINGS.PROCESS_STATUS_POLL_MS while WAITING_APPROVAL; when BFF status != 'PENDING' it refetches getProject and calls onProjectUpdate. The visible '전체 요청 취소' button is a cancel/regress action, not an advance. | advanceWorks: True | refetch: True
- issue: Transition wiring is correct: ProcessStatusCard.tsx:53-67 polls getProcessStatus, on change fetches getProject(targetSourceId) and propagates via onProjectUpdate → CloudTargetSourceLayout re-renders to ApplyingApprovedStep. Both advance + refetch work. NOT the IDC step1 TODO bug (different code path). Only caveat: because the resource list is empty (data bug), the cancel button operates on a 0-row set, but the transition mechanism itself is sound.

## azure / step 3 (반영 중, APPLYING_APPROVED) / targetS
- trigger: v15 has a 'Next' primary button (onclick setStep(4)) in the card footer. The impl renders NO Next button on this step; APPLYING_APPROVED is meant to auto-advance to INSTALLING via ProcessStatusCard polling. | advanceWorks: False | refetch: False
- issue: There is no manual advance control on the impl step-3 (v15's footer 'Next' button is absent). Advancing relies on ProcessStatusCard auto-poll, but the 2003 seed is a static cloneForStep fixture whose backend status never progresses past APPLYING_APPROVED and whose approvedIntegrationStore is unseeded, so nothing drives a transition to INSTALLING and no refetch of project/process-status is triggered from this cell. Net: for this demo id the step neither auto-advances nor offers a manual Next, unlike v15.

## azure / step 4 (Agent 설치) / targetSourceId=1004
- trigger: auto (install-complete polling): AzureInstallationInline fires onInstallComplete when allCompleted (every confirmed resource step === COMPLETED). No explicit next button in v15 step 4 either. | advanceWorks: True | refetch: True
- issue: Path is wired: onInstallComplete -> CloudInstallingStep.refreshProject -> getProject(targetSourceId) -> onProjectUpdate, which re-derives the step. For 1004 install is intentionally incomplete (phase3 진행중 0/3) so it correctly stays on step 4. Note: refreshProject only refetches the project (process-status card derives from project, so OK). Not the IdcStep1 TODO bug — that suspect is IDC-only and does not apply here.

## azure / step 5 (연결 테스트) / targetSourceId 2004
- trigger: v15: footer '완료 승인 요청' primary button (onclick setStep(6)). Impl: no such button — only '연결 테스트 수행' which triggers a connection-test poll. | advanceWorks: False | refetch: False
- issue: The impl step 5 has NO mechanism to advance processStatus from WAITING_CONNECTION_TEST → CONNECTION_VERIFIED. ConnectionTestPanel only runs useTestConnectionPolling.trigger() (triggerTestConnection + poll to SUCCESS/FAIL) and shows a ResultSummary; on success it does NOT call onProjectUpdate/refreshProject and does NOT change processStatus. onResourceUpdate (→ WaitingConnectionTestStep.refreshProject → getProject) fires ONLY after a credential modal completion (ConnectionTestPanel.tsx:116-121), not after a successful test. There is no '완료 승인 요청' button to move to step 6. Net: the v15 advance-to-step-6 flow is missing entirely (analogous to the IDC step1 TODO suspect).

## azure / step 6 (완료 여부 관리자 승인 대기 — CONNECTION_VERIF
- trigger: none (admin out-of-band final approval). On-screen the only button '연결 테스트 재실행' is a toast stub: toast.info('연결 테스트 재실행 기능 준비중입니다.') | advanceWorks: False | refetch: False
- issue: CONNECTION_VERIFIED -> INSTALLATION_COMPLETE has NO client trigger and NO polling. ProcessStatusCard.tsx:35-46 polls only for WAITING_APPROVAL/APPLYING_APPROVED (shouldPoll excludes CONNECTION_VERIFIED), so when admin grants final approval the screen never refetches getProcessStatus/getProject — user must hard-reload to reach step 7. v15 likewise has no advance button here (admin-driven), so the missing button is by-design, but the missing poll/refetch for the approval transition is a real gap. The '연결 테스트 재실행' button is also a non-functional stub (toast only) whereas v15 wires it to openConfirmStep('retest').

## provider=azure, step=7 (연동 완료 / INSTALLATION_COMPL
- trigger: none (INSTALLATION_COMPLETE is terminal). On-card buttons 인프라 변경 / 연결 테스트 재실행 in v15 open a confirm modal to RE-RUN the process, not advance forward. | advanceWorks: None | refetch: None
- issue: Both action buttons are non-functional STUBS: InstallationCompleteActions (InstallationCompleteStep.tsx:39-62) wires onClick={stub('인프라 변경')} / stub('연결 테스트 재실행') which only fire toast.info('... 기능 준비중입니다.'). v15 calls openConfirmStep('infra'|'retest') which opens a confirmation modal that resets/re-runs the process and would refetch. So the re-run transition is NOT implemented: no processStatus change, no project/process-status refetch — same gap class as the IDC step1 TODO.

## provider=gcp · step=1 (연동 대상 선택/입력) · targetSource
- trigger: '연동 대상 승인 요청' button (in CandidateResourceTable footer; opens ApprovalRequestModal, then createApprovalRequest) | advanceWorks: True | refetch: True
- issue: Wired correctly (NOT the IDC step1 TODO bug). handleRequestApproval → approvalModal.open → handleApprovalSubmit → approval.mutate → createApprovalRequest(targetSourceId, …) then `await refreshProject()` (CandidateResourceSection.tsx:108-114), which calls getProject and onProjectUpdate, advancing processStatus WAITING_TARGET_CONFIRMATION → WAITING_APPROVAL and refetching. Caveat: the button is unreachable for 1002 in the impl because the candidate table never renders (no seeded scan), so the transition cannot be exercised end-to-end without the data seed.

## gcp / step 2 (승인 대기) / targetSourceId=2007
- trigger: auto-poll (admin approval) — WAITING_APPROVAL → APPLYING_APPROVED is external/mock auto-transition, not an in-page button. The only on-screen action '연동 대상 승인 요청 취소' moves BACKWARD to step 1. | advanceWorks: None | refetch: False
- issue: No forward-advance handler on this step. WaitingApprovalStep wires onProjectUpdate only to the cancel button's refreshProject (getProject + onProjectUpdate) and to ProcessStatusCard; there is no interval/poll that detects an external admin approval and re-fetches process-status, so a backend approval will NOT auto-advance the open page until manual reload. The cancel (backward) path DOES refetch via refreshProject(getProject) and updates state correctly. Forward auto-advance/refetch on this screen is effectively absent.

## gcp · step 3 (반영 중 / APPLYING_APPROVED) · targetSo
- trigger: auto-poll (ProcessStatusCard.tsx useEffect polls getProcessStatus every TIMINGS.PROCESS_STATUS_POLL_MS while step===APPLYING_APPROVED); v15 also offers a manual 'Next' button (setStep(4)) which impl does not render | advanceWorks: False | refetch: True
- issue: Auto-advance is wired (poll detects process_status !== 'CONFIRMING', then refetches via getProject + onProjectUpdate — refetch path itself is correct), BUT it can never fire for fixture 2008. Two compounding mock bugs: (1) the v1 process-status route ALWAYS derives process_status from project.processStatus via toBffApprovalProcessStatus, returning 'CONFIRMING' as long as the stored project stays APPLYING_APPROVED. (2) The underlying confirm.getProcessStatus auto-transition (confirm.ts:951) requires approval.status APPROVED/AUTO_APPROVED + installation PENDING + a seeded approvedAt, but createStatusForProcessStatus (lib/mock-data.ts:34-90) has NO case for APPLYING_APPROVED so it falls to the default base status (approval not APPROVED, no approvedAt, no approvalTimestampStore entry). The project therefore never updates to INSTALLING. Net: the screen is stuck on step 3 forever. FIX: add an APPLYING_APPROVED case to createStatusForProcessStatus (approval APPROVED + approvedAt + installation PENDING) AND seed approvalTimestampStore for the 200x applying fixtures, or render the manual Next button.

## gcp / step 4 (Agent 설치 / INSTALLING) / targetSourc
- trigger: auto on data-complete: useInstallationStatus fetches installation-status once on mount; when isComplete (summary.allCompleted===true) it calls onComplete -> CloudInstallingStep.refreshProject -> getProject -> onProjectUpdate. Also a manual "새로고침" icon (GcpInstallationInline.tsx:71-84) calls checkGcpInstallation. | advanceWorks: False | refetch: True
- issue: There is NO interval polling — useInstallationStatus (app/hooks/useInstallationStatus.ts) only runs fetchStatus once in a mount useEffect (line 66-68); the only re-fetch is the manual refresh button. More importantly, onComplete only re-fetches the PROJECT via getProject; nothing mutates processStatus INSTALLING -> WAITING_CONNECTION_TEST. The advance depends entirely on the mock getProject returning an already-advanced processStatus, which it does not (mock processStatus for 2009 is static INSTALLING). For 2009 the API already returns allCompleted=true on mount, so onComplete/refreshProject fires immediately, but getProject returns the same INSTALLING status -> the step never actually advances. Net: refetch is correctly wired (refreshProject -> getProject -> onProjectUpdate), but no real transition occurs because (a) no poll loop and (b) no server-side/mock processStatus progression.

## gcp / step5 (연결 테스트) / targetSourceId=2010
- trigger: v15 = '완료 승인 요청' btn.primary (setStep(6)). Impl has NO such button; the only step-5 action is '연결 테스트 수행' (ConnectionTestPanel) which triggers a test-connection job, not a step advance. | advanceWorks: False | refetch: False
- issue: Two-part gap. (1) No advance button: v15's footer '완료 승인 요청' (setStep 6) is absent — the user has no explicit control to move step5→step6. (2) Implicit advance is not reflected in UI: mock-test-connection.ts:197-210 DOES flip project.processStatus to CONNECTION_VERIFIED when the test job resolves SUCCESS, but the UI never refetches the project. useTestConnectionPolling (app/hooks/useTestConnectionPolling.ts) polls ONLY the test-connection job, and ConnectionTestPanel calls onResourceUpdate (refreshProject) only on the credential-completion path (line 117), NOT when the job reaches SUCCESS. ProcessStatusCard (app/components/features/ProcessStatusCard.tsx:36-37) polls process-status only for WAITING_APPROVAL/APPLYING_APPROVED, not WAITING_CONNECTION_TEST. Net: after a successful connection test the backend mock is at step6 but the page stays rendering step5 until a manual reload — the stepper does not advance and project is not refetched. Fix: call refreshProject when uiState transitions to SUCCESS (ConnectionTestPanel) and/or extend ProcessStatusCard polling to WAITING_CONNECTION_TEST; restore an explicit '완료 승인 요청' advance button to match v15.

## gcp / step 6 (완료 여부 관리자 승인 대기, CONNECTION_VERIFIED
- trigger: auto-poll (final admin approval) — NO user button advances CONNECTION_VERIFIED→INSTALLATION_COMPLETE; the only button '연결 테스트 재실행' is a non-advancing stub. v15 button calls openConfirmStep('retest'). | advanceWorks: False | refetch: None
- issue: ConnectionVerifiedStep has no handler to advance to step 7. ConnectionVerifiedRetestButton (line 29-43) only fires toast.info('연결 테스트 재실행 기능 준비중입니다.') — it neither re-runs the connection test nor advances/refetches processStatus. Advancement is expected from external admin final-approval polling, but /process-status for 2011 returns process_status:'CONNECTED' (stays step 6) with no mock path to flip to INSTALLATION_COMPLETE, so the step is terminal in this mock. v15's retest action is also unwired in impl.

## provider=gcp · step=7 (연동 완료 / INSTALLATION_COMPLE
- trigger: none (terminal step — INSTALLATION_COMPLETE is the final state; the two card actions '인프라 변경' and '연결 테스트 재실행' are re-process triggers, not advance-to-next-step) | advanceWorks: None | refetch: None
- issue: Step 7 is terminal so there is no next step to advance to — N/A. However both card actions are non-functional stubs: InstallationCompleteActions (InstallationCompleteStep.tsx:39-62) wires both buttons to toast.info('... 기능 준비중입니다.'). v15 wires them to openConfirmStep('infra') / openConfirmStep('retest') (v15 HTML lines 7066, 7070) which open the re-confirm flow. So '인프라 변경' (should restart the process back to step 1/target-confirm) and '연결 테스트 재실행' (should kick a new connection test) do nothing in the impl.

## aws / step 1 (연동 대상 선택·입력) / targetSourceId=1006
- trigger: '연동 대상 승인 요청' primary button -> opens ApprovalRequestModal -> modal submit calls handleApprovalSubmit -> approval.mutate -> createApprovalRequest(targetSourceId) | advanceWorks: True | refetch: True
- issue: Works. CandidateResourceSection.tsx:105-124 — after createApprovalRequest the mutation awaits refreshProject() (line 114), which calls getProject(targetSourceId) and onProjectUpdate (WaitingTargetConfirmationStep.tsx:38-41), so the page re-renders the new processStatus. The mock (lib/bff/mock/confirm.ts createApprovalRequest) computes getCurrentStep and updateProject -> processStatus moves to WAITING_APPROVAL (step 2), or AUTO_APPROVED->APPLYING when policy matches (1006 description notes auto-approval test). NOTE: advancing is gated behind opening+submitting the modal (two clicks), not a single button as in v15. The ScanController onScanComplete also refetches. No TODO/no-op here (unlike the IDC step1 IdcStep1TargetInput suspect — that is a different provider/component and does not apply to AWS).

## aws / step 2 (연동 대상 승인 대기) / targetSourceId 1007
- trigger: auto-poll (ProcessStatusCard polls getProcessStatus every TIMINGS.PROCESS_STATUS_POLL_MS; admin approval externally flips BFF status PENDING→CONFIRMING, advancing WAITING_APPROVAL→APPLYING_APPROVED). No user button advances step 2; the only button is '연동 대상 승인 요청 취소' which goes BACK to step 1. | advanceWorks: True | refetch: True
- issue: Wiring is correct: ProcessStatusCard.tsx:53-67 polls getProcessStatus and on divergence calls getProject + onProjectUpdate (refetch + advance). Cancel button (WaitingApprovalCancelButton.tsx:28-33) calls cancelApprovalRequest then refreshProject. HOWEVER for THIS targetSourceId 1007 the mock state is approval=REJECTED, and computeProcessStatus (lib/bff/mock/confirm.ts:108-110) pins REJECTED/UNAVAILABLE at WAITING_APPROVAL permanently — so 1007 can never auto-advance to step 3 (terminal rejected state). That is a data-state choice, not a wiring bug, but it means this exact screen never demonstrates the forward transition. RejectionAlert is correctly shown (mock-data 1007 isRejected:true) — but note v15 data-stepc=2 for aws shows the normal 승인 대기 happy-path with a populated table, NOT a rejected state, so 1007 is the wrong fixture to back the v15 step-2 design.

## aws / step 3 (반영 중 · APPLYING_APPROVED) / targetSo
- trigger: auto-poll (no button). ProcessStatusCard.tsx:35-67 polls getProcessStatus every TIMINGS.PROCESS_STATUS_POLL_MS while currentStep===APPLYING_APPROVED, expecting BFF 'CONFIRMING'; when process_status differs it calls getProject and onProjectUpdate. | advanceWorks: False | refetch: True
- issue: Refetch wiring is correct (poll → getProcessStatus → on change getProject + onProjectUpdate). BUT the mock never transitions, so it never fires for this fixture. lib/bff/mock/confirm.ts getProcessStatus (lines 952-961) only advances APPLYING_APPROVED→INSTALLING when isApprovedStatus (approval.status === 'APPROVED'|'AUTO_APPROVED') AND installation==='PENDING' AND approvedAt !== undefined. The seeded 2001 fails ALL of approval/approvedAt: createStatusForProcessStatus in lib/mock-data.ts:34-89 has NO case for APPLYING_APPROVED, so it hits default→base = createInitialProjectStatus() with approval.status='PENDING' and no approvedAt; approvalTimestampStore also has no entry. Therefore isApprovedStatus=false AND approvedAt=undefined → guard never passes → process-status stays 'CONFIRMING' forever → poll never sees a change → step 3 is a permanent dead-end (cannot reach step 4). Same class of bug as the IDC step1 handleSubmit TODO. FIX: add an APPLYING_APPROVED case to createStatusForProcessStatus that sets approval={status:'APPROVED', approvedAt}, and/or seed approvalTimestampStore + approvedIntegrationStore for the demo applying fixtures.

## aws / step 4 (Agent 설치) / targetSourceId=1008
- trigger: auto (no button). On mount AwsInstallationInline fetches getAwsInstallationStatus; when isFullyCompleted(status) -> onComplete -> onInstallComplete -> refreshProject (getProject) re-reads processStatus, advancing INSTALLING -> WAITING_CONNECTION_TEST. | advanceWorks: True | refetch: True
- issue: Wiring is correct (onComplete -> refreshProject -> getProject re-reads processStatus) and DOES refetch the project. BUT useInstallationStatus.ts fetches ONLY once on mount (no polling interval); the manual refresh()/checkAwsInstallation path is never invoked automatically. For 1008 (bdcTf IN_PROGRESS, not fully completed) the step never advances on its own without a manual page reload — there is no auto-poll to detect later completion. Functionally not broken for an already-complete status, but unlike a polling step the in-progress -> complete transition is not observed live.

## aws / step 5 (연결 테스트) / targetSourceId=1010
- trigger: v15: '완료 승인 요청' primary button in the unified card footer → setStep(6). impl: implicit — successful connection test should auto-advance processStatus WAITING_CONNECTION_TEST → CONNECTION_VERIFIED (mock-test-connection.ts:210 does set project.processStatus via getCurrentStep on SUCCESS) | advanceWorks: False | refetch: False
- issue: BROKEN advancement wiring. (1) ProcessStatusCard.tsx:36-38 only polls process-status when currentStep is WAITING_APPROVAL or APPLYING_APPROVED — it does NOT poll during WAITING_CONNECTION_TEST, so when the server advances processStatus to CONNECTION_VERIFIED after a successful test the page never refetches the project. (2) useTestConnectionPolling.ts updates only its local job state and never notifies the parent; ConnectionTestPanel.onResourceUpdate/refreshProject is called only in handleCredentialComplete (ConnectionTestPanel.tsx:117), NOT when the test poll resolves SUCCESS. (3) There is no '완료 승인 요청' button at all. Net: after a passing connection test the UI stays stuck on step 5 until a manual full-page reload. Fix: on useTestConnectionPolling SUCCESS, call refreshProject (the callback already exists in WaitingConnectionTestStep.tsx:38 but is only wired to credential completion), or extend ProcessStatusCard polling to cover WAITING_CONNECTION_TEST.

## aws / step 6 (완료 여부 관리자 승인 대기) / targetSourceId=10
- trigger: admin completion-approval (CONNECTION_VERIFIED → INSTALLATION_COMPLETE) — external/auto; the only on-screen button is '연결 테스트 재실행' which is a re-test, NOT the advance trigger (it just fires toast.info('연결 테스트 재실행 기능 준비중입니다.')) | advanceWorks: False | refetch: False
- issue: ProcessStatusCard polling (app/components/features/ProcessStatusCard.tsx:36-38) only runs shouldPoll for WAITING_APPROVAL and APPLYING_APPROVED. For CONNECTION_VERIFIED (step 6) shouldPoll=false, so there is NO poll/refetch of process-status or project. Even if the backend grants final completion approval, the screen never auto-advances to step 7 nor refetches — requires a manual reload. The retest button (ConnectionVerifiedStep.tsx:33-40) only toasts 'preparing', never advances and never refetches.

## aws / step 7 (연동 완료) / targetSourceId=1012
- trigger: none — step 7 (INSTALLATION_COMPLETE) is the terminal step. The two on-card buttons (인프라 변경 / 연결 테스트 재실행) are the only actions; in v15 they re-enter the process (openConfirmStep('infra'/'retest')). | advanceWorks: None | refetch: None
- issue: No forward transition expected (terminal step). BUT both action buttons are non-functional stubs: InstallationCompleteStep.tsx:39-61 InstallationCompleteActions wires both onClick to toast.info('...기능 준비중입니다.') — '인프라 변경' does NOT start a new approval/infra-change flow and '연결 테스트 재실행' does NOT trigger a connection test. In v15 these open the confirm-step modal to re-run the pipeline. So the re-entry transition is missing/stubbed. processStatus refetch is moot since nothing advances.

## idc / step 1 — 연동 대상 DB 입력 (manual target input)
- trigger: '연동 대상 승인 요청' primary button → opens IdcSubmitModal → modal confirm calls handleSubmit (IdcStep1TargetInput.tsx:194) | advanceWorks: False | refetch: True
- issue: handleSubmit (L194-207) calls updateIdcResources(targetSourceId, rows) then refreshProject() (which DOES refetch the project via getProject), but a TODO at L199-200 means it NEVER advances processStatus to WAITING_APPROVAL: 'TODO(integration): advance processStatus → WAITING_APPROVAL via the shared approval flow once the IDC approval transition is wired in mock.' Because status stays WAITING_TARGET_CONFIRMATION, the refetch returns the same step and the user is stuck on step1. Also note no IDC-specific approval-request mutation is wired (unlike the cloud WaitingApprovalStep flow). Process-status is never re-fetched separately (only the project), but that is moot since nothing transitions.

## idc / step 2 (승인 대기) / targetSourceId=1021
- trigger: auto-poll (admin-side approval). ProcessStatusCard polls getProcessStatus while processStatus===WAITING_APPROVAL; the only user button '전체 요청 취소'/'연동 대상 승인 요청 취소' goes BACKWARD to step1. | advanceWorks: True | refetch: True
- issue: Advance is correctly wired (unlike the known step1 IdcStep1TargetInput TODO). ProcessStatusCard.tsx:53-59 polls getProcessStatus; when process_status !== 'PENDING' it refetches getProject and calls onProjectUpdate, which re-renders IdcTargetSourceLayout into IdcStep3Applying. Cancel path (WaitingApprovalCancelButton.handleConfirm) calls cancelApprovalRequest then onSuccess → getProject refetch. Both transitions refetch project.

## IDC · step 3 (반영 중 / APPLYING_APPROVED) · targetSo
- trigger: auto-poll (ProcessStatusCard useEffect polls getProcessStatus every TIMINGS.PROCESS_STATUS_POLL_MS while processStatus===APPLYING_APPROVED). v15 prototype uses a manual 'Next →' button → setStep(4); impl uses auto-poll, which is the production-correct behavior. | advanceWorks: True | refetch: True
- issue: Correctly wired: when BFF process_status leaves 'CONFIRMING', poll() calls getProject(targetSourceId) and onProjectUpdate(updated), re-rendering into IdcStep4Installing (ProcessStatusCard.tsx:53-59). This is NOT the IdcStep1 TODO suspect — step 3 advance + refetch both fire. Only caveat: actual advance depends on the mock backend flipping 1022 off CONFIRMING; the client-side transition logic itself is sound. v15's visible Next button is absent by design.

## idc / step 4 (Agent 설치 / INSTALLING) / targetSourc
- trigger: auto (install tasks complete → should advance INSTALLING → WAITING_CONNECTION_TEST). No explicit advance button in v15 or impl. | advanceWorks: False | refetch: False
- issue: Step 4 (INSTALLING) NEVER advances to step 5. ProcessStatusCard's poll loop (ProcessStatusCard.tsx:35-74) only runs when processStatus is WAITING_APPROVAL or APPLYING_APPROVED (shouldPoll, lines 36-38); INSTALLING is excluded, so no project re-poll/refetch happens here. IdcStep4Installing only drives the install-task pipeline off useIdcInstallationStatus, which fetches/refreshes the IDC *installation status* (bdc_tf / firewall_opened / per-resource) but NEVER fetches the project's processStatus and NEVER calls onProjectUpdate. The manual '설치 상태 새로고침' button calls useIdcInstallationStatus.refresh() (check-installation) only — it updates pill state, not processStatus. Net: even after both install tasks reach done/firewall-open, the step cannot transition to WAITING_CONNECTION_TEST. Same wiring gap class as the known IDC step1 (handleSubmit does-not-advance) suspect. Fix: have step 4 poll getProcessStatus and, on transition past INSTALLING, getProject(...) + onProjectUpdate(...) — or extend ProcessStatusCard.shouldPoll to include INSTALLING.

## idc / step5 (연결 테스트) / targetSourceId=1024
- trigger: 완료 승인 요청 button (ApproveRequestButton in IdcStep5ConnectionTest.tsx:30-41) | advanceWorks: False | refetch: False
- issue: 완료 승인 요청 is a pure toast stub: onClick={() => toast.info('완료 승인 요청 기능 준비중입니다.')}. It does NOT mutate processStatus from WAITING_CONNECTION_TEST → CONNECTION_VERIFIED, does NOT call onProjectUpdate, and does NOT refetch project/process-status. Same unimplemented-advance pattern as the known IdcStep1 TODO. onProjectUpdate is only wired into ProcessStatusCard (the stepper bar), never to this step's CTA. Separately, 'Run Test' (runTest, lines 95-112) is a local 1.8s simulation that optimistically flips every non-excluded row's connection to SUCCESS in component state only — purely cosmetic, no server/processStatus effect. Net: this step cannot advance to step 6 in the running app.

## IDC · Step 6 (완료 여부 관리자 승인 대기 / CONNECTION_VERIFIE
- trigger: none (admin-gated). On-screen button '연결 테스트 재실행' is an intentional toast stub (toast.info('연결 테스트 재실행 기능 준비중입니다.')) and does not advance. Advancement to Step 7 (INSTALLATION_COMPLETE) happens only via PII-Agent ops final approval server-side. | advanceWorks: False | refetch: False
- issue: ProcessStatusCard.useEffect only polls when processStatus === WAITING_APPROVAL or APPLYING_APPROVED (ProcessStatusCard.tsx:36-38). CONNECTION_VERIFIED is NOT in the poll set, so once admin grants final approval the UI never re-fetches process-status/project and stays stuck on Step 6 until a manual page reload — it will not auto-advance to Step 7. This mirrors the WAITING_APPROVAL poll pattern that is missing here. (Separate from the known IDC Step1 handleSubmit TODO — this is a Step6 polling gap.) The v15 prototype's Step6 retest button also only opens a confirm modal, so no on-screen advance is expected; the gap is purely the missing CONNECTION_VERIFIED poll-to-refetch.

## IDC · step 7 (연동 완료 / INSTALLATION_COMPLETE) · tar
- trigger: none — step 7 (INSTALLATION_COMPLETE) is the terminal step; no next step exists. The only actions are 인프라 변경 / 연결 테스트 재실행, which are intentional toast stubs (CompleteActions in IdcStep7Complete.tsx:19-35, mirroring cloud siblings; v15 wires them to openConfirmStep('infra'/'retest') modal stubs too). | advanceWorks: None | refetch: None
- issue: No transition expected from a terminal step, so no advance bug here. The 인프라 변경/연결 테스트 재실행 buttons being toast-only matches the v15 prototype's stub behavior — acceptable. (Unlike IDC step1 IdcStep1TargetInput which has the known no-advance TODO, step7 has no advance obligation.)
