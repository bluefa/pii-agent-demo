# AWS/Azure 설치 상태 UX 리디자인 v2

> 2026-02-09 작성
> Issue #50 — 설치 상태 UI/UX 전체 개선

---

## 1. 설계 원칙

1. **간단하게 보여주고, 원하면 더 많은 정보** — 기본 뷰는 요약, 확장으로 상세
2. **내부 용어 은닉** — TF, BDC, Service TF, PE, Subnet 등 인프라 용어 제거
3. **조치 중심** — 사용자가 해야 할 일이 있으면 명확히, 없으면 안심 문구
4. **프로세스 현실 반영** — Linear하지 않은 프로세스를 억지로 단계로 나누지 않음
5. **각 단계의 실체 설명** — "무엇이 설치되는지" 사용자가 이해할 수 있도록

---

## 2. 각 Step이 실제로 하는 일 (도메인 정리)

### AWS 설치 프로세스

```
자동 모드 (hasTfPermission = true):
  시스템이 자동으로 2개 작업을 순차 실행
  ┌─ Service TF ─────────────────────────────────────────┐
  │ AWS 계정에 VPC Endpoint, IAM Role 등                  │
  │ 네트워크/권한 리소스를 생성                            │
  │ → "AWS 환경에 보안 연결 통로 구축"                     │
  └──────────────────────────────────────────────────────┘
  ┌─ BDC TF ─────────────────────────────────────────────┐
  │ 에이전트 측에서 DB 접근 설정 수행                      │
  │ → "에이전트가 DB에 접근할 수 있도록 설정"               │
  └──────────────────────────────────────────────────────┘

수동 모드 (hasTfPermission = false):
  사용자가 스크립트를 다운로드하여 직접 실행
  → 위와 동일한 인프라가 생성되지만, 담당자가 실행
```

**핵심**: 사용자 관점에서는 자동이든 수동이든 **"설치"라는 하나의 과정**. 내부의 2단계 분리는 사용자에게 불필요한 정보.

### Azure 설치 프로세스

```
DB 리소스 (비 VM):
  시스템이 자동 실행 → 사용자가 승인만
  ┌─ TF 설치 (자동) ─────────────────────────────────────┐
  │ DB에 Private Endpoint(보안 연결 통로) 요청              │
  └──────────────────────────────────────────────────────┘
  ┌─ PE 승인 (사용자 액션) ──────────────────────────────┐
  │ Azure Portal에서 연결 요청을 승인                      │
  │ → "보안 연결 승인"                                     │
  └──────────────────────────────────────────────────────┘

VM 리소스:
  3단계지만, 사용자 조치가 필요한 부분만 노출
  ┌─ 1. Subnet 확인 (조건부 사용자 액션) ────────────────┐
  │ VM 전용 네트워크(Subnet)가 필요                        │
  │ 없으면 → 서비스 담당자가 직접 생성 (가이드 제공)         │
  │ → "네트워크 설정"                                      │
  └──────────────────────────────────────────────────────┘
  ┌─ 2. VM TF 설치 (사용자 액션) ────────────────────────┐
  │ VM에 에이전트 실행 환경 구성 스크립트 실행              │
  │ → 서비스 담당자가 스크립트 다운로드 후 직접 실행         │
  │ → "VM 환경 설정"                                      │
  └──────────────────────────────────────────────────────┘
  ┌─ 3. PE 승인 (사용자 액션) ──────────────────────────┐
  │ DB와 동일하게 Azure Portal에서 연결 승인                │
  │ → "연결 승인"                                          │
  └──────────────────────────────────────────────────────┘
```

**핵심**: Azure는 리소스마다 다른 경로로 **병렬** 진행됨. DB와 VM이 동시에 각자의 단계를 밟음. "리소스 생성 → 에이전트 연동"이라는 2단계 모델은 맞지 않음.

### GCP 설치 프로세스 (참고 — 이미 구현 완료)

```
리소스별로:
  ┌─ Service TF ─────────────────────────────────────────┐
  │ GCP 프로젝트에 네트워크/IAM 리소스 생성                 │
  │ - Private IP: PSC NEG, Backend Service, Proxy 등       │
  │ - BigQuery: IAM 바인딩                                 │
  │ → "GCP 리소스 생성"                                    │
  └──────────────────────────────────────────────────────┘
  ┌─ BDC TF ─────────────────────────────────────────────┐
  │ 에이전트 연결 설정                                      │
  │ → "에이전트 연동"                                       │
  └──────────────────────────────────────────────────────┘
  + 조건부: Regional Managed Proxy → "네트워크 설정"
  + 조건부: PSC 승인 → "연결 승인"
```

GCP는 리소스별로 serviceTf/bdcTf 2단계가 있고, 모든 리소스가 동일 경로 → 그룹 집계가 의미 있음.

---

## 3. 용어 변환 테이블

### 공통 용어

| 현재 (내부) | 변경 (사용자) | 설명 |
|------------|-------------|------|
| Service TF | _(노출하지 않음)_ | AWS/Azure에서는 별도 표시 안 함 |
| BDC TF | _(노출하지 않음)_ | AWS/Azure에서는 별도 표시 안 함 |
| TF Script | 설치 스크립트 | 사용자가 다운받아 실행하는 파일 |
| Terraform | _(노출하지 않음)_ | 도구명은 사용자에게 불필요 |

### AWS 용어

| 현재 (내부) | 변경 (사용자) | 맥락 |
|------------|-------------|------|
| hasTfPermission = true | 자동 설치 모드 | 시스템이 자동으로 환경 구성 |
| hasTfPermission = false | 수동 설치 모드 | 스크립트를 받아 직접 실행 |
| TerraformExecutionRole | 자동 설치 권한 | AWS 계정에 등록하는 Role |
| Role 등록 가이드 | 권한 설정 가이드 | Role 생성/등록 안내 |
| TF Script 다운로드 | 설치 스크립트 다운로드 | 수동 설치용 파일 |

### Azure 용어

| 현재 (내부) | 변경 (사용자) | 맥락 |
|------------|-------------|------|
| Subnet 설정 필요 | 네트워크 설정 필요 | VM 전용 서브넷 구성 |
| VM TF 설치 필요 | VM 환경 설정 필요 | VM에 에이전트 환경 구성 |
| PE 승인요청 | 연결 승인 요청 | 보안 연결 통로 승인 |
| PE 승인대기 | 연결 승인 대기 | Azure Portal에서 승인 대기 |
| PE 거부 | 연결 거부됨 | 재승인 필요 |
| BDC측 PE 승인요청 필요 | 연결 요청 준비 중 | 시스템이 요청을 보내기 전 |
| Private Endpoint | 보안 연결 | DB/VM에 대한 전용 연결 |

---

## 4. 색상 매핑 (theme.ts statusColors)

| UI 요소 | 상태 | 색상 토큰 |
|---------|------|----------|
| 메인 카드 | 진행 중 | `statusColors.warning` (bg, border, textDark) |
| 메인 카드 | 완료 | `statusColors.success` |
| 메인 카드 | 에러 포함 | `statusColors.error` |
| 프로그레스 바 | 진행 중 | `statusColors.warning.dot` |
| 프로그레스 바 | 완료 | `statusColors.success.dot` |
| 프로그레스 바 | 에러 | `statusColors.error.dot` |
| 조치 카드 | — | `statusColors.warning` (bg, border) |
| 오류 카드 | — | `statusColors.error` (bg, border) |
| 설치 모드 카드 | 자동 | `statusColors.info` (bg, border) |
| 설치 모드 카드 | 수동 | `statusColors.pending` (bg, border) |
| 로딩 카드 | — | `statusColors.pending` (bg, border) |

---

## 5. AWS 설치 상태 리디자인

### 5-1. 설계 방향

AWS는 사용자 관점에서 **"설치"라는 하나의 과정**입니다.
- 내부적으로 Service TF → BDC TF 2단계지만, 사용자에게는 불필요한 정보
- "2/2 완료" 대신 **진행률 바 하나**로 표현
- 자동/수동 모드 차이만 별도 카드로 안내

### 5-2. AWS 상태 매핑

| serviceTf | bdcTf | 진행률 | 상태 텍스트 |
|-----------|-------|--------|-----------|
| false | false | 0% | 설치 준비 중 |
| (진행 중) | false | ~30% | 설치 진행 중 |
| true | false | 50% | 설치 진행 중 |
| true | (진행 중) | ~80% | 설치 진행 중 |
| true | true | 100% | 설치 완료 |

> 현재 API는 boolean만 제공 (completed or not). 진행 중 표현은 `!serviceTfCompleted && hasTfPermission` → "진행 중"으로 추정.

### 5-3. AWS 와이어프레임

**— 1. 자동 설치: 진행 중 —**

```
┌──────────────────────────────────────────────────────┐
│  ◐ AWS 에이전트 설치 진행 중                        🔄 │  ← warning.bg/border
│  ████████████████░░░░░░░░░░░░░░░░░░░░  50%            │
│                                                       │
│  자동으로 설치가 진행되고 있습니다.                     │
│  AWS 계정에 보안 연결 환경을 구성하고                    │
│  에이전트가 DB에 접근할 수 있도록 설정합니다.            │
└──────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────┐  ← info.bg/border
│  ⚡ 자동 설치 모드                  권한 설정 가이드 → │
│  시스템이 자동으로 설치를 실행합니다.                   │
└──────────────────────────────────────────────────────┘
```

**— 2. 자동 설치: 초기 (0%) —**

```
┌──────────────────────────────────────────────────────┐
│  ◐ AWS 에이전트 설치 준비 중                        🔄 │  ← warning.bg/border
│  ░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░  0%            │
│                                                       │
│  자동으로 설치가 진행될 예정입니다.                     │
└──────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────┐  ← info.bg/border
│  ⚡ 자동 설치 모드                  권한 설정 가이드 → │
│  시스템이 자동으로 설치를 실행합니다.                   │
└──────────────────────────────────────────────────────┘
```

**— 3. 수동 설치: 스크립트 실행 전 —**

```
┌──────────────────────────────────────────────────────┐
│  ○ AWS 에이전트 설치 대기 중                        🔄 │  ← pending.bg/border
│  ░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░  0%            │
└──────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────┐  ← pending.bg/border
│  📋 수동 설치 모드                                    │
│  설치 스크립트를 다운로드하여 직접 실행해주세요.       │
│                                                       │
│  [설치 스크립트 다운로드]        설치 가이드 보기 →    │
└──────────────────────────────────────────────────────┘

┌─ ⚠ 조치 필요 ────────────────────────────────────────┐  ← warning.bg/border
│  설치 스크립트를 담당자와 함께 실행해주세요.           │
│  스크립트는 AWS 계정에 보안 연결 환경을 구성하고       │
│  에이전트 접근 설정을 수행합니다.                      │
│                                                       │
│  [설치 스크립트 다운로드]        설치 가이드 보기 →    │
└──────────────────────────────────────────────────────┘
```

**— 4. 수동 설치: 실행 후 진행 중 —**

```
┌──────────────────────────────────────────────────────┐
│  ◐ AWS 에이전트 설치 진행 중                        🔄 │  ← warning.bg/border
│  ████████████████████████░░░░░░░░░░░░  50%            │
│                                                       │
│  설치 스크립트가 실행되었습니다. 자동으로 완료됩니다.   │
└──────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────┐  ← pending.bg/border
│  📋 수동 설치 모드                  설치 가이드 보기 → │
│  설치 스크립트가 실행되었습니다.                       │
└──────────────────────────────────────────────────────┘
```

**— 5. 전체 완료 —**

```
┌──────────────────────────────────────────────────────┐
│  ● AWS 에이전트 설치 완료                             │  ← success.bg/border
│  ████████████████████████████████████████  100%       │
└──────────────────────────────────────────────────────┘
```

**— 6. 로딩 / 에러 —**

```
로딩: ┌──────────────────────────────────────────────┐
      │  ◐ AWS 설치 상태 확인 중...                  │  ← pending.bg/border
      └──────────────────────────────────────────────┘

에러: ┌──────────────────────────────────────────────┐
      │  상태 조회에 실패했습니다.       [다시 시도]  │  ← error.bg/border
      └──────────────────────────────────────────────┘
```

### 5-4. AWS 변경 포인트

| 항목 | Before | After |
|------|--------|-------|
| 진행 표시 | "Service TF / BDC TF" 2행 | 단일 진행률 바 |
| 완료 카운트 | "1/2 완료" | 진행률 % |
| 설치 설명 | 없음 | "보안 연결 환경 구성 + 에이전트 접근 설정" |
| 설치 모드 | 카드 내부 영역 | 별도 모드 안내 카드 |
| 색상 | raw 클래스 | statusColors 토큰 |
| "Terraform" | 직접 노출 | 노출하지 않음 |
| "TF Script" | 직접 노출 | "설치 스크립트" |

---

## 6. Azure 설치 상태 리디자인

### 6-1. 설계 방향

Azure는 **리소스마다 다른 경로로 병렬 진행**됩니다.
- DB: (자동) TF → PE 승인요청 → 사용자가 승인
- VM: Subnet 확인 → TF Script 실행 → PE 승인
- "리소스 생성 / 에이전트 연동" 2줄로 표현하면 실제 프로세스와 맞지 않음

따라서 Azure는:
- **기본 뷰**: 전체 진행률 + 요약 (N건 완료, M건 조치 필요)
- **확장 뷰** (접기/펼치기): 리소스별 현재 단계를 자연어로 표시
- **조치 카드**: 사용자 액션이 필요한 리소스만 별도 카드

### 6-2. Azure 리소스별 상태 매핑

#### DB 리소스

| 조건 | 통합 상태 | 사용자 라벨 | 설명 |
|------|----------|-----------|------|
| PE = NOT_REQUESTED | `PENDING` | 연결 요청 준비 중 | 시스템이 보안 연결을 요청하기 전 |
| PE = PENDING_APPROVAL | `ACTION_REQUIRED` | 연결 승인 대기 | Azure Portal에서 승인 필요 |
| PE = REJECTED | `ACTION_REQUIRED` | 연결 거부됨 | 재승인 필요 |
| PE = APPROVED | `COMPLETED` | 완료 | — |

#### VM 리소스

| 조건 | 통합 상태 | 사용자 라벨 | 설명 |
|------|----------|-----------|------|
| subnet = false | `ACTION_REQUIRED` | 네트워크 설정 필요 | VM 전용 네트워크 구성 필요 |
| subnet = true, tf = false | `ACTION_REQUIRED` | VM 환경 설정 필요 | 스크립트 실행 필요 |
| tf = true, PE = NOT_REQUESTED | `PENDING` | 연결 요청 준비 중 | 시스템이 보안 연결 요청 전 |
| tf = true, PE = PENDING_APPROVAL | `ACTION_REQUIRED` | 연결 승인 대기 | Azure Portal에서 승인 필요 |
| tf = true, PE = REJECTED | `ACTION_REQUIRED` | 연결 거부됨 | 재승인 필요 |
| tf = true, PE = APPROVED | `COMPLETED` | 완료 | — |

### 6-3. Azure 와이어프레임

**— 1. 기본 뷰: 진행 중 —**

```
┌──────────────────────────────────────────────────────┐
│  ◐ Azure 에이전트 설치 중... (2/6 완료)             🔄 │  ← warning.bg/border
│  ██████████░░░░░░░░░░░░░░░░░░░░░░░░░░  33%            │
│                                                       │
│  자동으로 설치가 진행 중입니다.                        │
│  각 리소스에 보안 연결을 설정하고 있습니다.             │
│                                                       │
│  ▸ 리소스별 상태 보기 (6건)                           │
└──────────────────────────────────────────────────────┘
```

**— 2. 확장 뷰: 리소스별 상세 (펼친 상태) —**

```
┌──────────────────────────────────────────────────────┐
│  ◐ Azure 에이전트 설치 중... (2/6 완료)             🔄 │
│  ██████████░░░░░░░░░░░░░░░░░░░░░░░░░░  33%            │
│                                                       │
│  ▾ 리소스별 상태 보기 (6건)                           │
│  ┌────────────────────────────────────────────────┐  │
│  │  VM                                            │  │
│  │  🖥 vm-webapp-01          ● 완료               │  │
│  │  🖥 vm-webapp-02          ⚠ 네트워크 설정 필요  │  │
│  │                                                │  │
│  │  DB                                            │  │
│  │  🗄 mssql-primary         ● 완료               │  │
│  │  🗄 postgresql-analytics  ⚠ 연결 승인 대기      │  │
│  │  🗄 mysql-reporting       ○ 연결 요청 준비 중   │  │
│  │  🗄 cosmosdb-catalog      ○ 연결 요청 준비 중   │  │
│  └────────────────────────────────────────────────┘  │
└──────────────────────────────────────────────────────┘
```

**— 3. 조치 필요: 네트워크 설정 (VM) —**

```
┌──────────────────────────────────────────────────────┐
│  ◐ Azure 에이전트 설치 중... (1/6 완료)             🔄 │  ← warning.bg/border
│  ████░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░  17%            │
│                                                       │
│  ▸ 리소스별 상태 보기 (6건)                           │
└──────────────────────────────────────────────────────┘

┌─ ⚠ 조치 필요 ────────────────────────────────────────┐  ← warning.bg/border
│  🖥 vm-webapp-02                                      │
│  네트워크 설정이 필요합니다                            │
│                                                       │
│  VM을 연동하려면 전용 네트워크(서브넷)가 필요합니다.    │
│  서비스 담당자가 Azure Portal에서 직접 생성해주세요.    │
│                                                       │
│  [네트워크 설정 가이드]                               │
└──────────────────────────────────────────────────────┘
```

**— 4. 조치 필요: VM 환경 설정 (스크립트 실행) —**

```
┌──────────────────────────────────────────────────────┐
│  ◐ Azure 에이전트 설치 중... (1/6 완료)             🔄 │
│  ████░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░  17%            │
│                                                       │
│  ▸ 리소스별 상태 보기 (6건)                           │
└──────────────────────────────────────────────────────┘

┌─ ⚠ 조치 필요 ────────────────────────────────────────┐  ← warning.bg/border
│  VM 환경 설정이 필요합니다                            │
│  설치 스크립트를 다운로드하여 VM에서 실행해주세요.     │
│  스크립트는 VM에 에이전트 실행 환경을 구성합니다.      │
│                                                       │
│  [설치 스크립트 다운로드]                             │
└──────────────────────────────────────────────────────┘
```

**— 5. 조치 필요: 연결 승인 대기 —**

```
┌──────────────────────────────────────────────────────┐
│  ◐ Azure 에이전트 설치 중... (3/6 완료)             🔄 │
│  ██████████████████░░░░░░░░░░░░░░░░░░  50%            │
│                                                       │
│  ▸ 리소스별 상태 보기 (6건)                           │
└──────────────────────────────────────────────────────┘

┌─ ⚠ 조치 필요 ────────────────────────────────────────┐  ← warning.bg/border
│  연결 승인이 필요한 리소스가 2건 있습니다              │
│                                                       │
│  🗄 postgresql-analytics     승인 대기 중             │
│  🗄 mysql-reporting          승인 대기 중             │
│                                                       │
│  Azure Portal에서 보안 연결 요청을 승인해주세요.      │
│  [승인 방법 보기]                                     │
└──────────────────────────────────────────────────────┘
```

**— 6. 조치 필요: 연결 거부됨 —**

```
┌─ ✕ 연결 거부 ────────────────────────────────────────┐  ← error.bg/border
│  🗄 mysql-reporting                                   │
│  보안 연결이 거부되었습니다. 재승인이 필요합니다.      │
│                                                       │
│  Azure Portal에서 연결 요청을 다시 승인해주세요.      │
│  [승인 방법 보기]                                     │
└──────────────────────────────────────────────────────┘
```

**— 7. 복합 상황: 여러 조치 동시 —**

```
┌──────────────────────────────────────────────────────┐
│  ◐ Azure 에이전트 설치 중... (1/6 완료)             🔄 │
│  ████░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░  17%            │
│                                                       │
│  ▸ 리소스별 상태 보기 (6건)                           │
└──────────────────────────────────────────────────────┘

┌─ ⚠ 조치 필요 ────────────────────────────────────────┐
│  🖥 vm-webapp-02                                      │
│  네트워크 설정이 필요합니다                            │
│  [네트워크 설정 가이드]                               │
└──────────────────────────────────────────────────────┘

┌─ ⚠ 조치 필요 ────────────────────────────────────────┐
│  🗄 postgresql-analytics     연결 승인 대기 중        │
│  Azure Portal에서 보안 연결 요청을 승인해주세요.      │
│  [승인 방법 보기]                                     │
└──────────────────────────────────────────────────────┘
```

**— 8. 전체 완료 —**

```
┌──────────────────────────────────────────────────────┐
│  ● Azure 에이전트 설치 완료 (6/6)                     │  ← success.bg/border
│  ████████████████████████████████████████  100%       │
│                                                       │
│  ▸ 리소스별 상태 보기 (6건)                           │
└──────────────────────────────────────────────────────┘
```

**— 9. 로딩 / 에러 —**

```
로딩: ┌──────────────────────────────────────────────┐
      │  ◐ Azure 설치 상태 확인 중...               │  ← pending.bg/border
      └──────────────────────────────────────────────┘

에러: ┌──────────────────────────────────────────────┐
      │  상태 조회에 실패했습니다.       [다시 시도]  │  ← error.bg/border
      └──────────────────────────────────────────────┘
```

### 6-4. Azure 접기/펼치기 상세

**기본 상태**: 접힘

**리소스 목록 정렬**:
1. VM 그룹 → DB 그룹
2. 그룹 내: 조치 필요 → 진행 중 → 대기 중 → 완료

**5개 초과 시**: 그룹 내 "더보기" 버튼

```
│  DB (8)                                                │
│  🗄 postgresql-analytics     ⚠ 연결 승인 대기          │
│  🗄 mysql-reporting          ○ 연결 요청 준비 중       │
│  🗄 mssql-primary            ● 완료                   │
│  🗄 cosmosdb-catalog         ● 완료                   │
│  🗄 mysql-secondary          ● 완료                   │
│                                                        │
│  ▸ 나머지 3건 더보기                                   │
```

### 6-5. Azure 변경 포인트

| 항목 | Before | After |
|------|--------|-------|
| 기본 뷰 | 탭(전체/조치) + 리소스 목록 | 진행률 바 + 요약 + 접기/펼치기 |
| 상태 2줄 | _(없었음 — 리소스 나열)_ | 제거 (리소스별 병렬이라 의미 없음) |
| 탭 | 전체/조치 필요 2탭 | 제거 (조치는 별도 카드) |
| 페이지네이션 | 5건 단위 | 그룹 내 "더보기" |
| 단계 라벨 | STEP_INFO 하드코딩 (인프라 용어) | 자연어 라벨 |
| 설명 | 없음 | "보안 연결 설정" 등 실체 설명 |
| 색상 | raw 클래스 47회 | statusColors 토큰 |

---

## 7. 3개 프로바이더 UI 비교

| 기능 | GCP (구현 완료) | AWS (리디자인) | Azure (리디자인) |
|------|:---------------:|:--------------:|:----------------:|
| **진행률 바** | ✅ | ✅ | ✅ |
| **그룹 상태 2줄** | ✅ (리소스 생성/에이전트 연동) | ❌ (단일 진행률) | ❌ (리소스별 병렬) |
| **접기/펼치기** | ❌ | ❌ | ✅ |
| **설치 모드 카드** | ❌ | ✅ (자동/수동) | ❌ |
| **조치 카드** | ✅ | ✅ | ✅ |
| **리소스별 상세** | ❌ | ❌ | ✅ (접기/펼치기) |
| **안심 문구** | ✅ | ✅ | ✅ |
| **설체 설명** | ❌ | ✅ (v2 추가) | ✅ (v2 추가) |

---

## 8. 컴포넌트 구조

### 8-1. 공통 (이번 PR 범위)

GCP는 이번에 건드리지 않으므로, AWS/Azure 간 공유 가능한 최소 컴포넌트만 추출:

```
app/components/features/process-status/shared/
├── InstallationLoadingView.tsx    # 로딩 상태 카드 (공통)
├── InstallationErrorView.tsx      # API 에러 카드 (공통)
└── ActionCard.tsx                 # 조치 필요 카드 wrapper (공통)
```

> InstallationProgressCard, GroupStatusLine은 GCP 전환 시 추출. AWS/Azure는 구조가 달라서 무리하게 공통화하지 않음.

### 8-2. AWS

```
app/components/features/process-status/aws/
├── AwsInstallationInline.tsx      # 리팩터링 (단일 진행률 + 모드 카드)
├── AwsInstallModeCard.tsx         # 신규: 자동/수동 모드 안내
├── TfRoleGuideModal.tsx           # 기존 유지, 용어 변경
└── TfScriptGuideModal.tsx         # 기존 유지, 용어 변경
```

### 8-3. Azure

```
app/components/features/process-status/azure/
├── AzureInstallationInline.tsx    # 리팩터링 (진행률 + 접기/펼치기 + 조치 카드)
├── AzureResourceList.tsx          # 신규: 접기/펼치기 리소스 목록
├── AzureSubnetGuide.tsx           # 기존 경로에서 유지
└── AzurePeApprovalGuide.tsx       # 기존 인라인 모달을 개선
```

### 8-4. 파일 변경 요약

| 파일 | 변경 유형 | 주요 내용 |
|------|---------|----------|
| `shared/InstallationLoadingView.tsx` | 신규 | 공통 로딩 뷰 |
| `shared/InstallationErrorView.tsx` | 신규 | 공통 에러 뷰 |
| `shared/ActionCard.tsx` | 신규 | 공통 조치 카드 |
| `aws/AwsInstallationInline.tsx` | 리팩터링 | 2줄 상태 → 단일 진행률, statusColors |
| `aws/AwsInstallModeCard.tsx` | 신규 | 자동/수동 모드 안내 카드 |
| `aws/TfRoleGuideModal.tsx` | 수정 | 용어 변경 |
| `aws/TfScriptGuideModal.tsx` | 수정 | 용어 변경 |
| `azure/AzureInstallationInline.tsx` | 리팩터링 | 탭/페이지네이션 제거, 진행률+접기/펼치기 |
| `azure/AzureResourceList.tsx` | 신규 | VM/DB 그룹화 리소스 목록 |
| `azure/AzurePeApprovalGuide.tsx` | 리팩터링 | 용어 변경, Modal 개선 |

### 8-5. 구현 우선순위

1. **Phase 1**: 공통 컴포넌트 3개 + AWS/Azure 상수/라벨 정의
2. **Phase 2**: AWS 리팩터링 (가장 단순 — boolean 2개)
3. **Phase 3**: Azure 리팩터링 (접기/펼치기 + 리소스 목록)
4. _(후속)_ GCP 공통 컴포넌트 전환

---

## 9. 결정 필요 사항

1. **Azure PeApprovalGuide**: 기존 인라인 구현 → Modal UI 컴포넌트로 교체할지?
2. **Azure 리소스 "더보기" 기준**: 5개 유지?
3. **AWS 진행률 표현**: 현재 API가 boolean만 제공하므로, 0% → 50% → 100% 3단계 점프 허용?
