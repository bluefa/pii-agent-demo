1. `/Users/study/pii-agent-demo-single-pipeline-tick/design/pipeline/single-pipeline-tick-proposal.md`:4 — [MINOR] Stale/broken local references: both `design/pipeline/orchestrator-design.md` and `design/pipeline/minimal-redesign.md` are referenced as related docs, but neither file exists in this worktree under `design/pipeline/` — Remove these local references, restore the referenced docs, or point to the current canonical location.
2. `/Users/study/pii-agent-demo-single-pipeline-tick/design/pipeline/single-pipeline-tick-proposal.md`:12,66,180-203 — [MINOR] The prose says one worker owns a pipeline "from start to finish" and "nobody else can touch it", but the crash section correctly says an expired lease can be reclaimed and can cause duplicate external calls — Qualify the ownership claim as "while the lease is valid and not reclaimed"; keep the duplicate-call warning.
3. `/Users/study/pii-agent-demo-single-pipeline-tick/design/pipeline/single-pipeline-tick-proposal.md`:16,107,253 — [NIT] The TL;DR/V1 contract uses `=` for API-call concurrency, while the derivation correctly gives an upper bound `<= min(totalWorkerCount, runningPipelineCount)`; actual concurrency can be lower when fewer tasks are due or workers are sleeping — Change the `=` statements to `<=` or "upper bound".
4. `/Users/study/pii-agent-demo-single-pipeline-tick/design/pipeline/single-pipeline-tick-proposal.md`:17,118,237,255 — [MINOR] Terminology drift: `N` means worker count in the worker-pool discussion, then pod count in the overshoot formula. This makes `M+N-1` ambiguous — Use distinct symbols, e.g. `W = totalWorkerCount`, `P = activePodCount`, and write running-pipeline overshoot as `M + P - 1` under the stated "max 1 promotion per pod per race" premise.
5. `/Users/study/pii-agent-demo-single-pipeline-tick/design/pipeline/single-pipeline-tick-proposal.md`:133,237,256 — [BLOCKER] The `slotCap` overshoot is stated as bounded like pod-level `runningPipelineCap` overshoot, but slot admission happens in worker code (`tryAcquireSlot()`), so concurrent workers in the same pod can also race. Without per-pod serialization or CAS, worst-case slot overshoot is bounded by concurrent slot admitters, not by pods alone — State the bound as `slotCap + concurrentSlotAdmitters - 1` (or `slotCap + totalWorkerCount - 1` as a conservative bound), or require the counter-CAS for hard/known bounds.
6. `/Users/study/pii-agent-demo-single-pipeline-tick/design/pipeline/single-pipeline-tick-proposal.md`:140 — [MINOR] SQL correctness: `UPDATE tf_slot_counter SET used = used + 1 WHERE used < cap` only has the documented `rows=1` behavior if the table is physically guaranteed to contain exactly one row. The snippet does not show a singleton key/constraint, so multiple rows could be updated — Add an enforced singleton primary key and scope the statement, e.g. `WHERE name = 'terraform' AND used < cap RETURNING used`, or explicitly document the one-row invariant.
7. `/Users/study/pii-agent-demo-single-pipeline-tick/design/pipeline/single-pipeline-tick-proposal.md`:133,135 — [MINOR] Stale terminology: the proposal calls `slotCap` a "BFF-side" target and says BFF is the only caller, but the current direction calls for a separate server rather than BFF-owned orchestration — Replace "BFF-side/BFF caller" with the actual orchestration service/server name.
8. `/Users/study/pii-agent-demo-adr-016-pipeline-architecture/docs/adr/ADR-016-install-delete-pipeline-orchestration.md`:1,16,23,41,127 — [BLOCKER] Stale terminology/factual ownership: the ADR title and core decision place orchestration "in BFF", but the implementation direction is a separate server, not BFF — Retitle/reword the ADR to name the separate orchestration service and update BFF-specific requirement wording.
9. `/Users/study/pii-agent-demo-adr-016-pipeline-architecture/docs/adr/ADR-016-install-delete-pipeline-orchestration.md`:11,64,77-78,139 — [MINOR] Internal stale-reference inconsistency: the ADR says auxiliary docs such as `orchestrator-design` were deleted, but line 64 still cites `orchestrator §1.2` as the canonical source for snapshot JSON shape — Remove the `orchestrator §1.2` reference or replace it with an existing current doc/inline definition.
10. `/Users/study/pii-agent-demo-adr-016-pipeline-architecture/docs/adr/ADR-016-install-delete-pipeline-orchestration.md`:29,51-53,89,112-113 — [NIT] No objective contradiction found in the workerPool/slotCap hard-cap wording: it distinguishes TerraformWorker capacity, BFF submission throttle, and the fact that BFF does not guarantee the worker global cap — No correction required unless terminology changes from BFF to the separate server.
11. `Math/formula class overall` — [NIT] No objective error found in the proposal's `M + P - 1` running-pipeline overshoot formula under its explicit premise that each pod can promote at most one pipeline in the race window — No correction required beyond renaming `N` to `P` for pod count.
12. `Lease invariant class overall` — [NIT] No objective error found in `leaseDuration > maxApiCallTimeout + safetyMargin` as a sufficient condition for preventing lease expiry before a bounded external call returns/times out, assuming `safetyMargin` includes clock skew and report latency — Optionally spell out what `safetyMargin` covers.
13. `Claim SQL / tx2 CAS class overall` — [NIT] No objective SQL error found in the `FOR UPDATE SKIP LOCKED` claim pattern or the tx2 ownership-token check as written, assuming the commented claim update runs in the same transaction as the `SELECT ... FOR UPDATE` and the token is changed on reclaim — No correction required beyond making those assumptions explicit if desired.
