#!/usr/bin/env bash
set -euo pipefail

repo_root="$(git rev-parse --show-toplevel)"
cd "${repo_root}"

# bash scripts/sync-claude-skills-to-codex.sh || {
#   echo "[post-checkout] skill mirror failed (non-blocking)" >&2
# }
