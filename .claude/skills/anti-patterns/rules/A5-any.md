# A5. `any` (project rule)

Severity: 🔴 critical

Use `unknown` + a type guard, or a concrete type.

**ESLint**: `@typescript-eslint/no-explicit-any`

**CLAUDE.md**: explicitly forbidden — any `any` annotation blocks merge.
