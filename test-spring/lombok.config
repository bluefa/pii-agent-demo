# Propagate Spring's nullability annotations from fields onto the Lombok-generated
# getters and builder setters, so the @Nullable/@NonNull contract declared once on a
# field is visible to tooling on every generated accessor (null-safety, Stage D).
lombok.copyableAnnotations += org.springframework.lang.Nullable
lombok.copyableAnnotations += org.springframework.lang.NonNull
